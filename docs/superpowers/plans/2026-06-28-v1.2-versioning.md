# RoboKeep v1.2 — Versioning (snapshot hard-link) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Conservare uno storico datato dei backup tramite snapshot con hard-link su NTFS locale, con ritenzione semplice e apertura degli snapshot in Esplora risorse.

**Architecture:** Logica pura nei servizi Core (`SnapshotName`, `SnapshotPlanner`) in TDD; primitive IO Windows (`HardLink`, `HardLinkSupport`, `HardLinkCloner`); orchestrazione in `SnapshotService` che riusa `RobocopyRunner` con una destinazione effettiva (lo snapshot in costruzione). `BackupRunner` instrada i job versionati. UI sottile nel WPF.

**Tech Stack:** .NET 10 (`net10.0-windows`), WPF, WPF-UI, xUnit, P/Invoke `CreateHardLinkW` (kernel32), robocopy di sistema.

**Spec:** [docs/superpowers/specs/2026-06-28-v1.2-versioning-design.md](../specs/2026-06-28-v1.2-versioning-design.md)

---

## Note operative (specifiche di questo repo)

- **Test:** xUnit; pattern `IDisposable` + cartella temp in `Path.GetTempPath()` con `Guid` (vedi `src/RoboKeep.Tests/LastResultStoreTests.cs`). I test di integrazione di questa tappa usano `Path.GetTempPath()` (di norma NTFS su `C:`).
- **Build/test (PowerShell):** `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj` · `dotnet build src/RoboKeep.sln -c Debug`.
- **L'app blocca il build se aperta.** Prima di ogni `dotnet build` del progetto WPF: `Get-Process RoboKeep -ErrorAction SilentlyContinue`; se presente, fermarsi e chiedere all'utente di chiuderla.
- **Commit:** messaggi **senza virgolette doppie**; chiudere con `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. Branch: `v1.2-versioning`.
- Servizi Core puri: il tempo è un parametro `now`, mai `DateTime.Now` interno.

---

## File Structure

**Core (nuovi):**
- `src/RoboKeep.Core/Services/HardLink.cs` — primitiva P/Invoke `CreateHardLinkW`
- `src/RoboKeep.Core/Services/HardLinkSupport.cs` — rilevamento idoneità destinazione (IO)
- `src/RoboKeep.Core/Services/HardLinkCloner.cs` — clone ricorsivo via hard-link (IO)
- `src/RoboKeep.Core/Services/SnapshotName.cs` — naming/parse snapshot (puro)
- `src/RoboKeep.Core/Services/SnapshotPlanner.cs` — selezione ritenzione (puro)
- `src/RoboKeep.Core/Services/SnapshotService.cs` — orchestrazione snapshot

**Core (modificati):**
- `src/RoboKeep.Core/Models/BackupJob.cs` — campi `Versioned`, `SnapshotKeepCount`, `SnapshotMaxAgeDays`
- `src/RoboKeep.Core/Services/RobocopyArgsBuilder.cs` — parametro `destinationOverride`
- `src/RoboKeep.Core/Services/RobocopyRunner.cs` — propaga `destinationOverride`
- `src/RoboKeep.Core/Services/BackupRunner.cs` — instradamento job versionati

**Test (nuovi):**
- `src/RoboKeep.Tests/HardLinkRobocopySpikeTests.cs`
- `src/RoboKeep.Tests/SnapshotNameTests.cs`
- `src/RoboKeep.Tests/SnapshotPlannerTests.cs`
- `src/RoboKeep.Tests/HardLinkSupportTests.cs`
- `src/RoboKeep.Tests/HardLinkClonerTests.cs`
- `src/RoboKeep.Tests/SnapshotServiceTests.cs`
- `src/RoboKeep.Tests/RobocopyArgsOverrideTests.cs`
- estensione round-trip in `src/RoboKeep.Tests/AppSettingsRoundTripTests.cs` o nuovo `BackupJobVersioningRoundTripTests.cs`

**WPF (modificati):**
- `src/RoboKeep/AppHost.cs` — wiring `SnapshotService`
- `src/RoboKeep/JobEditorWindow.xaml(.cs)` + `ViewModels/JobEditorViewModel.cs` — toggle versioning + N/giorni + controllo idoneità
- `src/RoboKeep/MainWindow.xaml(.cs)` o nuova finestra — sfoglia versioni + Apri in Esplora
- `src/RoboKeep/Localization/Loc.cs` — chiavi nuove (5 lingue)

**Docs:** `README.md`, `readmeita.md`

---

## Task 1: SPIKE — robocopy rompe gli hard-link aggiornando un file

Gate dell'intera tappa. Crea la primitiva `HardLink` e un test di integrazione reale.

**Files:**
- Create: `src/RoboKeep.Core/Services/HardLink.cs`
- Test: `src/RoboKeep.Tests/HardLinkRobocopySpikeTests.cs`

- [ ] **Step 1: Create the hard-link primitive**

Create `src/RoboKeep.Core/Services/HardLink.cs`:

```csharp
using System.Runtime.InteropServices;

namespace RoboKeep.Core.Services;

/// <summary>Primitiva per creare hard-link NTFS (P/Invoke CreateHardLinkW).</summary>
public static class HardLink
{
    [DllImport("kernel32.dll", SetLastError = true, CharSet = CharSet.Unicode)]
    [return: MarshalAs(UnmanagedType.Bool)]
    private static extern bool CreateHardLinkW(string lpFileName, string lpExistingFileName, IntPtr lpSecurityAttributes);

    /// <summary>Crea un hard-link <paramref name="linkPath"/> che punta a <paramref name="targetPath"/>. Lancia in caso di errore.</summary>
    public static void Create(string linkPath, string targetPath)
    {
        if (!CreateHardLinkW(linkPath, targetPath, IntPtr.Zero))
            throw new IOException($"CreateHardLink fallito (Win32 {Marshal.GetLastWin32Error()}): {linkPath} -> {targetPath}");
    }

    /// <summary>Come <see cref="Create"/> ma ritorna false invece di lanciare.</summary>
    public static bool TryCreate(string linkPath, string targetPath)
        => CreateHardLinkW(linkPath, targetPath, IntPtr.Zero);
}
```

- [ ] **Step 2: Write the spike integration test**

Create `src/RoboKeep.Tests/HardLinkRobocopySpikeTests.cs`:

```csharp
using System.Diagnostics;
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public class HardLinkRobocopySpikeTests : IDisposable
{
    private readonly string _root = Path.Combine(Path.GetTempPath(), "RbcSpike_" + Guid.NewGuid().ToString("N"));

    public void Dispose()
    {
        if (Directory.Exists(_root)) Directory.Delete(_root, recursive: true);
    }

    private static int Robocopy(string source, string dest, params string[] extra)
    {
        var psi = new ProcessStartInfo
        {
            FileName = Path.Combine(Environment.SystemDirectory, "Robocopy.exe"),
            UseShellExecute = false, CreateNoWindow = true,
            RedirectStandardOutput = true, RedirectStandardError = true,
        };
        psi.ArgumentList.Add(source); psi.ArgumentList.Add(dest);
        psi.ArgumentList.Add("/MIR");
        foreach (var e in extra) psi.ArgumentList.Add(e);
        using var p = Process.Start(psi)!;
        p.StandardOutput.ReadToEnd(); p.StandardError.ReadToEnd();
        p.WaitForExit();
        return p.ExitCode;
    }

    [Fact]
    public void Robocopy_Update_BreaksHardLink_PreservingOldVersionInSnapshot()
    {
        var source = Path.Combine(_root, "src");
        var dest = Path.Combine(_root, "dest");
        var snap = Path.Combine(_root, "snap");
        Directory.CreateDirectory(source);

        // v1 nella sorgente, mirror in dest
        File.WriteAllText(Path.Combine(source, "f.txt"), "v1");
        Robocopy(source, dest);
        Assert.Equal("v1", File.ReadAllText(Path.Combine(dest, "f.txt")));

        // snapshot: hard-link di dest\f.txt
        Directory.CreateDirectory(snap);
        HardLink.Create(Path.Combine(snap, "f.txt"), Path.Combine(dest, "f.txt"));
        Assert.Equal("v1", File.ReadAllText(Path.Combine(snap, "f.txt")));

        // la sorgente cambia a v2, rimirror in dest
        File.WriteAllText(Path.Combine(source, "f.txt"), "v2");
        Robocopy(source, dest);

        // ASSUNZIONE CRITICA: dest aggiornato a v2, lo snapshot conserva v1 (hard-link rotto da robocopy)
        Assert.Equal("v2", File.ReadAllText(Path.Combine(dest, "f.txt")));
        Assert.Equal("v1", File.ReadAllText(Path.Combine(snap, "f.txt")));
    }
}
```

- [ ] **Step 3: Run the spike**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter HardLinkRobocopySpikeTests`
Expected: PASS. **Se fallisce** (lo snapshot mostra `v2`): robocopy modifica in-place — FERMARSI e segnalare al coordinatore: il meccanismo va rivisto (es. cancellare il file in dest prima del rimirror). Non proseguire con gli altri task.

- [ ] **Step 4: Commit**

```
git add src/RoboKeep.Core/Services/HardLink.cs src/RoboKeep.Tests/HardLinkRobocopySpikeTests.cs
git commit -m "spike(v1.2): robocopy rompe gli hard-link aggiornando un file (gate versioning)"
```

---

## Task 2: SnapshotName (naming/parse, puro)

**Files:**
- Create: `src/RoboKeep.Core/Services/SnapshotName.cs`
- Test: `src/RoboKeep.Tests/SnapshotNameTests.cs`

- [ ] **Step 1: Write the failing test**

Create `src/RoboKeep.Tests/SnapshotNameTests.cs`:

```csharp
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public class SnapshotNameTests
{
    [Fact]
    public void For_FormatsTimestamp()
        => Assert.Equal("2026-06-28_150000", SnapshotName.For(new DateTime(2026, 6, 28, 15, 0, 0)));

    [Fact]
    public void TryParse_ValidName_ReturnsDate()
    {
        Assert.True(SnapshotName.TryParse("2026-06-28_150000", out var d));
        Assert.Equal(new DateTime(2026, 6, 28, 15, 0, 0), d);
    }

    [Fact]
    public void TryParse_Invalid_ReturnsFalse()
        => Assert.False(SnapshotName.TryParse("latest", out _));

    [Fact]
    public void IsInProgress_DetectsSuffix()
    {
        Assert.True(SnapshotName.IsInProgress("2026-06-28_150000.inprogress"));
        Assert.False(SnapshotName.IsInProgress("2026-06-28_150000"));
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter SnapshotNameTests`
Expected: FAIL — `SnapshotName` non esiste.

- [ ] **Step 3: Implement**

Create `src/RoboKeep.Core/Services/SnapshotName.cs`:

```csharp
using System.Globalization;

namespace RoboKeep.Core.Services;

/// <summary>Convenzioni di naming degli snapshot datati (cartelle in destinazione).</summary>
public static class SnapshotName
{
    public const string Format = "yyyy-MM-dd_HHmmss";
    public const string InProgressSuffix = ".inprogress";

    public static string For(DateTime now) => now.ToString(Format, CultureInfo.InvariantCulture);

    public static bool IsInProgress(string name)
        => name.EndsWith(InProgressSuffix, StringComparison.OrdinalIgnoreCase);

    public static bool TryParse(string name, out DateTime date)
        => DateTime.TryParseExact(name, Format, CultureInfo.InvariantCulture, DateTimeStyles.None, out date);
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter SnapshotNameTests`
Expected: PASS (4 test)

- [ ] **Step 5: Commit**

```
git add src/RoboKeep.Core/Services/SnapshotName.cs src/RoboKeep.Tests/SnapshotNameTests.cs
git commit -m "feat(v1.2): SnapshotName naming/parse in TDD"
```

---

## Task 3: SnapshotPlanner (ritenzione, puro)

**Files:**
- Create: `src/RoboKeep.Core/Services/SnapshotPlanner.cs`
- Test: `src/RoboKeep.Tests/SnapshotPlannerTests.cs`

- [ ] **Step 1: Write the failing test**

Create `src/RoboKeep.Tests/SnapshotPlannerTests.cs`:

```csharp
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public class SnapshotPlannerTests
{
    private static readonly DateTime Now = new(2026, 6, 28, 12, 0, 0);

    // Snapshot a -1,-2,-5,-10 giorni rispetto a Now
    private static readonly string[] Snaps =
    {
        SnapshotName.For(Now.AddDays(-1)),
        SnapshotName.For(Now.AddDays(-2)),
        SnapshotName.For(Now.AddDays(-5)),
        SnapshotName.For(Now.AddDays(-10)),
    };

    [Fact]
    public void KeepCount_DeletesOldestBeyondN()
    {
        var del = SnapshotPlanner.SnapshotsToDelete(Snaps, keepCount: 2, maxAgeDays: 0, Now);
        Assert.Equal(2, del.Count);
        Assert.Contains(SnapshotName.For(Now.AddDays(-5)), del);
        Assert.Contains(SnapshotName.For(Now.AddDays(-10)), del);
    }

    [Fact]
    public void MaxAgeDays_DeletesOlderThanThreshold()
    {
        var del = SnapshotPlanner.SnapshotsToDelete(Snaps, keepCount: 0, maxAgeDays: 3, Now);
        Assert.Equal(2, del.Count); // -5 e -10 giorni
        Assert.Contains(SnapshotName.For(Now.AddDays(-5)), del);
        Assert.Contains(SnapshotName.For(Now.AddDays(-10)), del);
    }

    [Fact]
    public void BothLimits_UnionOfDeletions()
    {
        var del = SnapshotPlanner.SnapshotsToDelete(Snaps, keepCount: 3, maxAgeDays: 3, Now);
        // keep 3 -> elimina -10; age>3 -> elimina -5 e -10; unione = {-5,-10}
        Assert.Equal(2, del.Count);
    }

    [Fact]
    public void NoLimits_DeletesNothing()
        => Assert.Empty(SnapshotPlanner.SnapshotsToDelete(Snaps, keepCount: 0, maxAgeDays: 0, Now));

    [Fact]
    public void IgnoresInProgressAndInvalid()
    {
        var names = new[] { "2026-06-28_120000.inprogress", "latest", SnapshotName.For(Now.AddDays(-10)) };
        var del = SnapshotPlanner.SnapshotsToDelete(names, keepCount: 0, maxAgeDays: 3, Now);
        Assert.Equal(new[] { SnapshotName.For(Now.AddDays(-10)) }, del);
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter SnapshotPlannerTests`
Expected: FAIL — `SnapshotPlanner` non esiste.

- [ ] **Step 3: Implement**

Create `src/RoboKeep.Core/Services/SnapshotPlanner.cs`:

```csharp
namespace RoboKeep.Core.Services;

/// <summary>
/// Decide quali snapshot eliminare in base alla ritenzione. Funzione pura: il tempo è un
/// parametro. Ignora le cartelle .inprogress e i nomi non parsabili.
/// </summary>
public static class SnapshotPlanner
{
    public static IReadOnlyList<string> SnapshotsToDelete(
        IEnumerable<string> existingNames, int keepCount, int maxAgeDays, DateTime now)
    {
        var snaps = existingNames
            .Where(n => !SnapshotName.IsInProgress(n) && SnapshotName.TryParse(n, out _))
            .Select(n => { SnapshotName.TryParse(n, out var d); return (Name: n, Date: d); })
            .OrderByDescending(s => s.Date)
            .ToList();

        var toDelete = new HashSet<string>(StringComparer.OrdinalIgnoreCase);

        if (keepCount > 0)
            foreach (var s in snaps.Skip(keepCount))
                toDelete.Add(s.Name);

        if (maxAgeDays > 0)
            foreach (var s in snaps.Where(s => (now - s.Date).TotalDays > maxAgeDays))
                toDelete.Add(s.Name);

        return toDelete.ToList();
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter SnapshotPlannerTests`
Expected: PASS (5 test)

- [ ] **Step 5: Commit**

```
git add src/RoboKeep.Core/Services/SnapshotPlanner.cs src/RoboKeep.Tests/SnapshotPlannerTests.cs
git commit -m "feat(v1.2): SnapshotPlanner ritenzione (ultime-N + eta) in TDD"
```

---

## Task 4: HardLinkSupport (rilevamento idoneità, IO)

**Files:**
- Create: `src/RoboKeep.Core/Services/HardLinkSupport.cs`
- Test: `src/RoboKeep.Tests/HardLinkSupportTests.cs`

- [ ] **Step 1: Write the failing test**

Create `src/RoboKeep.Tests/HardLinkSupportTests.cs`:

```csharp
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public class HardLinkSupportTests : IDisposable
{
    private readonly string _dir = Path.Combine(Path.GetTempPath(), "RbcHls_" + Guid.NewGuid().ToString("N"));

    public HardLinkSupportTests() => Directory.CreateDirectory(_dir);
    public void Dispose() { if (Directory.Exists(_dir)) Directory.Delete(_dir, recursive: true); }

    [Fact]
    public void TempDir_OnNtfs_IsSupported()
        => Assert.True(HardLinkSupport.IsSupported(_dir)); // %TEMP% e' di norma NTFS

    [Fact]
    public void MissingDir_IsNotSupported()
        => Assert.False(HardLinkSupport.IsSupported(Path.Combine(_dir, "nope")));

    [Fact]
    public void LeavesNoProbeFiles()
    {
        HardLinkSupport.IsSupported(_dir);
        Assert.Empty(Directory.GetFiles(_dir)); // i file di prova vengono ripuliti
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter HardLinkSupportTests`
Expected: FAIL — `HardLinkSupport` non esiste.

- [ ] **Step 3: Implement**

Create `src/RoboKeep.Core/Services/HardLinkSupport.cs`:

```csharp
namespace RoboKeep.Core.Services;

/// <summary>
/// Rileva se una cartella di destinazione supporta gli hard-link, con un test funzionale
/// (crea un file di prova, prova a linkarlo, pulisce). Più affidabile del solo controllo NTFS.
/// </summary>
public static class HardLinkSupport
{
    public static bool IsSupported(string directory)
    {
        try
        {
            if (string.IsNullOrWhiteSpace(directory) || !Directory.Exists(directory))
                return false;

            var id = Guid.NewGuid().ToString("N");
            var probe = Path.Combine(directory, $".robokeep-probe-{id}");
            var link = Path.Combine(directory, $".robokeep-link-{id}");
            File.WriteAllText(probe, "x");
            try
            {
                return HardLink.TryCreate(link, probe);
            }
            finally
            {
                try { File.Delete(link); } catch { /* best-effort */ }
                try { File.Delete(probe); } catch { /* best-effort */ }
            }
        }
        catch
        {
            return false;
        }
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter HardLinkSupportTests`
Expected: PASS (3 test)

- [ ] **Step 5: Commit**

```
git add src/RoboKeep.Core/Services/HardLinkSupport.cs src/RoboKeep.Tests/HardLinkSupportTests.cs
git commit -m "feat(v1.2): HardLinkSupport rilevamento idoneita destinazione"
```

---

## Task 5: HardLinkCloner (clone ricorsivo via hard-link, IO)

**Files:**
- Create: `src/RoboKeep.Core/Services/HardLinkCloner.cs`
- Test: `src/RoboKeep.Tests/HardLinkClonerTests.cs`

- [ ] **Step 1: Write the failing test**

Create `src/RoboKeep.Tests/HardLinkClonerTests.cs`:

```csharp
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public class HardLinkClonerTests : IDisposable
{
    private readonly string _root = Path.Combine(Path.GetTempPath(), "RbcClone_" + Guid.NewGuid().ToString("N"));

    public void Dispose() { if (Directory.Exists(_root)) Directory.Delete(_root, recursive: true); }

    [Fact]
    public void Clone_RecreatesTree_WithHardLinkedFiles()
    {
        var src = Path.Combine(_root, "src");
        var dst = Path.Combine(_root, "dst");
        Directory.CreateDirectory(Path.Combine(src, "sub"));
        File.WriteAllText(Path.Combine(src, "a.txt"), "AAA");
        File.WriteAllText(Path.Combine(src, "sub", "b.txt"), "BBB");

        HardLinkCloner.Clone(src, dst);

        // struttura e contenuti replicati
        Assert.Equal("AAA", File.ReadAllText(Path.Combine(dst, "a.txt")));
        Assert.Equal("BBB", File.ReadAllText(Path.Combine(dst, "sub", "b.txt")));

        // sono hard-link: modificando il contenuto via uno, l'altro vede lo stesso dato
        // (verifica dell'identita': scrivere sul file sorgente IN-PLACE si riflette sul clone)
        using (var fs = new FileStream(Path.Combine(src, "a.txt"), FileMode.Open, FileAccess.Write))
        {
            var bytes = System.Text.Encoding.ASCII.GetBytes("ZZZ");
            fs.Write(bytes, 0, bytes.Length);
        }
        Assert.Equal("ZZZ", File.ReadAllText(Path.Combine(dst, "a.txt")));
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter HardLinkClonerTests`
Expected: FAIL — `HardLinkCloner` non esiste.

- [ ] **Step 3: Implement**

Create `src/RoboKeep.Core/Services/HardLinkCloner.cs`:

```csharp
namespace RoboKeep.Core.Services;

/// <summary>
/// Clona ricorsivamente una cartella: ricrea le sottocartelle come reali e collega i file
/// con hard-link (zero copia di dati). Sorgente e destinazione devono stare sullo stesso volume NTFS.
/// </summary>
public static class HardLinkCloner
{
    public static void Clone(string sourceDir, string destDir)
    {
        Directory.CreateDirectory(destDir);

        foreach (var dir in Directory.EnumerateDirectories(sourceDir, "*", SearchOption.AllDirectories))
        {
            var rel = Path.GetRelativePath(sourceDir, dir);
            Directory.CreateDirectory(Path.Combine(destDir, rel));
        }

        foreach (var file in Directory.EnumerateFiles(sourceDir, "*", SearchOption.AllDirectories))
        {
            var rel = Path.GetRelativePath(sourceDir, file);
            var target = Path.Combine(destDir, rel);
            Directory.CreateDirectory(Path.GetDirectoryName(target)!);
            HardLink.Create(target, file);
        }
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter HardLinkClonerTests`
Expected: PASS

- [ ] **Step 5: Commit**

```
git add src/RoboKeep.Core/Services/HardLinkCloner.cs src/RoboKeep.Tests/HardLinkClonerTests.cs
git commit -m "feat(v1.2): HardLinkCloner clone ricorsivo via hard-link"
```

---

## Task 6: Campi BackupJob + round-trip

**Files:**
- Modify: `src/RoboKeep.Core/Models/BackupJob.cs`
- Test: `src/RoboKeep.Tests/BackupJobVersioningRoundTripTests.cs`

- [ ] **Step 1: Write the failing test**

Create `src/RoboKeep.Tests/BackupJobVersioningRoundTripTests.cs`:

```csharp
using RoboKeep.Core.Models;
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public class BackupJobVersioningRoundTripTests : IDisposable
{
    private readonly string _dir = Path.Combine(Path.GetTempPath(), "RbcVer_" + Guid.NewGuid().ToString("N"));
    public void Dispose() { if (Directory.Exists(_dir)) Directory.Delete(_dir, recursive: true); }

    [Fact]
    public void Defaults_VersioningOff()
    {
        var j = new BackupJob();
        Assert.False(j.Versioned);
        Assert.Equal(0, j.SnapshotKeepCount);
        Assert.Equal(0, j.SnapshotMaxAgeDays);
    }

    [Fact]
    public void NewFields_RoundTrip()
    {
        var path = Path.Combine(_dir, "config.json");
        var store = new ConfigStore(path);
        store.Save(new AppConfig { Jobs = { new BackupJob { Name = "V", Source = @"D:\s", Destination = @"E:\d",
            Versioned = true, SnapshotKeepCount = 14, SnapshotMaxAgeDays = 90 } } });

        var j = store.Load().Jobs[0];
        Assert.True(j.Versioned);
        Assert.Equal(14, j.SnapshotKeepCount);
        Assert.Equal(90, j.SnapshotMaxAgeDays);
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter BackupJobVersioningRoundTripTests`
Expected: FAIL — campi inesistenti.

- [ ] **Step 3: Add the fields**

In `src/RoboKeep.Core/Models/BackupJob.cs`, dopo `CredentialId` (in fondo alla classe):

```csharp
    /// <summary>true → mantiene snapshot datati con hard-link (versioning). Richiede destinazione NTFS locale.</summary>
    public bool Versioned { get; set; }

    /// <summary>Numero massimo di snapshot da conservare. 0 = nessun limite di numero.</summary>
    public int SnapshotKeepCount { get; set; }

    /// <summary>Elimina gli snapshot più vecchi di questi giorni. 0 = nessun limite di età.</summary>
    public int SnapshotMaxAgeDays { get; set; }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter BackupJobVersioningRoundTripTests`
Expected: PASS (2 test)

- [ ] **Step 5: Commit**

```
git add src/RoboKeep.Core/Models/BackupJob.cs src/RoboKeep.Tests/BackupJobVersioningRoundTripTests.cs
git commit -m "feat(v1.2): campi versioning su BackupJob + round-trip"
```

---

## Task 7: destinationOverride in args/runner

**Files:**
- Modify: `src/RoboKeep.Core/Services/RobocopyArgsBuilder.cs`
- Modify: `src/RoboKeep.Core/Services/RobocopyRunner.cs`
- Test: `src/RoboKeep.Tests/RobocopyArgsOverrideTests.cs`

- [ ] **Step 1: Write the failing test**

Create `src/RoboKeep.Tests/RobocopyArgsOverrideTests.cs`:

```csharp
using RoboKeep.Core.Models;
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public class RobocopyArgsOverrideTests
{
    private static BackupJob Job() => new() { Name = "J", Source = @"D:\s", Destination = @"E:\d" };

    [Fact]
    public void Build_WithoutOverride_UsesJobDestination()
    {
        var args = RobocopyArgsBuilder.Build(Job());
        Assert.Equal(@"D:\s", args[0]);
        Assert.Equal(@"E:\d", args[1]);
    }

    [Fact]
    public void Build_WithOverride_UsesOverrideAsDestination()
    {
        var args = RobocopyArgsBuilder.Build(Job(), destinationOverride: @"E:\d\2026-06-28_150000.inprogress");
        Assert.Equal(@"D:\s", args[0]);
        Assert.Equal(@"E:\d\2026-06-28_150000.inprogress", args[1]);
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter RobocopyArgsOverrideTests`
Expected: FAIL — `Build` non ha `destinationOverride`.

- [ ] **Step 3: Add the override to Build and BuildForceCopyPass**

In `src/RoboKeep.Core/Services/RobocopyArgsBuilder.cs`:

`Build` — cambia la firma e la riga del dest:

```csharp
    public static IReadOnlyList<string> Build(
        BackupJob job, bool dryRun = false, string? logFile = null, string? destinationOverride = null)
    {
        ArgumentNullException.ThrowIfNull(job);

        var source = (job.Source ?? "").Trim();
        var dest = (destinationOverride ?? job.Destination ?? "").Trim();
```

`BuildForceCopyPass` — stessa cosa:

```csharp
    public static IReadOnlyList<string> BuildForceCopyPass(
        BackupJob job, IReadOnlyList<string> filters, bool dryRun = false, string? logFile = null,
        string? destinationOverride = null)
    {
        ArgumentNullException.ThrowIfNull(job);
        ArgumentNullException.ThrowIfNull(filters);

        var source = (job.Source ?? "").Trim();
        var dest = (destinationOverride ?? job.Destination ?? "").Trim();
```

(Il resto dei due metodi resta invariato.)

- [ ] **Step 4: Thread it through RobocopyRunner**

In `src/RoboKeep.Core/Services/RobocopyRunner.cs`, `RunAsync`: aggiungi il parametro e passalo alle due `Build`:

```csharp
    public async Task<RobocopyRunResult> RunAsync(
        BackupJob job, bool dryRun = false, IProgress<string>? progress = null, CancellationToken ct = default,
        string? destinationOverride = null)
    {
        var started = DateTime.Now;

        var (exit1, text1) = await RunPassAsync(
            RobocopyArgsBuilder.Build(job, dryRun, destinationOverride: destinationOverride), progress, ct)
            .ConfigureAwait(false);
```

E nella passata forza-copia:

```csharp
                var pass2Args = RobocopyArgsBuilder.BuildForceCopyPass(
                    job, plan.Filters, dryRun, destinationOverride: destinationOverride);
```

- [ ] **Step 5: Run test + build**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter RobocopyArgsOverrideTests` → PASS (2)
Run: `dotnet build src/RoboKeep.Core/RoboKeep.Core.csproj -c Debug` → Build succeeded.

- [ ] **Step 6: Commit**

```
git add src/RoboKeep.Core/Services/RobocopyArgsBuilder.cs src/RoboKeep.Core/Services/RobocopyRunner.cs src/RoboKeep.Tests/RobocopyArgsOverrideTests.cs
git commit -m "feat(v1.2): destinationOverride in RobocopyArgsBuilder e RobocopyRunner"
```

---

## Task 8: SnapshotService (orchestrazione) + test di integrazione

**Files:**
- Create: `src/RoboKeep.Core/Services/SnapshotService.cs`
- Test: `src/RoboKeep.Tests/SnapshotServiceTests.cs`

- [ ] **Step 1: Implement the service**

Create `src/RoboKeep.Core/Services/SnapshotService.cs`:

```csharp
using RoboKeep.Core.Models;

namespace RoboKeep.Core.Services;

/// <summary>
/// Esegue un job versionato producendo uno snapshot datato con hard-link:
/// clona lo snapshot precedente, robocopy nella cartella .inprogress, rinomina a esito
/// riuscito, applica la ritenzione. Assume destinazione idonea agli hard-link (verificata a monte).
/// </summary>
public sealed class SnapshotService
{
    private readonly RobocopyRunner _runner;

    public SnapshotService(RobocopyRunner runner) => _runner = runner;

    public async Task<RobocopyRunResult> RunVersionedAsync(
        BackupJob job, IProgress<string>? progress = null, CancellationToken ct = default)
    {
        var dest = (job.Destination ?? "").Trim();
        Directory.CreateDirectory(dest);

        var existing = Directory.GetDirectories(dest).Select(Path.GetFileName).Where(n => n is not null).Cast<string>().ToList();

        var prevName = existing
            .Where(n => !SnapshotName.IsInProgress(n) && SnapshotName.TryParse(n, out _))
            .OrderByDescending(n => { SnapshotName.TryParse(n, out var d); return d; })
            .FirstOrDefault();

        var now = DateTime.Now;
        var newName = SnapshotName.For(now);
        var curr = Path.Combine(dest, newName + SnapshotName.InProgressSuffix);
        Directory.CreateDirectory(curr);

        if (prevName is not null)
        {
            progress?.Report($"[versioning] clono lo snapshot precedente ({prevName}) via hard-link...");
            HardLinkCloner.Clone(Path.Combine(dest, prevName), curr);
        }

        var run = await _runner.RunAsync(job, dryRun: false, progress, ct, destinationOverride: curr).ConfigureAwait(false);

        if (run.Result.Success)
        {
            var final = Path.Combine(dest, newName);
            if (Directory.Exists(final))
                final = Path.Combine(dest, newName + "_" + Guid.NewGuid().ToString("N")[..4]);
            Directory.Move(curr, final);
            progress?.Report($"[versioning] snapshot creato: {Path.GetFileName(final)}");

            var after = Directory.GetDirectories(dest).Select(Path.GetFileName).Cast<string>();
            foreach (var name in SnapshotPlanner.SnapshotsToDelete(after, job.SnapshotKeepCount, job.SnapshotMaxAgeDays, now))
            {
                try
                {
                    Directory.Delete(Path.Combine(dest, name), recursive: true);
                    progress?.Report($"[versioning] rimosso snapshot vecchio: {name}");
                }
                catch (Exception ex)
                {
                    progress?.Report($"[versioning] impossibile rimuovere {name}: {ex.Message}");
                }
            }
        }
        else
        {
            progress?.Report($"[versioning] backup non riuscito: snapshot incompleto lasciato come {Path.GetFileName(curr)}.");
        }

        return run;
    }
}
```

- [ ] **Step 2: Write the integration test**

Create `src/RoboKeep.Tests/SnapshotServiceTests.cs`:

```csharp
using RoboKeep.Core.Models;
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public class SnapshotServiceTests : IDisposable
{
    private readonly string _root = Path.Combine(Path.GetTempPath(), "RbcSnap_" + Guid.NewGuid().ToString("N"));
    public void Dispose() { if (Directory.Exists(_root)) Directory.Delete(_root, recursive: true); }

    private static string[] SnapshotDirs(string dest) =>
        Directory.GetDirectories(dest).Select(Path.GetFileName)
            .Where(n => n is not null && !SnapshotName.IsInProgress(n!) && SnapshotName.TryParse(n!, out _))
            .Cast<string>().OrderBy(n => n).ToArray();

    [Fact]
    public async Task TwoRuns_ProduceTwoSnapshots_AndVersionAChangedFile()
    {
        var source = Path.Combine(_root, "src");
        var dest = Path.Combine(_root, "dest");
        Directory.CreateDirectory(source);
        File.WriteAllText(Path.Combine(source, "f.txt"), "v1");

        var job = new BackupJob { Name = "V", Source = source, Destination = dest, Versioned = true };
        var svc = new SnapshotService(new RobocopyRunner());

        var r1 = await svc.RunVersionedAsync(job);
        Assert.True(r1.Result.Success || r1.Result.ExitCode <= 7); // robocopy: <8 = ok
        var afterFirst = SnapshotDirs(dest);
        Assert.Single(afterFirst);

        // assicura un timestamp diverso (naming al secondo)
        await Task.Delay(1100);
        File.WriteAllText(Path.Combine(source, "f.txt"), "v2");
        var r2 = await svc.RunVersionedAsync(job);

        var snaps = SnapshotDirs(dest);
        Assert.Equal(2, snaps.Length);
        // il più vecchio conserva v1, il più recente ha v2
        Assert.Equal("v1", File.ReadAllText(Path.Combine(dest, snaps[0], "f.txt")));
        Assert.Equal("v2", File.ReadAllText(Path.Combine(dest, snaps[1], "f.txt")));
    }

    [Fact]
    public async Task Retention_KeepCount1_DeletesOlderSnapshot()
    {
        var source = Path.Combine(_root, "src2");
        var dest = Path.Combine(_root, "dest2");
        Directory.CreateDirectory(source);
        File.WriteAllText(Path.Combine(source, "f.txt"), "a");

        var job = new BackupJob { Name = "V", Source = source, Destination = dest, Versioned = true, SnapshotKeepCount = 1 };
        var svc = new SnapshotService(new RobocopyRunner());

        await svc.RunVersionedAsync(job);
        await Task.Delay(1100);
        File.WriteAllText(Path.Combine(source, "f.txt"), "b");
        await svc.RunVersionedAsync(job);

        Assert.Single(SnapshotDirs(dest)); // keepCount=1 -> resta solo il più recente
        Assert.Equal("b", File.ReadAllText(Path.Combine(dest, SnapshotDirs(dest)[0], "f.txt")));
    }
}
```

- [ ] **Step 3: Run the tests**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter SnapshotServiceTests`
Expected: PASS (2 test). Note: usano robocopy reale + `Task.Delay` per timestamp distinti.

- [ ] **Step 4: Commit**

```
git add src/RoboKeep.Core/Services/SnapshotService.cs src/RoboKeep.Tests/SnapshotServiceTests.cs
git commit -m "feat(v1.2): SnapshotService orchestrazione snapshot + ritenzione (integrazione)"
```

---

## Task 9: Instradamento in BackupRunner + fallback run-time

**Files:**
- Modify: `src/RoboKeep.Core/Services/BackupRunner.cs`
- Modify: `src/RoboKeep/AppHost.cs`

- [ ] **Step 1: Inject SnapshotService into BackupRunner**

In `src/RoboKeep.Core/Services/BackupRunner.cs`, aggiungi un campo e parametro opzionale al costruttore:

```csharp
    private readonly SnapshotService? _snapshots;

    public BackupRunner(
        AppConfig config,
        RobocopyRunner runner,
        LogService log,
        EmailService email,
        CredentialService credentials,
        LastResultStore? results = null,
        SnapshotService? snapshots = null)
    {
        _config = config;
        _runner = runner;
        _log = log;
        _email = email;
        _credentials = credentials;
        _results = results;
        _snapshots = snapshots;
    }
```

- [ ] **Step 2: Route versioned jobs**

In `RunJobAsync`, sostituisci la riga `var run = await _runner.RunAsync(job, dryRun, progress, ct).ConfigureAwait(false);` con:

```csharp
            RobocopyRunResult run;
            if (job.Versioned && !dryRun && _snapshots is not null)
            {
                if (HardLinkSupport.IsSupported(job.Destination))
                {
                    run = await _snapshots.RunVersionedAsync(job, progress, ct).ConfigureAwait(false);
                }
                else
                {
                    progress?.Report("[versioning] ATTENZIONE: la destinazione non supporta gli hard-link. "
                        + "Eseguo un mirror semplice (nessuno snapshot). Usa una destinazione NTFS locale per le versioni.");
                    run = await _runner.RunAsync(job, dryRun, progress, ct).ConfigureAwait(false);
                }
            }
            else
            {
                run = await _runner.RunAsync(job, dryRun, progress, ct).ConfigureAwait(false);
            }
```

(Anteprima/dry-run di un job versionato: cade nel ramo `else` → solo elenco robocopy, nessuno snapshot.)

- [ ] **Step 3: Wire SnapshotService in AppHost**

In `src/RoboKeep/AppHost.cs`, metodo `BuildRunner`, costruisci il service e passalo:

```csharp
    public BackupRunner BuildRunner()
    {
        var planner = new ForceCopyPlanner(ForceCopyHashes);
        var runner = new RobocopyRunner(forceCopyPlanner: planner);
        var log = new LogService(Config.Settings);
        var email = new EmailService(Credentials);
        var snapshots = new SnapshotService(runner);
        return new BackupRunner(Config, runner, log, email, Credentials, Results, snapshots);
    }
```

- [ ] **Step 4: Build + full tests**

Verifica `Get-Process RoboKeep` (chiusa). Run: `dotnet build src/RoboKeep.sln -c Debug` → succeeded.
Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj` → tutti verdi.

- [ ] **Step 5: Commit**

```
git add src/RoboKeep.Core/Services/BackupRunner.cs src/RoboKeep/AppHost.cs
git commit -m "feat(v1.2): instradamento job versionati + fallback mirror su destinazione non idonea"
```

---

## Task 10: UI editor — toggle versioning + controllo idoneità

**Files:**
- Modify: `src/RoboKeep/ViewModels/JobEditorViewModel.cs`
- Modify: `src/RoboKeep/JobEditorWindow.xaml(.cs)`
- Modify: `src/RoboKeep/Localization/Loc.cs`

Leggi prima `JobEditorViewModel.cs` e `JobEditorWindow.xaml` per seguire il pattern esistente (come sono esposte e bindate le altre opzioni del job, es. Mirror/CopyAll/MultiThread).

- [ ] **Step 1: Espose le proprietà nel ViewModel**

In `src/RoboKeep/ViewModels/JobEditorViewModel.cs`, aggiungi proprietà pass-through verso il `BackupJob` in editing (stesso pattern delle altre, con notifica): `Versioned` (bool), `SnapshotKeepCount` (int), `SnapshotMaxAgeDays` (int).

- [ ] **Step 2: Controllo idoneità al toggle / al salvataggio**

Nel punto di validazione/salvataggio del job (cerca il metodo che valida prima di chiudere l'editor, es. `Validate()`/`OnSave`): se `Versioned` è true, verifica `RoboKeep.Core.Services.HardLinkSupport.IsSupported(Destination)`. Se false, **impedisci il salvataggio come versionato** mostrando il messaggio `{l:Tr Ver_DestNotSupported}` (l'utente sceglie una destinazione NTFS o disattiva il versioning). Segui il modo in cui l'editor già blocca/segnala input non validi.

- [ ] **Step 3: Controlli XAML**

In `src/RoboKeep/JobEditorWindow.xaml`, nella sezione opzioni, aggiungi (stile coerente con i controlli esistenti):
- `ui:ToggleSwitch` per `Versioned` con etichetta `{l:Tr Ver_Enable}`.
- due `ui:NumberBox`/`ui:TextBox` (mirrorando il controllo numerico già usato, es. MultiThread) per `SnapshotKeepCount` (`{l:Tr Ver_KeepCount}`) e `SnapshotMaxAgeDays` (`{l:Tr Ver_MaxAgeDays}`), idealmente abilitati solo quando `Versioned` è true (`IsEnabled="{Binding Versioned}"`).

- [ ] **Step 4: Chiavi Loc (5 lingue)**

In `src/RoboKeep/Localization/Loc.cs` (it, en, es, fr, de — copia il formato esistente):
- `Ver_Enable` — it "Mantieni versioni datate (snapshot)" · en "Keep dated versions (snapshots)" · es "Mantener versiones por fecha (instantáneas)" · fr "Conserver des versions datées (instantanés)" · de "Datierte Versionen behalten (Snapshots)"
- `Ver_KeepCount` — it "Numero massimo di versioni (0 = illimitate)" · en "Max number of versions (0 = unlimited)" · es "Número máximo de versiones (0 = ilimitadas)" · fr "Nombre max de versions (0 = illimité)" · de "Maximale Anzahl Versionen (0 = unbegrenzt)"
- `Ver_MaxAgeDays` — it "Elimina versioni più vecchie di (giorni, 0 = mai)" · en "Delete versions older than (days, 0 = never)" · es "Eliminar versiones anteriores a (días, 0 = nunca)" · fr "Supprimer les versions de plus de (jours, 0 = jamais)" · de "Versionen löschen älter als (Tage, 0 = nie)"
- `Ver_DestNotSupported` — it "La destinazione non supporta gli hard-link (serve NTFS locale): impossibile attivare le versioni qui. Scegli una destinazione NTFS o disattiva le versioni." · en "The destination does not support hard links (local NTFS required): versions cannot be enabled here. Choose an NTFS destination or turn versions off." · es "El destino no admite enlaces duros (se requiere NTFS local): no se pueden activar las versiones aquí. Elige un destino NTFS o desactiva las versiones." · fr "La destination ne prend pas en charge les liens physiques (NTFS local requis) : impossible d'activer les versions ici. Choisissez une destination NTFS ou désactivez les versions." · de "Das Ziel unterstützt keine Hardlinks (lokales NTFS erforderlich): Versionen können hier nicht aktiviert werden. Wähle ein NTFS-Ziel oder deaktiviere Versionen."

- [ ] **Step 5: Build + verifica + commit**

Verifica `Get-Process RoboKeep`. Run: `dotnet build src/RoboKeep.sln -c Debug` → succeeded (no XAML parse error). `dotnet test ...` verde.
Verifica manuale: attivando il toggle su una destinazione non-NTFS appare l'avviso e non si salva; su NTFS si salva.

```
git add src/RoboKeep/ViewModels/JobEditorViewModel.cs src/RoboKeep/JobEditorWindow.xaml src/RoboKeep/JobEditorWindow.xaml.cs src/RoboKeep/Localization/Loc.cs
git commit -m "feat(v1.2): editor job - versioning con controllo idoneita destinazione"
```

---

## Task 11: UI — sfoglia versioni e apri in Esplora

**Files:**
- Modify: `src/RoboKeep/MainWindow.xaml(.cs)` (o nuova finestra `SnapshotsWindow`)
- Modify: `src/RoboKeep/Localization/Loc.cs`

- [ ] **Step 1: Comando "Versioni..." per il job selezionato**

Per un job con `Versioned == true`, aggiungi un'azione (pulsante o voce di menu contestuale nella griglia job) `{l:Tr Ver_Browse}` abilitata solo se il job selezionato è versionato. Segui il pattern dei comandi esistenti in `MainViewModel`/`MainWindow`.

- [ ] **Step 2: Elenco snapshot + apertura**

L'azione apre un piccolo elenco (una `SnapshotsWindow` o un popup) che mostra le cartelle snapshot della destinazione del job, ordinate per data decrescente, ottenute con:

```csharp
var dest = job.Destination;
var snapshots = System.IO.Directory.Exists(dest)
    ? System.IO.Directory.GetDirectories(dest)
        .Select(System.IO.Path.GetFileName)
        .Where(n => n is not null && !RoboKeep.Core.Services.SnapshotName.IsInProgress(n!)
                    && RoboKeep.Core.Services.SnapshotName.TryParse(n!, out _))
        .Cast<string>()
        .OrderByDescending(n => n)
        .ToList()
    : new System.List<string>();
```

Ogni voce ha un pulsante **Apri in Esplora** che esegue:

```csharp
System.Diagnostics.Process.Start(new System.Diagnostics.ProcessStartInfo
{
    FileName = "explorer.exe",
    Arguments = System.IO.Path.Combine(job.Destination, snapshotName),
    UseShellExecute = true,
});
```

(Adatta i `using` ai pattern del file. Se preferisci, un doppio-click sulla riga apre Esplora.)

- [ ] **Step 3: Chiavi Loc**

In `Loc.cs` (5 lingue):
- `Ver_Browse` — it "Versioni..." · en "Versions..." · es "Versiones..." · fr "Versions..." · de "Versionen..."
- `Ver_OpenInExplorer` — it "Apri in Esplora risorse" · en "Open in File Explorer" · es "Abrir en el Explorador" · fr "Ouvrir dans l'Explorateur" · de "Im Explorer öffnen"
- `Ver_NoSnapshots` — it "Ancora nessuna versione per questo job." · en "No versions yet for this job." · es "Aún no hay versiones para este trabajo." · fr "Aucune version pour cette tâche pour l'instant." · de "Noch keine Versionen für diesen Job."

- [ ] **Step 4: Build + verifica + commit**

Verifica `Get-Process RoboKeep`. Run: `dotnet build src/RoboKeep.sln -c Debug` → succeeded. Verifica manuale: per un job versionato l'elenco mostra le date e "Apri in Esplora" apre la cartella giusta.

```
git add src/RoboKeep/MainWindow.xaml src/RoboKeep/MainWindow.xaml.cs src/RoboKeep/Localization/Loc.cs
git commit -m "feat(v1.2): sfoglia versioni e apri snapshot in Esplora risorse"
```

---

## Task 12: README (IT + EN)

**Files:**
- Modify: `readmeita.md`
- Modify: `README.md`

- [ ] **Step 1: Aggiorna la sezione Funzioni/Features e Limitazioni**

In entrambi i README:
- Aggiungi in **Funzioni/Features** una voce sul **versioning**: snapshot datati con hard-link su NTFS locale (i file immutati non occupano spazio extra), ritenzione per numero/età, ripristino aprendo la versione in Esplora risorse.
- In **Limitazioni/Limitations**, aggiorna la riga "Non è un sistema di versioni": ora il versioning **opzionale** esiste per destinazioni **NTFS locali**; rete/exFAT non ancora supportate per le versioni.

Mantieni intatti badge, immagini e link; testo IT e EN equivalenti.

- [ ] **Step 2: Commit**

```
git add README.md readmeita.md
git commit -m "docs(v1.2): documenta il versioning (snapshot hard-link)"
```

---

## Task 13: Verifica finale

- [ ] **Step 1: Suite completa** — `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj` → tutti verdi (inclusi spike, SnapshotName, SnapshotPlanner, HardLinkSupport, HardLinkCloner, SnapshotService, args override, round-trip).
- [ ] **Step 2: Build Release** — verifica `Get-Process RoboKeep`; `dotnet build src/RoboKeep.sln -c Release` → 0 errori.
- [ ] **Step 3: Smoke test manuale** — crea un job versionato verso una cartella NTFS locale; esegui due volte modificando un file in mezzo; verifica due cartelle datate, che la più vecchia conservi la versione precedente, e che "Versioni... → Apri in Esplora" funzioni. Prova la ritenzione (KeepCount basso). Prova il toggle su una destinazione non idonea (avviso + niente salvataggio versionato).
- [ ] **Step 4: Bump versione** — opzionale a tappa verificata: `<Version>` in `src/Directory.Build.props` a `1.2.0`.

---

## Self-Review (eseguita su questo piano)

**Copertura spec:**
- Meccanismo clona-poi-mirror → Task 1 (spike), 8 (service). ✓
- Layout cartelle datate + .inprogress → Task 2 (naming), 8. ✓
- SnapshotPlanner ritenzione → Task 3. ✓
- HardLinkCloner / HardLinkSupport → Task 5 / 4. ✓
- SnapshotService → Task 8. ✓
- destinationOverride args/runner → Task 7. ✓
- Campi BackupJob → Task 6. ✓
- Instradamento + fallback run-time → Task 9. ✓
- UI editor + controllo idoneità (blocco config) → Task 10. ✓
- Sfoglia versioni + Esplora → Task 11. ✓
- Ritenzione applicata → Task 8. ✓
- Dry-run senza snapshot → Task 9 (ramo else). ✓
- README → Task 12. ✓

**Coerenza tipi:** `HardLink.Create/TryCreate` (T1) usati da `HardLinkSupport` (T4) e `HardLinkCloner` (T5); `SnapshotName.For/TryParse/IsInProgress` (T2) usati da `SnapshotPlanner` (T3) e `SnapshotService` (T8); `destinationOverride` (T7) usato da `SnapshotService` (T8); `SnapshotService` (T8) iniettato in `BackupRunner` (T9) e wired in `AppHost` (T9). Coerente. ✓

**Note di adattamento:** i task UI (10–11) richiedono di adattarsi ai nomi reali di `JobEditorViewModel`/`JobEditorWindow` e alla struttura di `MainWindow`/`MainViewModel`; i task Core (1–9) sono completamente specificati.
