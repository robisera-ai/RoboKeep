# v1.5 Rotazione dei dischi — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** RoboKeep riconosce *quale disco* è collegato e non esegue mai un job sul disco sbagliato, anche quando due dischi a rotazione hanno la stessa lettera.

**Architecture:** Due unità piccole e separate in Core: `VolumeIdentity` (l'unico punto che tocca Windows: P/Invoke `GetVolumeNameForVolumeMountPointW`) e `VolumeGuard` (funzione pura che decide). `BackupRunner` interroga la guardia prima di lock/VSS/robocopy e, se il disco è sbagliato, restituisce un `JobResult` con `Skipped = true` senza toccare nulla. "Saltato" diventa un terzo esito di prima classe in UI, cronologia ed exit code.

**Tech Stack:** .NET 10 (net10.0-windows), WPF + WPF-UI, P/Invoke kernel32, xUnit.

**Spec:** `docs/superpowers/specs/2026-07-09-v1.5-rotazione-dischi-design.md`

**Regole di progetto (vincolanti):**
- Messaggi di commit SENZA doppi apici `"` (rompono PowerShell); here-string `@'...'@`; chiusura `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Test: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj` — **baseline 243 verdi**. Il progetto test referenzia sia Core sia WPF (dalla v1.4.1), quindi i ViewModel sono testabili.
- Build WPF: `dotnet build src/RoboKeep/RoboKeep.csproj -c Debug -v quiet` solo quando indicato. Se fallisce per file lock (MSB3027/MSB3021) l'app è aperta: **segnalarlo, NON terminare processi**.
- Stringhe UI in `src/RoboKeep/Localization/Loc.cs` (5 blocchi IT/EN/ES/FR/DE nello stesso file); stringhe di log in `src/RoboKeep.Core/CoreLoc.cs` (metodo `L(it,en,es,fr,de)`). Un test di parità verifica che ogni chiave esista in tutte e 5 le lingue: aggiungerle ovunque o la suite diventa rossa.
- Niente nuove `SymbolIcon` (validate a runtime, non dal compilatore).

---

### Task 1: VolumeIdentity — leggere l'identità del volume

**Files:**
- Create: `src/RoboKeep.Core/Services/VolumeIdentity.cs`
- Test: `src/RoboKeep.Tests/VolumeIdentityTests.cs`

- [ ] **Step 1: scrivi i test che falliscono**

```csharp
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public class VolumeIdentityTests
{
    [Fact]
    public void ForPath_SystemDrive_ReturnsVolumeIdAndLabel()
    {
        var info = VolumeIdentity.ForPath(@"C:\");
        Assert.NotNull(info);
        Assert.StartsWith(@"\\?\Volume{", info!.VolumeId);
        Assert.EndsWith(@"\", info.VolumeId);
        Assert.NotNull(info.Label); // puo' essere stringa vuota: un volume senza etichetta e' legittimo
    }

    [Fact]
    public void ForPath_SubfolderOfSameVolume_ReturnsSameId()
    {
        var root = VolumeIdentity.ForPath(@"C:\");
        var sub = VolumeIdentity.ForPath(@"C:\Windows");
        Assert.NotNull(root);
        Assert.NotNull(sub);
        Assert.Equal(root!.VolumeId, sub!.VolumeId);
    }

    [Fact]
    public void ForPath_UncPath_ReturnsNull()
        => Assert.Null(VolumeIdentity.ForPath(@"\\server\share\cartella"));

    [Theory]
    [InlineData(null)]
    [InlineData("")]
    [InlineData("   ")]
    public void ForPath_EmptyOrNull_ReturnsNull(string? path)
        => Assert.Null(VolumeIdentity.ForPath(path));

    [Fact]
    public void ForPath_UnmountedDriveLetter_ReturnsNull()
    {
        // Cerca una lettera libera partendo dal fondo: se non ce ne sono (improbabile), il
        // test non ha nulla da verificare e passa.
        var used = DriveInfo.GetDrives().Select(d => char.ToUpperInvariant(d.Name[0])).ToHashSet();
        var free = "ZYXWVU".FirstOrDefault(c => !used.Contains(c));
        if (free == '\0') return;
        Assert.Null(VolumeIdentity.ForPath($@"{free}:\qualsiasi"));
    }
}
```

- [ ] **Step 2: verifica che falliscano**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter VolumeIdentity`
Expected: FAIL di compilazione (`VolumeIdentity` non esiste).

- [ ] **Step 3: implementa** `src/RoboKeep.Core/Services/VolumeIdentity.cs`

```csharp
using System.Runtime.InteropServices;
using System.Text;

namespace RoboKeep.Core.Services;

/// <summary>Identità di un volume: identificativo univoco (immutabile) ed etichetta leggibile.</summary>
public sealed record VolumeInfo(string VolumeId, string Label);

/// <summary>
/// Legge l'identità del volume che ospita un percorso. Serve alla rotazione dei dischi: due
/// dischi esterni alternati prendono spesso la stessa lettera (E:), ma hanno identificativi
/// di volume diversi — assegnati alla formattazione e indipendenti dalla lettera.
/// Best-effort: qualunque problema restituisce null (nessun controllo possibile), mai
/// un'eccezione: una diagnostica non deve poter impedire un backup legittimo.
/// </summary>
public static class VolumeIdentity
{
    // Restituisce il nome del volume nella forma \\?\Volume{GUID}\ per un mount point (es. "E:\").
    [DllImport("kernel32.dll", SetLastError = true, CharSet = CharSet.Unicode)]
    [return: MarshalAs(UnmanagedType.Bool)]
    private static extern bool GetVolumeNameForVolumeMountPointW(
        string lpszVolumeMountPoint, StringBuilder lpszVolumeName, int cchBufferLength);

    /// <summary>Identità del volume che ospita <paramref name="path"/>, o null se non
    /// determinabile: percorso vuoto, UNC (nessun volume locale), disco non collegato.</summary>
    public static VolumeInfo? ForPath(string? path)
    {
        try
        {
            if (string.IsNullOrWhiteSpace(path)) return null;

            var root = Path.GetPathRoot(Path.GetFullPath(path));
            if (string.IsNullOrEmpty(root)) return null;
            // Share di rete: nessun volume locale da identificare.
            if (root.StartsWith(@"\\", StringComparison.Ordinal)) return null;
            // L'API richiede il backslash finale ("E:\", non "E:").
            if (!root.EndsWith('\\')) root += '\\';

            var buffer = new StringBuilder(64);
            if (!GetVolumeNameForVolumeMountPointW(root, buffer, buffer.Capacity)) return null;

            var id = buffer.ToString();
            if (string.IsNullOrEmpty(id)) return null;

            // L'etichetta e' solo informativa (modificabile e duplicabile): mai un criterio
            // di confronto. Un volume senza etichetta e' legittimo.
            var label = "";
            try { label = new DriveInfo(root).VolumeLabel ?? ""; } catch { }

            return new VolumeInfo(id, label);
        }
        catch { return null; }
    }
}
```

- [ ] **Step 4: verifica che passino**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter VolumeIdentity`
Expected: PASS (7 casi). Poi suite completa → 250, nessuna regressione.

- [ ] **Step 5: commit**

```powershell
git add src/RoboKeep.Core/Services/VolumeIdentity.cs src/RoboKeep.Tests/VolumeIdentityTests.cs
git commit -m @'
feat(v1.5): VolumeIdentity - identificativo ed etichetta del volume di un percorso

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 2: VolumeGuard — la decisione (puro)

**Files:**
- Create: `src/RoboKeep.Core/Services/VolumeGuard.cs`
- Test: `src/RoboKeep.Tests/VolumeGuardTests.cs`

- [ ] **Step 1: scrivi i test che falliscono**

```csharp
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public class VolumeGuardTests
{
    private const string IdA = @"\\?\Volume{aaaaaaaa-1111-2222-3333-444444444444}\";
    private const string IdB = @"\\?\Volume{bbbbbbbb-1111-2222-3333-444444444444}\";

    private static VolumeInfo Info(string id) => new(id, "ETICHETTA");

    [Fact]
    public void NoExpectedId_NoCheck()
        => Assert.Equal(VolumeCheck.NoExpectation, VolumeGuard.Check(null, Info(IdA)));

    [Fact]
    public void EmptyExpectedId_NoCheck()
        => Assert.Equal(VolumeCheck.NoExpectation, VolumeGuard.Check("   ", Info(IdA)));

    [Fact]
    public void CurrentNotDeterminable_NoCheck()
        => Assert.Equal(VolumeCheck.NoExpectation, VolumeGuard.Check(IdA, null));

    [Fact]
    public void SameId_Ok()
        => Assert.Equal(VolumeCheck.Ok, VolumeGuard.Check(IdA, Info(IdA)));

    [Fact]
    public void SameId_DifferentCase_Ok()
        => Assert.Equal(VolumeCheck.Ok, VolumeGuard.Check(IdA.ToUpperInvariant(), Info(IdA.ToLowerInvariant())));

    [Fact]
    public void DifferentId_WrongDisk()
        => Assert.Equal(VolumeCheck.WrongDisk, VolumeGuard.Check(IdA, Info(IdB)));

    [Fact]
    public void SameLabelDifferentId_WrongDisk()
    {
        // Due dischi possono avere la stessa etichetta: decide solo l'identificativo.
        var current = new VolumeInfo(IdB, "ETICHETTA");
        Assert.Equal(VolumeCheck.WrongDisk, VolumeGuard.Check(IdA, current));
    }
}
```

- [ ] **Step 2: verifica che falliscano**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter VolumeGuard`
Expected: FAIL di compilazione.

- [ ] **Step 3: implementa** `src/RoboKeep.Core/Services/VolumeGuard.cs`

```csharp
namespace RoboKeep.Core.Services;

/// <summary>Esito del controllo del disco di destinazione.</summary>
public enum VolumeCheck
{
    /// <summary>Il disco collegato è quello atteso: si può eseguire.</summary>
    Ok,
    /// <summary>Nessun controllo possibile o richiesto: si esegue come sempre.</summary>
    NoExpectation,
    /// <summary>Il disco collegato NON è quello per cui il job è stato configurato.</summary>
    WrongDisk,
}

/// <summary>
/// Decide se un job può girare sul disco attualmente collegato (funzione pura).
/// La protezione della rotazione dei dischi vive qui: è la decisione che impedisce a un
/// mirror di cancellare gli snapshot del disco sbagliato, quindi è testata al 100%.
/// </summary>
public static class VolumeGuard
{
    /// <summary>Confronta il volume atteso dal job con quello effettivamente presente.</summary>
    public static VolumeCheck Check(string? expectedId, VolumeInfo? current)
    {
        // Nessuna aspettativa registrata (job creato prima della v1.5, o destinazione di rete).
        if (string.IsNullOrWhiteSpace(expectedId)) return VolumeCheck.NoExpectation;
        // Volume non determinabile (rete, disco assente): la raggiungibilita' e' gia' compito
        // dei controlli pre-avvio, qui non si blocca nulla.
        if (current is null) return VolumeCheck.NoExpectation;

        // Solo l'identificativo decide: le etichette sono modificabili e duplicabili.
        return string.Equals(expectedId, current.VolumeId, StringComparison.OrdinalIgnoreCase)
            ? VolumeCheck.Ok
            : VolumeCheck.WrongDisk;
    }
}
```

- [ ] **Step 4: verifica che passino**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter VolumeGuard`
Expected: PASS (7 test). Suite completa → 257.

- [ ] **Step 5: commit**

```powershell
git add src/RoboKeep.Core/Services/VolumeGuard.cs src/RoboKeep.Tests/VolumeGuardTests.cs
git commit -m @'
feat(v1.5): VolumeGuard - decisione pura sul disco atteso

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 3: modello — campi del job, esito saltato, voce di cronologia

**Files:**
- Modify: `src/RoboKeep.Core/Models/BackupJob.cs` (in fondo alla classe, dopo `InterPacketGapMs`)
- Modify: `src/RoboKeep.Core/Models/JobResult.cs`
- Modify: `src/RoboKeep.Core/Models/RunHistory.cs` (nuova factory accanto a `ForVerify`)
- Test: `src/RoboKeep.Tests/BackupJobTests.cs` (nuova classe in fondo al file)

- [ ] **Step 1: scrivi i test che falliscono**

Append in `src/RoboKeep.Tests/BackupJobTests.cs`:

```csharp
public class VolumeFieldsTests
{
    [Fact]
    public void NewJob_HasNoVolumeExpectation()
    {
        var j = new BackupJob();
        Assert.Null(j.DestinationVolumeId);
        Assert.Null(j.DestinationVolumeLabel);
    }

    [Fact]
    public void VolumeFields_RoundTripThroughJson()
    {
        var j = new BackupJob
        {
            Name = "x",
            DestinationVolumeId = @"\\?\Volume{aaaaaaaa-1111-2222-3333-444444444444}\",
            DestinationVolumeLabel = "BACKUP1",
        };
        var back = JsonSerializer.Deserialize<BackupJob>(JsonSerializer.Serialize(j))!;
        Assert.Equal(j.DestinationVolumeId, back.DestinationVolumeId);
        Assert.Equal("BACKUP1", back.DestinationVolumeLabel);
    }

    [Fact]
    public void JobResult_SkippedDefaultsToFalse()
        => Assert.False(new JobResult().Skipped);
}
```

Append in `src/RoboKeep.Tests/RunHistoryStoreTests.cs` (accanto a `RunHistoryForVerifyTests`):

```csharp
public class RunHistoryForSkippedTests
{
    [Fact]
    public void ForSkipped_IsNeitherSuccessNorFailure()
    {
        var when = new DateTime(2026, 7, 9, 10, 0, 0);
        var e = RunHistoryEntry.ForSkipped("J", when);
        Assert.Equal("skipped", e.Kind);
        Assert.Equal("J", e.JobName);
        Assert.True(e.Success);      // saltare non e' fallire: nessuna icona rossa
        Assert.Equal(when, e.StartedAt);
        Assert.Equal(when, e.FinishedAt);
        Assert.Null(e.LogPath);      // nessun log: non e' stato eseguito nulla
    }
}
```

- [ ] **Step 2: verifica che falliscano**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter "VolumeFields|ForSkipped"`
Expected: FAIL di compilazione.

- [ ] **Step 3: aggiungi i campi a `BackupJob.cs`** (in fondo alla classe, dopo `InterPacketGapMs`, PRIMA dei metodi `Clone`/`CopyInto`)

```csharp
    /// <summary>Identificativo univoco del volume di destinazione (forma <c>\\?\Volume{GUID}\</c>),
    /// memorizzato alla creazione del job o al cambio destinazione. null = nessun controllo.
    /// Serve alla rotazione dei dischi: due dischi alternati hanno spesso la stessa lettera,
    /// ma identificativi diversi. Confrontare le lettere non protegge da nulla.</summary>
    public string? DestinationVolumeId { get; set; }

    /// <summary>Etichetta leggibile del volume di destinazione (es. BACKUP1), usata SOLO nei
    /// messaggi: le etichette sono modificabili e duplicabili, non decidono mai nulla.</summary>
    public string? DestinationVolumeLabel { get; set; }
```

- [ ] **Step 4: aggiungi `Skipped` a `JobResult.cs`** (dopo `DryRun`)

```csharp
    /// <summary>true se il job non è stato eseguito perché il disco di destinazione non è
    /// quello atteso. Non è né un successo né un fallimento: è un terzo esito.</summary>
    public bool Skipped { get; set; }
```

- [ ] **Step 5: aggiungi la factory a `RunHistory.cs`** (dentro il record, accanto a `ForVerify`)

```csharp
    /// <summary>Voce di cronologia per un job saltato perché il disco atteso non era collegato.
    /// Success = true: saltare non è fallire, e la cronologia non deve mostrare un errore.
    /// Nessun log associato: non è stato eseguito nulla.</summary>
    public static RunHistoryEntry ForSkipped(string jobName, DateTime when) =>
        new(jobName, "skipped", when, when, true, 0, 0, 0, 0, 0, 0, null);
```

Aggiorna anche il commento del record: `string Kind, // "backup" | "verify" | "skipped"`.

- [ ] **Step 6: verifica che passino**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj`
Expected: PASS, 261. NB: `BackupJobCloneTests` (test reflection della v1.4.1) copre automaticamente i due campi nuovi — se fallisce, i campi non sono gestiti da `Clone`/`CopyInto`: è la rete di sicurezza che funziona.

- [ ] **Step 7: commit**

```powershell
git add src/RoboKeep.Core/Models/BackupJob.cs src/RoboKeep.Core/Models/JobResult.cs src/RoboKeep.Core/Models/RunHistory.cs src/RoboKeep.Tests/BackupJobTests.cs src/RoboKeep.Tests/RunHistoryStoreTests.cs
git commit -m @'
feat(v1.5): modello - volume atteso sul job, esito saltato, voce di cronologia

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 4: BackupRunner — il controllo che protegge i dati

**Files:**
- Modify: `src/RoboKeep.Core/CoreLoc.cs` (nel dizionario `Map`, dopo il gruppo `Verify_*`)
- Modify: `src/RoboKeep.Core/Services/BackupRunner.cs` (inizio di `RunJobAsync`, riga ~55)
- Test: `src/RoboKeep.Tests/BackupRunnerVolumeTests.cs`

- [ ] **Step 1: scrivi il test che falliscono** — è **il test che conta**: dimostra che robocopy non parte affatto.

```csharp
using RoboKeep.Core.Models;
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public class BackupRunnerVolumeTests : IDisposable
{
    private readonly string _root = Path.Combine(Path.GetTempPath(), Guid.NewGuid().ToString());
    private string Src => Path.Combine(_root, "src");
    private string Dst => Path.Combine(_root, "dst");

    public BackupRunnerVolumeTests()
    {
        Directory.CreateDirectory(Src);
        Directory.CreateDirectory(Dst);
        File.WriteAllText(Path.Combine(Src, "a.txt"), "contenuto");
    }

    public void Dispose() { if (Directory.Exists(_root)) Directory.Delete(_root, true); }

    private BackupRunner NewRunner(AppConfig config)
    {
        config.Settings.LogRoot = Path.Combine(_root, "logs");
        config.Settings.TempRoot = Path.Combine(_root, "temp");
        var creds = new CredentialService(config.Settings.CredentialScope);
        return new BackupRunner(config, new RobocopyRunner(), new LogService(config.Settings),
            new EmailService(creds), creds);
    }

    [Fact]
    public async Task WrongDisk_SkipsWithoutRunningRobocopy()
    {
        var config = new AppConfig();
        var job = new BackupJob
        {
            Name = "T", Source = Src, Destination = Dst,
            // Identificativo palesemente diverso da quello del volume vero: disco sbagliato.
            DestinationVolumeId = @"\\?\Volume{deadbeef-0000-0000-0000-000000000000}\",
            DestinationVolumeLabel = "ALTRO-DISCO",
        };
        config.Jobs.Add(job);

        var result = await NewRunner(config).RunJobAsync(job);

        Assert.True(result.Skipped);
        Assert.False(result.Success);
        // La prova che robocopy non e' mai partito: la destinazione e' rimasta intatta.
        Assert.Empty(Directory.GetFiles(Dst));
    }

    [Fact]
    public async Task RightDisk_RunsNormally()
    {
        var config = new AppConfig();
        var expected = VolumeIdentity.ForPath(Dst);
        Assert.NotNull(expected); // la temp e' su un volume locale identificabile
        var job = new BackupJob
        {
            Name = "T", Source = Src, Destination = Dst,
            DestinationVolumeId = expected!.VolumeId,
            DestinationVolumeLabel = expected.Label,
        };
        config.Jobs.Add(job);

        var result = await NewRunner(config).RunJobAsync(job);

        Assert.False(result.Skipped);
        Assert.True(result.Success);
        Assert.True(File.Exists(Path.Combine(Dst, "a.txt")));
    }

    [Fact]
    public async Task NoVolumeExpectation_RunsNormally()
    {
        var config = new AppConfig();
        var job = new BackupJob { Name = "T", Source = Src, Destination = Dst }; // nessun volume atteso
        config.Jobs.Add(job);

        var result = await NewRunner(config).RunJobAsync(job);

        Assert.False(result.Skipped);
        Assert.True(File.Exists(Path.Combine(Dst, "a.txt")));
    }
}
```

- [ ] **Step 2: verifica che fallisca**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter BackupRunnerVolume`
Expected: FAIL — `WrongDisk_SkipsWithoutRunningRobocopy` fallisce perché oggi robocopy copia comunque (`result.Skipped` è false e `a.txt` è finito in destinazione). **È esattamente il difetto che la tappa risolve.**

- [ ] **Step 3: aggiungi le chiavi in `CoreLoc.cs`** (nel `Map`, dopo il gruppo `Verify_*`)

```csharp
        ["Volume_Skipped"] = L(
            "[disco] job saltato: scrive sul disco {0}, ma il disco collegato è {1}.",
            "[disk] job skipped: it writes to disk {0}, but the connected disk is {1}.",
            "[disco] trabajo omitido: escribe en el disco {0}, pero el disco conectado es {1}.",
            "[disque] tâche ignorée : elle écrit sur le disque {0}, mais le disque connecté est {1}.",
            "[Datenträger] Job übersprungen: schreibt auf {0}, angeschlossen ist aber {1}."),
        ["Volume_SkippedStatus"] = L(
            "Saltato: il disco atteso non è collegato.",
            "Skipped: the expected disk is not connected.",
            "Omitido: el disco esperado no está conectado.",
            "Ignoré : le disque attendu n'est pas connecté.",
            "Übersprungen: der erwartete Datenträger ist nicht angeschlossen."),
        ["Volume_Unknown"] = L(
            "sconosciuto", "unknown", "desconocido", "inconnu", "unbekannt"),
```

- [ ] **Step 4: implementa il controllo in `BackupRunner.RunJobAsync`** — PRIMA della riga del lock file (`using var lockHandle = ...`), come primissima cosa del metodo:

```csharp
        // Rotazione dei dischi: due dischi alternati hanno spesso la stessa lettera. Se il
        // volume collegato non e' quello per cui il job e' stato configurato, si salta senza
        // toccare NULLA (niente lock, niente UAC per VSS, niente robocopy): un mirror sul
        // disco sbagliato cancellerebbe i dati che ci trova.
        var currentVolume = VolumeIdentity.ForPath(job.Destination);
        if (VolumeGuard.Check(job.DestinationVolumeId, currentVolume) == VolumeCheck.WrongDisk)
        {
            var unknown = CoreLoc.S("Volume_Unknown");
            progress?.Report(string.Format(CoreLoc.S("Volume_Skipped"),
                string.IsNullOrEmpty(job.DestinationVolumeLabel) ? unknown : job.DestinationVolumeLabel,
                string.IsNullOrEmpty(currentVolume?.Label) ? unknown : currentVolume!.Label));

            var now = DateTime.Now;
            if (!dryRun)
                _history?.Append(RunHistoryEntry.ForSkipped(job.Name, now));
            // L'ultimo esito NON viene aggiornato: un salto non cancella la memoria di un
            // successo precedente.
            return new JobResult
            {
                JobName = job.Name,
                Skipped = true,
                Success = false,
                Status = CoreLoc.S("Volume_SkippedStatus"),
                StartedAt = now,
                DryRun = dryRun,
            };
        }

```

- [ ] **Step 5: verifica che passi**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter BackupRunnerVolume`
Expected: PASS (3 test). Suite completa → 264.

Se `RightDisk_RunsNormally` o `NoVolumeExpectation_RunsNormally` falliscono, robocopy non è disponibile o la cartella temp non è su un volume locale: **non indebolire l'assert**, segnalarlo.

- [ ] **Step 6: commit**

```powershell
git add src/RoboKeep.Core/CoreLoc.cs src/RoboKeep.Core/Services/BackupRunner.cs src/RoboKeep.Tests/BackupRunnerVolumeTests.cs
git commit -m @'
feat(v1.5): BackupRunner salta i job sul disco sbagliato senza eseguire nulla

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 5: "saltato" in tutta la catena — CLI, lista, cronologia

**Files:**
- Modify: `src/RoboKeep/AppHost.cs` (`RunHeadlessAsync`, righe ~83-86)
- Modify: `src/RoboKeep/ViewModels/MainViewModel.cs` (`RunJobsAsync`, righe ~397-413)
- Modify: `src/RoboKeep/HistoryWindow.xaml.cs` (`ToRow`, righe ~43-52)
- Modify: `src/RoboKeep/Localization/Loc.cs` (5 blocchi)

- [ ] **Step 1: exit code della CLI** — in `AppHost.RunHeadlessAsync` sostituisci le ultime righe:

```csharp
        foreach (var r in results)
        {
            var esito = r.Skipped ? "SALTATO" : r.Success ? "OK" : "ERRORE";
            Console.WriteLine($"{r.JobName}: {esito} - {r.Status}");
        }

        // Saltare non e' fallire: un job il cui disco non e' collegato non deve far risultare
        // fallita l'attivita' pianificata (era la causa degli allarmi rossi ogni notte).
        return results.All(r => r.Success || r.Skipped) ? 0 : 1;
```

- [ ] **Step 2: esito nella lista** — in `MainViewModel.RunJobsAsync`, sostituisci il blocco che imposta `LastStatus` dopo `RunJobAsync` (righe ~397-413) con:

```csharp
                    var result = await runner.RunJobAsync(jvm.Model, dryRun, progress, _cts.Token);
                    if (result.Skipped)
                        jvm.LastStatus = Loc.Instance["Run_SkippedDisk"];
                    else if (dryRun)
                        jvm.LastStatus = $"{Loc.Instance["Run_OK"]} · {Loc.Instance["Run_Preview"]}";
                    else
                        jvm.LastStatus = RunStatus.Format(result.Success, result.FilesCopied, result.FilesSkipped,
                            result.FilesExtra, result.FilesFailed + result.DirsFailed, DateTime.Now);
                    Enqueue($"=> {jvm.Name}: {result.Status}");

                    // Nessun toast per un job saltato: non e' successo niente da notificare.
                    if (!dryRun
                        && !result.Skipped
                        && _host.Config.Settings.NotificationsEnabled
                        && System.Windows.Application.Current?.MainWindow is MainWindow mw)
                    {
                        var msg = result.Success
                            ? Loc.Instance["Toast_Ok"]
                            : Loc.Instance["Toast_Error"];
                        mw.ShowJobToast(jvm.Name, msg);
                    }
```

**Comportamento voluto, da non "correggere"**: il `finally` subito sotto chiama `ReloadLastResults()`, che ricarica `LastStatus` da `lastresults.json`. Per un job saltato l'ultimo esito su disco è quello *precedente* (non lo aggiorniamo apposta), quindi nei run reali l'etichetta "Saltato" resta visibile solo durante l'esecuzione e poi la riga torna a mostrare **l'ultimo backup vero** — che è l'informazione corretta per una colonna chiamata "Ultimo esito". Il salto è raccontato dal log (riga `[disco] job saltato: ...`, sempre sotto gli occhi) e dalla cronologia. In anteprima non c'è reload, quindi l'etichetta "Saltato" resta: coerente, perché un'anteprima non ha un "ultimo esito" da ripristinare. **Non modificare il `finally`.**

- [ ] **Step 3: cronologia** — in `HistoryWindow.ToRow` sostituisci il metodo con:

```csharp
    private static HistoryRow ToRow(RunHistoryEntry e)
    {
        var kind = e.Kind switch
        {
            "verify" => Loc.Instance["Hist_KindVerify"],
            "skipped" => Loc.Instance["Hist_KindSkipped"],
            _ => Loc.Instance["Hist_KindBackup"],
        };
        var outcome = e.Kind == "skipped"
            ? Loc.Instance["Hist_OutcomeSkipped"]
            : e.Success ? "OK" : Loc.Instance["Run_Error"];
        var counts = e.Kind switch
        {
            "skipped" => Loc.Instance["Hist_CountsSkipped"],
            "verify" => string.Format(Loc.Instance["Hist_CountsVerify"], e.FilesCopied, e.FilesFailed, e.FilesSkipped),
            _ => string.Format(Loc.Instance["Hist_CountsBackup"], e.FilesCopied, e.FilesSkipped, e.FilesFailed + e.DirsFailed),
        };
        var duration = (e.FinishedAt - e.StartedAt).ToString(@"hh\:mm\:ss");
        return new HistoryRow($"{e.StartedAt:dd/MM/yyyy HH:mm}", e.JobName, kind, outcome, counts, duration, e.LogPath);
    }
```

- [ ] **Step 4: stringhe in `Loc.cs`** — in TUTTI e 5 i blocchi (vicino alle `Hist_*`):

IT:
```csharp
        ["Run_SkippedDisk"] = "Saltato · disco non collegato",
        ["Hist_KindSkipped"] = "Saltato",
        ["Hist_OutcomeSkipped"] = "—",
        ["Hist_CountsSkipped"] = "disco atteso non collegato",
```
EN:
```csharp
        ["Run_SkippedDisk"] = "Skipped · disk not connected",
        ["Hist_KindSkipped"] = "Skipped",
        ["Hist_OutcomeSkipped"] = "—",
        ["Hist_CountsSkipped"] = "expected disk not connected",
```
ES:
```csharp
        ["Run_SkippedDisk"] = "Omitido · disco no conectado",
        ["Hist_KindSkipped"] = "Omitido",
        ["Hist_OutcomeSkipped"] = "—",
        ["Hist_CountsSkipped"] = "disco esperado no conectado",
```
FR:
```csharp
        ["Run_SkippedDisk"] = "Ignoré · disque non connecté",
        ["Hist_KindSkipped"] = "Ignoré",
        ["Hist_OutcomeSkipped"] = "—",
        ["Hist_CountsSkipped"] = "disque attendu non connecté",
```
DE:
```csharp
        ["Run_SkippedDisk"] = "Übersprungen · Datenträger nicht angeschlossen",
        ["Hist_KindSkipped"] = "Übersprungen",
        ["Hist_OutcomeSkipped"] = "—",
        ["Hist_CountsSkipped"] = "erwarteter Datenträger nicht angeschlossen",
```

- [ ] **Step 5: verifica**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj` → 264 (il test di parità delle 5 lingue copre le chiavi nuove).
Run: `dotnet build src/RoboKeep/RoboKeep.csproj -c Debug -v quiet` → 0 errori.

- [ ] **Step 6: commit**

```powershell
git add src/RoboKeep/AppHost.cs src/RoboKeep/ViewModels/MainViewModel.cs src/RoboKeep/HistoryWindow.xaml.cs src/RoboKeep/Localization/Loc.cs
git commit -m @'
feat(v1.5): saltato come terzo esito in CLI, lista e cronologia

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 6: editor — riga "Disco", riassociazione, regola di associazione

**Files:**
- Modify: `src/RoboKeep/ViewModels/JobEditorViewModel.cs`
- Modify: `src/RoboKeep/JobEditorWindow.xaml` (dopo la Grid della destinazione, riga ~65)
- Modify: `src/RoboKeep/JobEditorWindow.xaml.cs`
- Modify: `src/RoboKeep/Localization/Loc.cs` (5 blocchi)
- Test: `src/RoboKeep.Tests/JobEditorViewModelTests.cs` (append)

- [ ] **Step 1: scrivi i test che falliscono** — inchiodano **la regola di associazione**, cioè la falla trovata in revisione:

```csharp
    private const string FakeVolumeId = @"\\?\Volume{aaaaaaaa-1111-2222-3333-444444444444}\";

    [Fact]
    public void SavingWithoutTouchingDestination_NeverReassociatesVolume()
    {
        // La falla che questo test blocca: aprire l'editor col disco sbagliato inserito e
        // salvare una modifica qualsiasi non deve riassociare il job al disco sbagliato.
        var job = new BackupJob
        {
            Name = "j", Source = @"C:\s", Destination = @"C:\d",
            DestinationVolumeId = FakeVolumeId, DestinationVolumeLabel = "ALTRO",
        };
        var vm = Vm(job);

        vm.Name = "nome nuovo";
        vm.Mirror = !vm.Mirror;

        Assert.Equal(FakeVolumeId, job.DestinationVolumeId);
        Assert.Equal("ALTRO", job.DestinationVolumeLabel);
    }

    [Fact]
    public void ChangingDestination_ReassociatesToNewVolume()
    {
        var job = new BackupJob
        {
            Name = "j", Source = @"C:\s", Destination = @"C:\d",
            DestinationVolumeId = FakeVolumeId, DestinationVolumeLabel = "ALTRO",
        };
        var vm = Vm(job);

        vm.Destination = @"C:\Windows";

        Assert.NotEqual(FakeVolumeId, job.DestinationVolumeId);
        Assert.StartsWith(@"\\?\Volume{", job.DestinationVolumeId!);
    }

    [Fact]
    public void UseCurrentVolume_AssociatesToTheConnectedDisk()
    {
        var job = new BackupJob
        {
            Name = "j", Source = @"C:\s", Destination = @"C:\",
            DestinationVolumeId = FakeVolumeId, DestinationVolumeLabel = "ALTRO",
        };
        var vm = Vm(job);
        Assert.True(vm.VolumeMismatch); // il disco memorizzato non e' quello presente

        vm.UseCurrentVolume();

        Assert.False(vm.VolumeMismatch);
        Assert.StartsWith(@"\\?\Volume{", job.DestinationVolumeId!);
    }
```

- [ ] **Step 2: verifica che falliscano**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter JobEditorViewModel`
Expected: FAIL di compilazione (`VolumeMismatch`/`UseCurrentVolume` non esistono).

- [ ] **Step 3: ViewModel** — sostituisci il setter `Destination` (righe ~40-44) con:

```csharp
    public string Destination
    {
        get => _job.Destination;
        set
        {
            if (_job.Destination == value) return;
            _job.Destination = value;
            // Destinazione cambiata → il disco memorizzato non c'entra piu' nulla: riassocia.
            // NB: il salvataggio a destinazione INVARIATA non riassocia mai (vedi UseCurrentVolume):
            // altrimenti basterebbe aprire l'editor col disco sbagliato inserito e salvare una
            // modifica qualsiasi per annullare in silenzio la protezione.
            UseCurrentVolume();
            OnPropertyChanged();
            RaisePreview();
        }
    }
```

E aggiungi in fondo alla classe (prima di `Validate`):

```csharp
    private bool _volumeMismatch;
    private string _currentVolumeLabel = "";

    /// <summary>Etichetta del disco a cui il job è associato (vuota = nessun controllo attivo).</summary>
    public string VolumeLabel => _job.DestinationVolumeLabel ?? "";

    /// <summary>true se il job ha un disco associato: la riga "Disco" è visibile solo allora.</summary>
    public bool HasVolume => !string.IsNullOrEmpty(_job.DestinationVolumeId);

    /// <summary>true se il disco attualmente collegato NON è quello associato al job.</summary>
    public bool VolumeMismatch => _volumeMismatch;

    /// <summary>Avviso da mostrare quando il disco collegato è un altro.</summary>
    public string VolumeMismatchText =>
        string.Format(Loc.Instance["Editor_VolumeMismatch"],
            string.IsNullOrEmpty(_currentVolumeLabel) ? Loc.Instance["Editor_VolumeUnknown"] : _currentVolumeLabel);

    /// <summary>Ricalcola lo stato del disco (chiamato all'apertura e a ogni riassociazione).</summary>
    public void RefreshVolumeState()
    {
        var current = VolumeIdentity.ForPath(_job.Destination);
        _currentVolumeLabel = current?.Label ?? "";
        _volumeMismatch = VolumeGuard.Check(_job.DestinationVolumeId, current) == VolumeCheck.WrongDisk;
        OnPropertyChanged(nameof(VolumeLabel));
        OnPropertyChanged(nameof(HasVolume));
        OnPropertyChanged(nameof(VolumeMismatch));
        OnPropertyChanged(nameof(VolumeMismatchText));
    }

    /// <summary>Associa il job al disco attualmente collegato: usato dal pulsante
    /// "Usa questo disco" e automaticamente quando cambia la destinazione. È l'UNICO modo
    /// di riassociare un job a destinazione invariata, e deve restare esplicito.</summary>
    public void UseCurrentVolume()
    {
        var current = VolumeIdentity.ForPath(_job.Destination);
        _job.DestinationVolumeId = current?.VolumeId;
        _job.DestinationVolumeLabel = current?.Label;
        RefreshVolumeState();
    }
```

E nel **costruttore**, come ultima riga: `RefreshVolumeState();`

- [ ] **Step 4: XAML** — in `JobEditorWindow.xaml`, subito DOPO la `</Grid>` della destinazione (riga ~65) e PRIMA della `<ui:Card>` delle opzioni:

```xml
                <!-- Disco di destinazione: protegge dalla rotazione dei dischi (due dischi
                     alternati prendono spesso la stessa lettera). -->
                <StackPanel Margin="0,6,0,0" Visibility="{Binding HasVolume, Converter={StaticResource BoolToVis}}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{l:Tr Editor_Volume}" Style="{StaticResource Hint}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding VolumeLabel}" FontWeight="SemiBold" Margin="6,0,0,0" VerticalAlignment="Center"/>
                        <ctl:InfoHint Margin="6,0,0,0" Text="{l:Tr Editor_VolumeTip}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0"
                                Visibility="{Binding VolumeMismatch, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="{Binding VolumeMismatchText}" Foreground="#C8860D"
                                   TextWrapping="Wrap" VerticalAlignment="Center"/>
                        <ui:Button Content="{l:Tr Editor_UseThisDisk}" Margin="8,0,0,0" Click="OnUseThisDisk"/>
                    </StackPanel>
                </StackPanel>
```

Verifica che `BoolToVis` sia nelle risorse della finestra (aggiunto nella v1.4): se manca, aggiungi `<BooleanToVisibilityConverter x:Key="BoolToVis"/>` alle `<ui:FluentWindow.Resources>` esistenti.

- [ ] **Step 5: code-behind** — in `JobEditorWindow.xaml.cs`, dopo `OnBrowseDest`:

```csharp
    private void OnUseThisDisk(object sender, RoutedEventArgs e) => _vm.UseCurrentVolume();
```

- [ ] **Step 6: stringhe in `Loc.cs`** — TUTTI e 5 i blocchi (vicino a `Editor_Dest`):

IT:
```csharp
        ["Editor_Volume"] = "Disco:",
        ["Editor_VolumeTip"] = "Il job gira solo quando è collegato questo disco. Serve se usi più dischi a rotazione: Windows spesso assegna loro la stessa lettera, e senza questo controllo un backup potrebbe scrivere sul disco sbagliato.",
        ["Editor_VolumeMismatch"] = "Ora è collegato il disco {0}: il job verrà saltato.",
        ["Editor_VolumeUnknown"] = "(sconosciuto)",
        ["Editor_UseThisDisk"] = "Usa questo disco",
```
EN:
```csharp
        ["Editor_Volume"] = "Disk:",
        ["Editor_VolumeTip"] = "The job only runs when this disk is connected. Useful if you rotate several disks: Windows often gives them the same letter, and without this check a backup could write to the wrong disk.",
        ["Editor_VolumeMismatch"] = "Disk {0} is connected now: the job will be skipped.",
        ["Editor_VolumeUnknown"] = "(unknown)",
        ["Editor_UseThisDisk"] = "Use this disk",
```
ES:
```csharp
        ["Editor_Volume"] = "Disco:",
        ["Editor_VolumeTip"] = "El trabajo solo se ejecuta cuando este disco está conectado. Útil si rotas varios discos: Windows suele darles la misma letra y, sin esta comprobación, una copia podría escribir en el disco equivocado.",
        ["Editor_VolumeMismatch"] = "Ahora está conectado el disco {0}: el trabajo se omitirá.",
        ["Editor_VolumeUnknown"] = "(desconocido)",
        ["Editor_UseThisDisk"] = "Usar este disco",
```
FR:
```csharp
        ["Editor_Volume"] = "Disque :",
        ["Editor_VolumeTip"] = "La tâche ne s'exécute que si ce disque est connecté. Utile si vous alternez plusieurs disques : Windows leur attribue souvent la même lettre et, sans ce contrôle, une sauvegarde pourrait écrire sur le mauvais disque.",
        ["Editor_VolumeMismatch"] = "Le disque {0} est connecté : la tâche sera ignorée.",
        ["Editor_VolumeUnknown"] = "(inconnu)",
        ["Editor_UseThisDisk"] = "Utiliser ce disque",
```
DE:
```csharp
        ["Editor_Volume"] = "Datenträger:",
        ["Editor_VolumeTip"] = "Der Job läuft nur, wenn dieser Datenträger angeschlossen ist. Nützlich bei mehreren wechselnden Datenträgern: Windows vergibt oft denselben Buchstaben, und ohne diese Prüfung könnte ein Backup auf den falschen Datenträger schreiben.",
        ["Editor_VolumeMismatch"] = "Aktuell ist Datenträger {0} angeschlossen: der Job wird übersprungen.",
        ["Editor_VolumeUnknown"] = "(unbekannt)",
        ["Editor_UseThisDisk"] = "Diesen Datenträger verwenden",
```

- [ ] **Step 7: verifica**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter JobEditorViewModel` → PASS.
Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj` → 267.
Run (app chiusa): `dotnet build src/RoboKeep/RoboKeep.csproj -c Debug -v quiet` → 0 errori.

- [ ] **Step 8: commit**

```powershell
git add src/RoboKeep/ViewModels/JobEditorViewModel.cs src/RoboKeep/JobEditorWindow.xaml src/RoboKeep/JobEditorWindow.xaml.cs src/RoboKeep/Localization/Loc.cs src/RoboKeep.Tests/JobEditorViewModelTests.cs
git commit -m @'
feat(v1.5): editor - riga Disco, riassociazione in un clic, regola di associazione protetta

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 7: verifica finale + prova manuale (con l'utente)

- [ ] **Step 1: suite completa**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj`
Expected: PASS, ~267 (243 baseline + 24 nuovi).

- [ ] **Step 2: build Debug** (chiedere all'utente di chiudere RoboKeep)

Run: `dotnet build src/RoboKeep/RoboKeep.csproj -c Debug -v quiet`
Expected: 0 errori, 0 avvisi.

- [ ] **Step 3: prova manuale** — guidare l'utente, non eseguire:

1. **Nessuna regressione**: aprire un job esistente (creato prima della v1.5, quindi senza disco associato) e avviarlo → gira normalmente, nessuna riga "Disco" (non ha associazione).
2. **Associazione**: aprire l'editor, ri-selezionare la stessa destinazione con Sfoglia (o riscriverla) → compare `Disco: <etichetta>`. Salvare.
3. **Il caso vero** (serve il secondo disco): con l'altro disco collegato, aprire l'editor di quel job → avviso arancione "Ora è collegato il disco X: il job verrà saltato".
4. **Il test che conta**: con il disco sbagliato collegato, premere **Avvia tutti** → i job dell'altro disco vengono **saltati** con la riga `[disco] job saltato: ...` nel log; **nessun file toccato sul disco sbagliato**; nessuna icona rossa; la riga in lista torna a mostrare l'ultimo backup reale (il salto non cancella la memoria di un successo).
5. **Riassociazione**: nell'editor, "Usa questo disco" → l'avviso sparisce; salvando, il job gira su quel disco.
6. **Cronologia**: le esecuzioni saltate compaiono con tipo "Saltato".
7. **Destinazione di rete o disco interno**: nessun controllo, tutto come prima.

- [ ] **Step 4** (SOLO dopo conferma esplicita dell'utente): CHANGELOG `[1.5.0]`, versione `1.5.0` in `src/Directory.Build.props`, commit, push e tag `v1.5.0`. **Il workflow GitHub Actions builda, scansiona con VirusTotal e pubblica la Release da solo**: non serve fare publish/zip a mano (v1.4.1 in poi).
