<Window x:Class="RobocopySW.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RobocopySW"
        mc:Ignorable="d"
        Title="RobocopySW — Backup configurabile" Height="640" Width="1000"
        WindowStartupLocation="CenterScreen">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <Button Style="{StaticResource ToolButton}" Click="OnNewJob" Content="➕ Nuovo"/>
            <Button Style="{StaticResource ToolButton}" Click="OnEditJob" Content="✏ Modifica"/>
            <Button Style="{StaticResource ToolButton}" Click="OnDeleteJob" Content="🗑 Elimina"/>
            <Separator/>
            <Button Style="{StaticResource ToolButton}" Command="{Binding PreviewSelectedCommand}" Content="👁 Anteprima"/>
            <Button Style="{StaticResource ToolButton}" Command="{Binding RunSelectedCommand}" Content="▶ Avvia selezionato"/>
            <Separator/>
            <Button Style="{StaticResource ToolButton}" Command="{Binding PreviewAllCommand}" Content="👁 Anteprima tutti"/>
            <Button Style="{StaticResource ToolButton}" Command="{Binding RunAllCommand}" Content="⏩ Avvia tutti"/>
            <Separator/>
            <Button Style="{StaticResource ToolButton}" Click="OnSettings" Content="⚙ Impostazioni"/>
        </ToolBar>

        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem><TextBlock Text="{Binding StatusText}"/></StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <ProgressBar Width="140" Height="14" IsIndeterminate="{Binding IsBusy}"/>
            </StatusBarItem>
        </StatusBar>

        <Grid Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="1.2*"/>
            </Grid.RowDefinitions>

            <DataGrid Grid.Row="0" ItemsSource="{Binding Jobs}" SelectedItem="{Binding SelectedJob}"
                      AutoGenerateColumns="False" IsReadOnly="False" CanUserAddRows="False"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      MouseDoubleClick="OnEditJob">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="Attivo" Binding="{Binding Enabled, UpdateSourceTrigger=PropertyChanged}" Width="56"/>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="160" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Modalità" Binding="{Binding ModeLabel}" Width="90" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Sorgente" Binding="{Binding Source}" Width="*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Destinazione" Binding="{Binding Destination}" Width="*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Ultimo esito" Binding="{Binding LastStatus}" Width="220" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>

            <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch" Background="#DDD"/>

            <DockPanel Grid.Row="2">
                <DockPanel DockPanel.Dock="Top" LastChildFill="False" Margin="0,4">
                    <TextBlock Text="Log esecuzione" FontWeight="Bold" VerticalAlignment="Center"/>
                    <Button DockPanel.Dock="Right" Content="Pulisci" Command="{Binding ClearLogCommand}" Padding="8,2"/>
                </DockPanel>
                <TextBox x:Name="LogBox" Text="{Binding LogText, Mode=OneWay}" IsReadOnly="True"
                         FontFamily="Consolas" FontSize="12" TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                         Background="#1E1E1E" Foreground="#DDDDDD"/>
            </DockPanel>
        </Grid>
    </DockPanel>
</Window>
