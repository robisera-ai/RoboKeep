# RoboKeep v1.1 — Fondamenta di affidabilità — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Eliminare i fallimenti silenziosi di RoboKeep aggiungendo cartella dati stabile, pre-check pre-avvio, allerta backup fermo/fallito e notifiche toast + tray.

**Architecture:** Logica "pura" e testabile nei servizi Core (`AppDataLocator`, `PreflightChecker`, `StaleBackupEvaluator`), in TDD con xUnit; UI sottile nel progetto WPF (`MainWindow`, `SettingsWindow`, `AppHost`) che consuma i servizi. Nuovi campi in `AppSettings` con default retrocompatibili.

**Tech Stack:** .NET 10 (`net10.0-windows`), WPF, WPF-UI, xUnit. Nuovo pacchetto NuGet `H.NotifyIcon.Wpf` per tray + toast.

**Spec:** [docs/superpowers/specs/2026-06-27-v1.1-affidabilita-design.md](../specs/2026-06-27-v1.1-affidabilita-design.md)

---

## Note operative (specifiche di questo repo)

- **Test:** xUnit. Pattern esistente: classe di test con `IDisposable`, cartella temporanea
  in `Path.GetTempPath()` con `Guid`. Vedi `src/RoboKeep.Tests/LastResultStoreTests.cs`.
- **Build/test da PowerShell** (la shell del progetto è PowerShell):
  - `dotnet build src/RoboKeep.sln -c Debug`
  - `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj`
- **L'app blocca il build se aperta.** Prima di ogni `dotnet build` che compila il progetto WPF,
  verificare: `Get-Process RoboKeep -ErrorAction SilentlyContinue`. Se presente, fermarsi e
  chiedere all'utente di chiuderla (non terminarla d'autorità).
- **Commit:** messaggi **senza virgolette doppie** (rompono PowerShell). Identità git già
  configurata (`robisera-ai@users.noreply.github.com`). Chiudere ogni messaggio con:
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- **Branch:** lavorare sul branch `roadmap` (già attivo).
- I servizi Core puri **non usano `DateTime.Now` internamente**: il tempo è un parametro
  (`now`), così i test sono deterministici.

---

## File Structure

**Core (nuovi):**
- `src/RoboKeep.Core/Services/AppDataLocator.cs` — risoluzione data root + prepare/migrazione
- `src/RoboKeep.Core/Services/StaleBackupEvaluator.cs` — classificazione salute job (puro)
- `src/RoboKeep.Core/Models/JobHealth.cs` — enum `BackupHealth` + record `JobHealth`
- `src/RoboKeep.Core/Services/PreflightChecker.cs` — valutazione avvisi (puro)
- `src/RoboKeep.Core/Services/PreflightCollector.cs` — raccolta dati IO (free space, size, reach)
- `src/RoboKeep.Core/Models/PreflightModels.cs` — `PreflightSeverity`, `PreflightWarning`, `PreflightInputs`

**Core (modificati):**
- `src/RoboKeep.Core/Models/AppSettings.cs` — nuovi campi

**Test (nuovi):**
- `src/RoboKeep.Tests/AppDataLocatorTests.cs`
- `src/RoboKeep.Tests/StaleBackupEvaluatorTests.cs`
- `src/RoboKeep.Tests/PreflightCheckerTests.cs`
- `src/RoboKeep.Tests/AppSettingsRoundTripTests.cs`

**WPF (modificati):**
- `src/RoboKeep/AppHost.cs` — data root via `AppDataLocator`; default log/temp
- `src/RoboKeep/ViewModels/MainViewModel.cs` — salute job + preflight nel run
- `src/RoboKeep/ViewModels/JobViewModel.cs` — proprietà `Health` per evidenziazione riga
- `src/RoboKeep/MainWindow.xaml(.cs)` — InfoBar salute, evidenziazione righe, tray
- `src/RoboKeep/SettingsWindow.xaml(.cs)` — nuovi campi impostazioni
- `src/RoboKeep/Localization/Loc.cs` — nuove chiavi (5 lingue)
- `src/RoboKeep/RoboKeep.csproj` — pacchetto `H.NotifyIcon.Wpf`

**Docs (modificati):**
- `README.md`, `readmeita.md` — sezione portabilità/cartella dati

---

## Task 1: Nuovi campi `AppSettings` (+ round-trip)

**Files:**
- Modify: `src/RoboKeep.Core/Models/AppSettings.cs`
- Test: `src/RoboKeep.Tests/AppSettingsRoundTripTests.cs`

- [ ] **Step 1: Write the failing test**

Create `src/RoboKeep.Tests/AppSettingsRoundTripTests.cs`:

```csharp
using RoboKeep.Core.Models;
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public class AppSettingsRoundTripTests : IDisposable
{
    private readonly string _dir = Path.Combine(Path.GetTempPath(), "RbcSet_" + Guid.NewGuid().ToString("N"));

    public void Dispose()
    {
        if (Directory.Exists(_dir)) Directory.Delete(_dir, recursive: true);
    }

    [Fact]
    public void Defaults_AreReliabilityFriendly()
    {
        var s = new AppSettings();
        Assert.Equal(7, s.StaleAfterDays);
        Assert.True(s.PreflightEnabled);
        Assert.Equal(1024, s.MinFreeSpaceMb);
        Assert.True(s.NotificationsEnabled);
        Assert.False(s.MinimizeToTray);
        Assert.False(s.StartMinimized);
    }

    [Fact]
    public void NewFields_RoundTripThroughConfigStore()
    {
        var path = Path.Combine(_dir, "config.json");
        var store = new ConfigStore(path);
        store.Save(new AppConfig
        {
            Settings = new AppSettings
            {
                StaleAfterDays = 3, PreflightEnabled = false, MinFreeSpaceMb = 2048,
                NotificationsEnabled = false, MinimizeToTray = true, StartMinimized = true,
            },
        });

        var loaded = store.Load().Settings;
        Assert.Equal(3, loaded.StaleAfterDays);
        Assert.False(loaded.PreflightEnabled);
        Assert.Equal(2048, loaded.MinFreeSpaceMb);
        Assert.False(loaded.NotificationsEnabled);
        Assert.True(loaded.MinimizeToTray);
        Assert.True(loaded.StartMinimized);
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter AppSettingsRoundTripTests`
Expected: FAIL — compilazione: `AppSettings` non ha `StaleAfterDays` ecc.

- [ ] **Step 3: Add the fields**

In `src/RoboKeep.Core/Models/AppSettings.cs`, dentro la classe `AppSettings` (dopo `LogRetentionDays`, prima di `Email`):

```csharp
    /// <summary>Giorni dopo i quali un backup riuscito è considerato "fermo". 0 = disattiva l'allerta "fermo".</summary>
    public int StaleAfterDays { get; set; } = 7;

    /// <summary>Se true, prima di avviare un job esegue i controlli pre-avvio (raggiungibilità, spazio).</summary>
    public bool PreflightEnabled { get; set; } = true;

    /// <summary>Soglia minima di spazio libero in destinazione (MB) sotto la quale il pre-check avvisa.</summary>
    public int MinFreeSpaceMb { get; set; } = 1024;

    /// <summary>Se true mostra notifiche toast a fine job.</summary>
    public bool NotificationsEnabled { get; set; } = true;

    /// <summary>Se true la chiusura/riduzione manda l'app nell'area di notifica (tray) invece di chiudere.</summary>
    public bool MinimizeToTray { get; set; } = false;

    /// <summary>Se true l'app si avvia minimizzata nel tray.</summary>
    public bool StartMinimized { get; set; } = false;
```

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter AppSettingsRoundTripTests`
Expected: PASS (2 test)

- [ ] **Step 5: Commit**

```
git add src/RoboKeep.Core/Models/AppSettings.cs src/RoboKeep.Tests/AppSettingsRoundTripTests.cs
git commit -m "feat(v1.1): nuovi campi AppSettings per affidabilita"
```

---

## Task 2: `AppDataLocator.ResolveDataRoot` (puro)

**Files:**
- Create: `src/RoboKeep.Core/Services/AppDataLocator.cs`
- Test: `src/RoboKeep.Tests/AppDataLocatorTests.cs`

- [ ] **Step 1: Write the failing test**

Create `src/RoboKeep.Tests/AppDataLocatorTests.cs`:

```csharp
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public class AppDataLocatorTests
{
    [Fact]
    public void PortableFlagPresent_UsesExeDir()
    {
        var root = AppDataLocator.ResolveDataRoot(
            exeDir: @"C:\Apps\RoboKeep", appDataDir: @"C:\Users\me\AppData\Roaming", portableFlagPresent: true);
        Assert.Equal(@"C:\Apps\RoboKeep", root);
    }

    [Fact]
    public void NoFlag_UsesAppDataRoboKeep()
    {
        var root = AppDataLocator.ResolveDataRoot(
            exeDir: @"C:\Apps\RoboKeep", appDataDir: @"C:\Users\me\AppData\Roaming", portableFlagPresent: false);
        Assert.Equal(@"C:\Users\me\AppData\Roaming\RoboKeep", root);
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter AppDataLocatorTests`
Expected: FAIL — `AppDataLocator` non esiste.

- [ ] **Step 3: Write minimal implementation**

Create `src/RoboKeep.Core/Services/AppDataLocator.cs`:

```csharp
namespace RoboKeep.Core.Services;

/// <summary>
/// Risolve la cartella dati di RoboKeep (config, esiti, hash, log/temp di default).
/// Default: %APPDATA%\RoboKeep (per-utente, stabile). Modalità portatile se accanto
/// all'eseguibile esiste un file "portable.flag" (allora i dati stanno lì).
/// </summary>
public static class AppDataLocator
{
    public const string PortableFlagFileName = "portable.flag";
    public const string FolderName = "RoboKeep";

    /// <summary>Parte pura e testabile: data root scelta in base alla presenza del flag.</summary>
    public static string ResolveDataRoot(string exeDir, string appDataDir, bool portableFlagPresent)
        => portableFlagPresent ? exeDir : Path.Combine(appDataDir, FolderName);
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter AppDataLocatorTests`
Expected: PASS (2 test)

- [ ] **Step 5: Commit**

```
git add src/RoboKeep.Core/Services/AppDataLocator.cs src/RoboKeep.Tests/AppDataLocatorTests.cs
git commit -m "feat(v1.1): AppDataLocator.ResolveDataRoot puro + test"
```

---

## Task 3: `AppDataLocator.PrepareDataRoot` (IO + migrazione)

Parte IO sottile, non unit-testata (usa `AppContext.BaseDirectory` reale). Va comunque scritta con cura.

**Files:**
- Modify: `src/RoboKeep.Core/Services/AppDataLocator.cs`

- [ ] **Step 1: Add the IO wrapper**

In `src/RoboKeep.Core/Services/AppDataLocator.cs`, dentro la classe `AppDataLocator`, dopo `ResolveDataRoot`:

```csharp
    /// <summary>
    /// Risolve e prepara la data root reale: crea la cartella e, in modalità %APPDATA%,
    /// migra (best-effort) config.json e lastresults.json dalla cartella dell'eseguibile
    /// se non sono ancora presenti nella data root. Restituisce il percorso della data root.
    /// </summary>
    public static string PrepareDataRoot()
    {
        var exeDir = AppContext.BaseDirectory;
        var appData = Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData);
        var portable = File.Exists(Path.Combine(exeDir, PortableFlagFileName));
        var root = ResolveDataRoot(exeDir, appData, portable);

        Directory.CreateDirectory(root);

        if (!portable)
        {
            MigrateFile(exeDir, root, "config.json");
            MigrateFile(exeDir, root, "lastresults.json");
        }
        return root;
    }

    private static void MigrateFile(string fromDir, string toDir, string fileName)
    {
        try
        {
            var src = Path.Combine(fromDir, fileName);
            var dst = Path.Combine(toDir, fileName);
            if (File.Exists(src) && !File.Exists(dst))
                File.Copy(src, dst);
        }
        catch
        {
            // best-effort: la migrazione non deve impedire l'avvio.
        }
    }
```

- [ ] **Step 2: Build to verify it compiles**

Run: `dotnet build src/RoboKeep.Core/RoboKeep.Core.csproj -c Debug`
Expected: Build succeeded.

- [ ] **Step 3: Commit**

```
git add src/RoboKeep.Core/Services/AppDataLocator.cs
git commit -m "feat(v1.1): AppDataLocator.PrepareDataRoot con migrazione best-effort"
```

---

## Task 4: Cablare la data root in `AppHost`

**Files:**
- Modify: `src/RoboKeep/AppHost.cs:27-31` (metodo `Load`)
- Modify: `src/RoboKeep/AppHost.cs:18-25` (costruttore, default log/temp)

- [ ] **Step 1: Usare la data root in `Load`**

In `src/RoboKeep/AppHost.cs`, sostituire il metodo `Load`:

```csharp
    public static AppHost Load(string? configPath = null)
    {
        var path = configPath ?? Path.Combine(AppDataLocator.PrepareDataRoot(), "config.json");
        var store = new ConfigStore(path);
        return new AppHost(store, store.Load());
    }
```

- [ ] **Step 2: Default log/temp sotto la data root**

Nel costruttore `private AppHost(ConfigStore store, AppConfig config)` di `src/RoboKeep/AppHost.cs`, dopo `Config = config;`, aggiungere il default per log/temp se vuoti:

```csharp
        if (string.IsNullOrWhiteSpace(config.Settings.LogRoot))
            config.Settings.LogRoot = Path.Combine(store.DirectoryPath, "logs");
        if (string.IsNullOrWhiteSpace(config.Settings.TempRoot))
            config.Settings.TempRoot = Path.Combine(store.DirectoryPath, "temp");
```

- [ ] **Step 3: Verificare che LogService usi questi valori**

Aprire `src/RoboKeep.Core/Services/LogService.cs` e confermare che `LogRoot`/`TempRoot` vuoti
non vengano più ricalcolati internamente in modo da scavalcare i default impostati qui. Se
LogService ha già un proprio fallback su `AppContext.BaseDirectory`, va bene: i valori ora sono
sempre valorizzati da AppHost, quindi il fallback non scatta. Nessuna modifica se il fallback
è solo per stringa vuota.

- [ ] **Step 4: Build**

Verificare prima: `Get-Process RoboKeep -ErrorAction SilentlyContinue` (se presente, chiedere all'utente di chiudere).
Run: `dotnet build src/RoboKeep.sln -c Debug`
Expected: Build succeeded.

- [ ] **Step 5: Commit**

```
git add src/RoboKeep/AppHost.cs
git commit -m "feat(v1.1): AppHost usa la data root stabile per config/esiti/log"
```

---

## Task 5: `StaleBackupEvaluator` + modello `JobHealth` (puro)

**Files:**
- Create: `src/RoboKeep.Core/Models/JobHealth.cs`
- Create: `src/RoboKeep.Core/Services/StaleBackupEvaluator.cs`
- Test: `src/RoboKeep.Tests/StaleBackupEvaluatorTests.cs`

- [ ] **Step 1: Write the failing test**

Create `src/RoboKeep.Tests/StaleBackupEvaluatorTests.cs`:

```csharp
using RoboKeep.Core.Models;
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public class StaleBackupEvaluatorTests
{
    private static readonly DateTime Now = new(2026, 6, 27, 12, 0, 0);

    private static Dictionary<string, JobLastResult> Map(params JobLastResult[] rs)
        => rs.ToDictionary(r => r.JobName);

    [Fact]
    public void NoResult_IsNeverRun()
    {
        var r = StaleBackupEvaluator.Evaluate(new[] { "A" }, Map(), staleAfterDays: 7, Now);
        Assert.Equal(BackupHealth.NeverRun, r.Single().Health);
    }

    [Fact]
    public void LastFailed_IsFailed()
    {
        var map = Map(new JobLastResult { JobName = "A", Success = false, FinishedAt = Now.AddHours(-1) });
        var r = StaleBackupEvaluator.Evaluate(new[] { "A" }, map, 7, Now);
        Assert.Equal(BackupHealth.Failed, r.Single().Health);
    }

    [Fact]
    public void OldSuccess_IsStale()
    {
        var map = Map(new JobLastResult { JobName = "A", Success = true, FinishedAt = Now.AddDays(-8) });
        var r = StaleBackupEvaluator.Evaluate(new[] { "A" }, map, 7, Now);
        Assert.Equal(BackupHealth.Stale, r.Single().Health);
    }

    [Fact]
    public void RecentSuccess_IsOk()
    {
        var map = Map(new JobLastResult { JobName = "A", Success = true, FinishedAt = Now.AddDays(-1) });
        var r = StaleBackupEvaluator.Evaluate(new[] { "A" }, map, 7, Now);
        Assert.Equal(BackupHealth.Ok, r.Single().Health);
    }

    [Fact]
    public void StaleDisabled_OldSuccess_IsOk()
    {
        var map = Map(new JobLastResult { JobName = "A", Success = true, FinishedAt = Now.AddDays(-100) });
        var r = StaleBackupEvaluator.Evaluate(new[] { "A" }, map, staleAfterDays: 0, Now);
        Assert.Equal(BackupHealth.Ok, r.Single().Health);
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter StaleBackupEvaluatorTests`
Expected: FAIL — `BackupHealth`/`StaleBackupEvaluator` non esistono.

- [ ] **Step 3: Create the model**

Create `src/RoboKeep.Core/Models/JobHealth.cs`:

```csharp
namespace RoboKeep.Core.Models;

/// <summary>Stato di salute di un job dal punto di vista dell'affidabilità del backup.</summary>
public enum BackupHealth
{
    /// <summary>Ultimo backup riuscito e recente.</summary>
    Ok,
    /// <summary>Ultimo backup terminato in errore.</summary>
    Failed,
    /// <summary>Ultimo successo troppo vecchio (oltre la soglia di giorni).</summary>
    Stale,
    /// <summary>Nessun esito registrato: job mai eseguito.</summary>
    NeverRun,
}

/// <summary>Salute di un job (nome + stato).</summary>
public sealed record JobHealth(string JobName, BackupHealth Health);
```

- [ ] **Step 4: Create the evaluator**

Create `src/RoboKeep.Core/Services/StaleBackupEvaluator.cs`:

```csharp
using RoboKeep.Core.Models;

namespace RoboKeep.Core.Services;

/// <summary>
/// Classifica la salute dei job in base all'ultimo esito persistito. Funzione pura:
/// il "now" è un parametro, così i test sono deterministici.
/// </summary>
public static class StaleBackupEvaluator
{
    public static IReadOnlyList<JobHealth> Evaluate(
        IEnumerable<string> jobNames,
        IReadOnlyDictionary<string, JobLastResult> lastResults,
        int staleAfterDays,
        DateTime now)
    {
        var list = new List<JobHealth>();
        foreach (var name in jobNames)
        {
            BackupHealth health;
            if (!lastResults.TryGetValue(name, out var r))
                health = BackupHealth.NeverRun;
            else if (!r.Success)
                health = BackupHealth.Failed;
            else if (staleAfterDays > 0 && (now - r.FinishedAt).TotalDays > staleAfterDays)
                health = BackupHealth.Stale;
            else
                health = BackupHealth.Ok;

            list.Add(new JobHealth(name, health));
        }
        return list;
    }
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter StaleBackupEvaluatorTests`
Expected: PASS (5 test)

- [ ] **Step 6: Commit**

```
git add src/RoboKeep.Core/Models/JobHealth.cs src/RoboKeep.Core/Services/StaleBackupEvaluator.cs src/RoboKeep.Tests/StaleBackupEvaluatorTests.cs
git commit -m "feat(v1.1): StaleBackupEvaluator + JobHealth in TDD"
```

---

## Task 6: `PreflightChecker` + modelli (puro)

**Files:**
- Create: `src/RoboKeep.Core/Models/PreflightModels.cs`
- Create: `src/RoboKeep.Core/Services/PreflightChecker.cs`
- Test: `src/RoboKeep.Tests/PreflightCheckerTests.cs`

- [ ] **Step 1: Write the failing test**

Create `src/RoboKeep.Tests/PreflightCheckerTests.cs`:

```csharp
using RoboKeep.Core.Models;
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public class PreflightCheckerTests
{
    private const long Mb = 1024L * 1024L;

    [Fact]
    public void AllGood_NoWarnings()
    {
        var w = PreflightChecker.Evaluate(new PreflightInputs(
            DestinationReachable: true, FreeBytes: 10_000 * Mb, MinFreeBytes: 1024 * Mb, SourceSizeBytes: 100 * Mb));
        Assert.Empty(w);
    }

    [Fact]
    public void Unreachable_OnlyUnreachableWarning()
    {
        var w = PreflightChecker.Evaluate(new PreflightInputs(
            DestinationReachable: false, FreeBytes: 0, MinFreeBytes: 1024 * Mb, SourceSizeBytes: 100 * Mb));
        var only = Assert.Single(w);
        Assert.Equal("Preflight_DestUnreachable", only.MessageKey);
    }

    [Fact]
    public void LowFreeSpace_Warns()
    {
        var w = PreflightChecker.Evaluate(new PreflightInputs(
            DestinationReachable: true, FreeBytes: 500 * Mb, MinFreeBytes: 1024 * Mb, SourceSizeBytes: null));
        Assert.Contains(w, x => x.MessageKey == "Preflight_LowSpace");
    }

    [Fact]
    public void SourceBiggerThanFree_Warns()
    {
        var w = PreflightChecker.Evaluate(new PreflightInputs(
            DestinationReachable: true, FreeBytes: 2000 * Mb, MinFreeBytes: 1024 * Mb, SourceSizeBytes: 5000 * Mb));
        Assert.Contains(w, x => x.MessageKey == "Preflight_SourceBigger");
    }

    [Fact]
    public void NullSourceSize_NoSizeWarning()
    {
        var w = PreflightChecker.Evaluate(new PreflightInputs(
            DestinationReachable: true, FreeBytes: 2000 * Mb, MinFreeBytes: 1024 * Mb, SourceSizeBytes: null));
        Assert.DoesNotContain(w, x => x.MessageKey == "Preflight_SourceBigger");
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter PreflightCheckerTests`
Expected: FAIL — tipi `PreflightInputs`/`PreflightChecker` non esistono.

- [ ] **Step 3: Create the models**

Create `src/RoboKeep.Core/Models/PreflightModels.cs`:

```csharp
namespace RoboKeep.Core.Models;

/// <summary>Gravità di un avviso pre-avvio.</summary>
public enum PreflightSeverity { Info, Warning }

/// <summary>Avviso pre-avvio: gravità, chiave messaggio localizzabile, dettaglio testuale.</summary>
public sealed record PreflightWarning(PreflightSeverity Severity, string MessageKey, string Detail);

/// <summary>
/// Dati raccolti (IO) per la valutazione pre-avvio. <see cref="SourceSizeBytes"/> è null
/// quando la scansione della sorgente non è stata fatta o ha superato il budget di tempo.
/// </summary>
public sealed record PreflightInputs(
    bool DestinationReachable,
    long FreeBytes,
    long MinFreeBytes,
    long? SourceSizeBytes);
```

- [ ] **Step 4: Create the checker**

Create `src/RoboKeep.Core/Services/PreflightChecker.cs`:

```csharp
using RoboKeep.Core.Models;

namespace RoboKeep.Core.Services;

/// <summary>
/// Valutazione pura dei controlli pre-avvio: produce la lista di avvisi a partire dai dati
/// già raccolti. Non blocca: gli avvisi servono a chiedere conferma all'utente.
/// </summary>
public static class PreflightChecker
{
    public static IReadOnlyList<PreflightWarning> Evaluate(PreflightInputs i)
    {
        var w = new List<PreflightWarning>();

        if (!i.DestinationReachable)
        {
            w.Add(new PreflightWarning(PreflightSeverity.Warning, "Preflight_DestUnreachable", ""));
            return w; // senza destinazione, gli altri controlli non hanno senso.
        }

        if (i.FreeBytes < i.MinFreeBytes)
            w.Add(new PreflightWarning(PreflightSeverity.Warning, "Preflight_LowSpace",
                $"{i.FreeBytes / (1024 * 1024)} MB"));

        if (i.SourceSizeBytes is long size && size > i.FreeBytes)
            w.Add(new PreflightWarning(PreflightSeverity.Warning, "Preflight_SourceBigger",
                $"{size / (1024 * 1024)} MB"));

        return w;
    }
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter PreflightCheckerTests`
Expected: PASS (5 test)

- [ ] **Step 6: Commit**

```
git add src/RoboKeep.Core/Models/PreflightModels.cs src/RoboKeep.Core/Services/PreflightChecker.cs src/RoboKeep.Tests/PreflightCheckerTests.cs
git commit -m "feat(v1.1): PreflightChecker + modelli in TDD"
```

---

## Task 7: `PreflightCollector` (raccolta dati IO)

Parte IO (free space via P/Invoke per supportare anche UNC, scansione sorgente a tempo limitato). Non unit-testata; scritta con cura.

**Files:**
- Create: `src/RoboKeep.Core/Services/PreflightCollector.cs`

- [ ] **Step 1: Implement the collector**

Create `src/RoboKeep.Core/Services/PreflightCollector.cs`:

```csharp
using System.Diagnostics;
using System.Runtime.InteropServices;
using RoboKeep.Core.Models;

namespace RoboKeep.Core.Services;

/// <summary>
/// Raccoglie i dati IO per il pre-avvio: raggiungibilità destinazione, spazio libero
/// (anche su share UNC, via GetDiskFreeSpaceEx) e dimensione sorgente entro un budget di tempo.
/// </summary>
public static class PreflightCollector
{
    public static PreflightInputs Collect(BackupJob job, long minFreeBytes, TimeSpan sizeBudget)
    {
        var reachable = IsReachable(job.Destination);
        var free = reachable ? GetFreeBytes(job.Destination) : 0L;
        var size = reachable ? TryGetSize(job.Source, sizeBudget) : null;
        return new PreflightInputs(reachable, free, minFreeBytes, size);
    }

    private static bool IsReachable(string dest)
    {
        try
        {
            if (string.IsNullOrWhiteSpace(dest)) return false;
            var root = Path.GetPathRoot(dest);
            if (!string.IsNullOrEmpty(root) && Directory.Exists(root)) return true;
            return Directory.Exists(dest);
        }
        catch { return false; }
    }

    private static long GetFreeBytes(string dest)
    {
        try
        {
            var root = Path.GetPathRoot(dest);
            if (string.IsNullOrEmpty(root)) return long.MaxValue;
            if (GetDiskFreeSpaceEx(root, out var freeForCaller, out _, out _))
                return (long)freeForCaller;
            return long.MaxValue; // non determinabile: non far scattare il warning spazio.
        }
        catch { return long.MaxValue; }
    }

    private static long? TryGetSize(string source, TimeSpan budget)
    {
        try
        {
            if (!Directory.Exists(source)) return 0;
            var sw = Stopwatch.StartNew();
            long total = 0;
            foreach (var file in Directory.EnumerateFiles(source, "*", SearchOption.AllDirectories))
            {
                try { total += new FileInfo(file).Length; } catch { /* file sparito: ignora */ }
                if (sw.Elapsed > budget) return null; // troppo lento: rinuncia al confronto dimensione.
            }
            return total;
        }
        catch { return null; }
    }

    [DllImport("kernel32.dll", SetLastError = true, CharSet = CharSet.Unicode)]
    [return: MarshalAs(UnmanagedType.Bool)]
    private static extern bool GetDiskFreeSpaceEx(
        string lpDirectoryName,
        out ulong lpFreeBytesAvailableToCaller,
        out ulong lpTotalNumberOfBytes,
        out ulong lpTotalNumberOfFreeBytes);
}
```

- [ ] **Step 2: Build to verify it compiles**

Run: `dotnet build src/RoboKeep.Core/RoboKeep.Core.csproj -c Debug`
Expected: Build succeeded.

- [ ] **Step 3: Commit**

```
git add src/RoboKeep.Core/Services/PreflightCollector.cs
git commit -m "feat(v1.1): PreflightCollector (free space UNC + size a tempo limitato)"
```

---

## Task 8: Allerta salute nella GUI (InfoBar + evidenziazione righe)

**Files:**
- Modify: `src/RoboKeep/ViewModels/JobViewModel.cs` (proprietà `Health`)
- Modify: `src/RoboKeep/ViewModels/MainViewModel.cs` (valutazione salute + proprietà banner)
- Modify: `src/RoboKeep/MainWindow.xaml` (InfoBar + stile riga)
- Modify: `src/RoboKeep/Localization/Loc.cs` (chiavi)

- [ ] **Step 1: Aggiungere `Health` a JobViewModel**

In `src/RoboKeep/ViewModels/JobViewModel.cs`, aggiungere una proprietà osservabile `Health`
(tipo `RoboKeep.Core.Models.BackupHealth`), seguendo il pattern delle altre proprietà del file
(campo privato + getter/setter con `OnPropertyChanged`). Valore iniziale `BackupHealth.Ok`.

```csharp
    private RoboKeep.Core.Models.BackupHealth _health = RoboKeep.Core.Models.BackupHealth.Ok;
    public RoboKeep.Core.Models.BackupHealth Health
    {
        get => _health;
        set { if (_health != value) { _health = value; OnPropertyChanged(nameof(Health)); } }
    }
```

- [ ] **Step 2: Valutare la salute in MainViewModel**

In `src/RoboKeep/ViewModels/MainViewModel.cs`:

1. Aggiungere proprietà osservabili per il banner (seguendo il pattern esistente):

```csharp
    private bool _healthBannerVisible;
    public bool HealthBannerVisible
    {
        get => _healthBannerVisible;
        private set { _healthBannerVisible = value; OnPropertyChanged(nameof(HealthBannerVisible)); }
    }

    private string _healthBannerText = "";
    public string HealthBannerText
    {
        get => _healthBannerText;
        private set { _healthBannerText = value; OnPropertyChanged(nameof(HealthBannerText)); }
    }
```

2. Aggiungere il metodo di valutazione (usa `_host` per config/esiti; adattare il nome del campo
   AppHost a quello reale del file — cercare il riferimento già usato per `Results`/`Config`):

```csharp
    private void EvaluateHealth()
    {
        var results = _host.Results.Load();
        var names = Jobs.Select(j => j.Name).ToList();
        var health = StaleBackupEvaluator.Evaluate(
            names, results, _host.Config.Settings.StaleAfterDays, DateTime.Now);

        var byName = health.ToDictionary(h => h.JobName, h => h.Health);
        foreach (var jvm in Jobs)
            if (byName.TryGetValue(jvm.Name, out var h)) jvm.Health = h;

        var alerts = health.Where(h => h.Health is BackupHealth.Failed or BackupHealth.Stale).ToList();
        if (alerts.Count == 0)
        {
            HealthBannerVisible = false;
            HealthBannerText = "";
            return;
        }
        var failed = alerts.Count(a => a.Health == BackupHealth.Failed);
        var stale = alerts.Count(a => a.Health == BackupHealth.Stale);
        HealthBannerText = string.Format(Loc.Instance["Health_BannerFormat"], failed, stale);
        HealthBannerVisible = true;
    }
```

3. Chiamare `EvaluateHealth()` alla fine del costruttore (dopo aver popolato `Jobs`) e dentro
   `ReloadLastResults()` (il metodo già esistente del `RefreshCommand`, vedi `MainViewModel.cs:55`),
   in coda. Aggiungere `using RoboKeep.Core.Services;` e `using RoboKeep.Core.Models;` se mancano.

- [ ] **Step 3: InfoBar + evidenziazione riga nello XAML**

In `src/RoboKeep/MainWindow.xaml`, sopra il `DataGrid` dei job, aggiungere un `ui:InfoBar`
(WPF-UI è già usato nel progetto) legato alle nuove proprietà:

```xml
<ui:InfoBar Grid.Row="0"
            Severity="Warning"
            IsOpen="{Binding HealthBannerVisible}"
            IsClosable="False"
            Title="{l:Tr Health_BannerTitle}"
            Message="{Binding HealthBannerText}"
            Margin="0,0,0,8"/>
```

Posizionarlo nella griglia esistente aggiungendo, se serve, una `RowDefinition Height="Auto"` in
cima e spostando di una riga gli elementi sottostanti (seguire la struttura attuale del file).

Per l'evidenziazione riga, aggiungere al `DataGrid` dei job uno `RowStyle` con trigger su `Health`
(usare un convertitore enum→Brush oppure `DataTrigger` per valore). Esempio con `DataTrigger`:

```xml
<DataGrid.RowStyle>
  <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
    <Style.Triggers>
      <DataTrigger Binding="{Binding Health}" Value="Failed">
        <Setter Property="Background" Value="#33FF5252"/>
      </DataTrigger>
      <DataTrigger Binding="{Binding Health}" Value="Stale">
        <Setter Property="Background" Value="#33FFB300"/>
      </DataTrigger>
    </Style.Triggers>
  </Style>
</DataGrid.RowStyle>
```

- [ ] **Step 4: Chiavi di localizzazione**

In `src/RoboKeep/Localization/Loc.cs`, aggiungere le chiavi nelle 5 lingue (seguire il formato
esistente del dizionario). Testo IT (e tradurre EN/ES/FR/DE coerentemente con le altre voci):

- `Health_BannerTitle` → IT "Attenzione ai backup"
- `Health_BannerFormat` → IT "Job con problemi: {0} falliti, {1} fermi da troppo tempo."

- [ ] **Step 5: Build + verifica manuale**

Verificare `Get-Process RoboKeep` (chiusa). Run: `dotnet build src/RoboKeep.sln -c Debug`
Expected: Build succeeded. Avvio manuale: con un job mai eseguito → nessun banner d'allarme;
forzando un `lastresults.json` con `success=false` → banner visibile e riga evidenziata.

- [ ] **Step 6: Commit**

```
git add src/RoboKeep/ViewModels/JobViewModel.cs src/RoboKeep/ViewModels/MainViewModel.cs src/RoboKeep/MainWindow.xaml src/RoboKeep/Localization/Loc.cs
git commit -m "feat(v1.1): allerta backup fermo/fallito (InfoBar + righe evidenziate)"
```

---

## Task 9: Pre-check all'avvio del job (dialog Avvia comunque / Annulla)

**Files:**
- Modify: `src/RoboKeep/ViewModels/MainViewModel.cs` (`StartAsync`, vedi `MainViewModel.cs:170`)
- Modify: `src/RoboKeep/Localization/Loc.cs` (chiavi messaggio preflight)

- [ ] **Step 1: Inserire il pre-check prima dell'esecuzione**

In `src/RoboKeep/ViewModels/MainViewModel.cs`, nel metodo `StartAsync(RunKind k)` (riga ~170),
**prima** di lanciare l'esecuzione effettiva (e solo per esecuzioni reali, non per `Preview`),
inserire il controllo. Aggiungere un metodo helper e chiamarlo:

```csharp
    private bool ConfirmPreflight(IReadOnlyList<JobViewModel> jobs, bool dryRun)
    {
        if (dryRun || !_host.Config.Settings.PreflightEnabled)
            return true;

        long minFree = (long)_host.Config.Settings.MinFreeSpaceMb * 1024 * 1024;
        var budget = TimeSpan.FromSeconds(2);
        var messages = new List<string>();

        foreach (var jvm in jobs)
        {
            var job = _host.Config.Jobs.FirstOrDefault(j => j.Name == jvm.Name);
            if (job is null) continue;
            var inputs = PreflightCollector.Collect(job, minFree, budget);
            foreach (var warn in PreflightChecker.Evaluate(inputs))
                messages.Add($"- {jvm.Name}: {Loc.Instance[warn.MessageKey]} {warn.Detail}".TrimEnd());
        }

        if (messages.Count == 0)
            return true;

        var body = Loc.Instance["Preflight_ConfirmIntro"] + "\n\n" + string.Join("\n", messages);
        var res = System.Windows.MessageBox.Show(
            body, Loc.Instance["Preflight_ConfirmTitle"],
            System.Windows.MessageBoxButton.YesNo, System.Windows.MessageBoxImage.Warning);
        return res == System.Windows.MessageBoxResult.Yes;
    }
```

Quindi, all'inizio di `StartAsync` (dopo aver determinato i job e `dryRun` da `RunKind`),
se `ConfirmPreflight(jobs, dryRun)` ritorna `false`, **interrompere** lo start (return senza
eseguire). Adattare ai nomi/variabili reali del metodo (lista job selezionati, flag dryRun).

- [ ] **Step 2: Chiavi di localizzazione**

In `src/RoboKeep/Localization/Loc.cs`, aggiungere nelle 5 lingue (testo IT, tradurre le altre):

- `Preflight_ConfirmTitle` → "Controlli pre-avvio"
- `Preflight_ConfirmIntro` → "Alcuni controlli segnalano possibili problemi. Avviare comunque?"
- `Preflight_DestUnreachable` → "destinazione non raggiungibile"
- `Preflight_LowSpace` → "spazio libero basso:"
- `Preflight_SourceBigger` → "la sorgente potrebbe non entrare; richiede:"

- [ ] **Step 3: Build + verifica manuale**

Verificare `Get-Process RoboKeep`. Run: `dotnet build src/RoboKeep.sln -c Debug`
Expected: Build succeeded. Verifica: job con destinazione su un percorso inesistente → al click
"Avvia selezionato" appare il dialog Sì/No; "No" non esegue.

- [ ] **Step 4: Commit**

```
git add src/RoboKeep/ViewModels/MainViewModel.cs src/RoboKeep/Localization/Loc.cs
git commit -m "feat(v1.1): pre-check pre-avvio con conferma Avvia comunque/Annulla"
```

---

## Task 10: Notifiche toast + icona tray

**Files:**
- Modify: `src/RoboKeep/RoboKeep.csproj` (pacchetto)
- Modify: `src/RoboKeep/MainWindow.xaml(.cs)` (TaskbarIcon, minimizza/avvio in tray, toast)
- Modify: `src/RoboKeep/ViewModels/MainViewModel.cs` (toast a fine job)
- Modify: `src/RoboKeep/Localization/Loc.cs` (chiavi menu tray / toast)

- [ ] **Step 1: Aggiungere il pacchetto**

In `src/RoboKeep/RoboKeep.csproj`, dentro un `<ItemGroup>` di `PackageReference`:

```xml
<PackageReference Include="H.NotifyIcon.Wpf" Version="2.1.3" />
```

Run: `dotnet restore src/RoboKeep/RoboKeep.csproj`
Expected: restore ok (verifica che la versione esista; in caso aggiornare alla più recente 2.x).

- [ ] **Step 2: TaskbarIcon nello XAML**

In `src/RoboKeep/MainWindow.xaml`, aggiungere il namespace e l'icona tray con menu contestuale:

```xml
xmlns:tb="clr-namespace:H.NotifyIcon;assembly=H.NotifyIcon.Wpf"
```

Dentro la finestra (es. subito dopo l'apertura del root layout):

```xml
<tb:TaskbarIcon x:Name="TrayIcon"
                IconSource="pack://application:,,,/Assets/app.ico"
                ToolTipText="RoboKeep"
                TrayMouseDoubleClick="OnTrayShow">
  <tb:TaskbarIcon.ContextMenu>
    <ContextMenu>
      <MenuItem Header="{l:Tr Tray_Open}" Click="OnTrayShow"/>
      <MenuItem Header="{l:Tr Tray_RunAll}" Click="OnTrayRunAll"/>
      <Separator/>
      <MenuItem Header="{l:Tr Tray_Exit}" Click="OnTrayExit"/>
    </ContextMenu>
  </tb:TaskbarIcon.ContextMenu>
</tb:TaskbarIcon>
```

- [ ] **Step 3: Comportamento tray nel code-behind**

In `src/RoboKeep/MainWindow.xaml.cs`, aggiungere gli handler e il comportamento minimizza/chiudi
in tray. Leggere le impostazioni da `AppHost` (adattare al campo reale, es. `_host`/`App.Host`):

```csharp
    private void OnTrayShow(object sender, System.EventArgs e)
    {
        Show();
        WindowState = System.Windows.WindowState.Normal;
        Activate();
    }

    private void OnTrayRunAll(object sender, System.Windows.RoutedEventArgs e)
        => (DataContext as ViewModels.MainViewModel)?.RunAllCommand.Execute(null);

    private void OnTrayExit(object sender, System.Windows.RoutedEventArgs e)
    {
        TrayIcon.Dispose();
        System.Windows.Application.Current.Shutdown();
    }
```

Per "riduci/chiudi in tray": nel gestore `OnStateChanged` (o override `OnStateChanged`), se
`MinimizeToTray` è attivo e lo stato è `Minimized`, chiamare `Hide()`. Per l'avvio minimizzato,
nel costruttore della finestra (o `OnSourceInitialized`), se `StartMinimized` è attivo,
`WindowState = Minimized; Hide();`. Recuperare i flag da `AppHost.Config.Settings`.

- [ ] **Step 4: Toast a fine job**

Aggiungere un metodo nel code-behind di MainWindow che mostra una notifica con l'API di
H.NotifyIcon:

```csharp
    public void ShowJobToast(string title, string message)
    {
        if (!(System.Windows.Application.Current?.Dispatcher.CheckAccess() ?? true)) return;
        TrayIcon.ShowNotification(title, message);
    }
```

In `src/RoboKeep/ViewModels/MainViewModel.cs`, al termine dell'esecuzione di ciascun job (dentro
`RunJobsAsync`, dove già si aggiornano gli esiti — vedi `MainViewModel.cs:233`), se
`_host.Config.Settings.NotificationsEnabled`, invocare il toast tramite un riferimento alla finestra
(es. `Application.Current.MainWindow as MainWindow`). Titolo: nome job; messaggio: localizzato in
base a successo/errore usando le chiavi `Toast_Ok` / `Toast_Error`.

- [ ] **Step 5: Chiavi di localizzazione**

In `src/RoboKeep/Localization/Loc.cs`, aggiungere (IT, poi EN/ES/FR/DE):

- `Tray_Open` → "Apri"
- `Tray_RunAll` → "Avvia tutti"
- `Tray_Exit` → "Esci"
- `Toast_Ok` → "Backup completato"
- `Toast_Error` → "Backup con errori"

- [ ] **Step 6: Build + verifica manuale**

Verificare `Get-Process RoboKeep`. Run: `dotnet build src/RoboKeep.sln -c Debug`
Expected: Build succeeded. Verifica: l'icona compare nel tray; doppio click riapre; a fine job
appare il toast; con `MinimizeToTray` la riduzione nasconde la finestra nel tray.

- [ ] **Step 7: Commit**

```
git add src/RoboKeep/RoboKeep.csproj src/RoboKeep/MainWindow.xaml src/RoboKeep/MainWindow.xaml.cs src/RoboKeep/ViewModels/MainViewModel.cs src/RoboKeep/Localization/Loc.cs
git commit -m "feat(v1.1): notifiche toast + icona tray (H.NotifyIcon)"
```

---

## Task 11: Impostazioni nuove nella finestra Impostazioni

**Files:**
- Modify: `src/RoboKeep/SettingsWindow.xaml(.cs)`
- Modify: `src/RoboKeep/Localization/Loc.cs`

- [ ] **Step 1: Aggiungere i controlli**

In `src/RoboKeep/SettingsWindow.xaml`, in una sezione "Affidabilità / Notifiche" (seguendo il
layout esistente delle altre impostazioni), aggiungere:

- `StaleAfterDays` → `ui:NumberBox` (0 = disattiva).
- `MinFreeSpaceMb` → `ui:NumberBox`.
- `PreflightEnabled`, `NotificationsEnabled`, `MinimizeToTray`, `StartMinimized` → `ui:ToggleSwitch`.

Etichette via `{l:Tr ...}`. Caricamento/salvataggio: nel code-behind `SettingsWindow.xaml.cs`,
seguendo il pattern già usato per gli altri campi (lettura da `Settings` all'apertura, scrittura
in `Settings` al salvataggio).

- [ ] **Step 2: Chiavi di localizzazione**

In `src/RoboKeep/Localization/Loc.cs` (IT, poi EN/ES/FR/DE):

- `Set_StaleAfterDays` → "Avvisa se un backup è fermo da (giorni, 0 = mai)"
- `Set_MinFreeSpace` → "Spazio libero minimo in destinazione (MB)"
- `Set_PreflightEnabled` → "Controlli pre-avvio (raggiungibilità e spazio)"
- `Set_NotificationsEnabled` → "Notifiche toast a fine backup"
- `Set_MinimizeToTray` → "Riduci nell'area di notifica invece di chiudere"
- `Set_StartMinimized` → "Avvia minimizzato nel tray"

- [ ] **Step 3: Build + verifica manuale**

Verificare `Get-Process RoboKeep`. Run: `dotnet build src/RoboKeep.sln -c Debug`
Expected: Build succeeded. Verifica: i campi compaiono, si salvano e si ricaricano (riaprendo
Impostazioni i valori persistono).

- [ ] **Step 4: Commit**

```
git add src/RoboKeep/SettingsWindow.xaml src/RoboKeep/SettingsWindow.xaml.cs src/RoboKeep/Localization/Loc.cs
git commit -m "feat(v1.1): impostazioni affidabilita/notifiche in Impostazioni"
```

---

## Task 12: README — sezione portabilità/cartella dati (IT + EN)

**Files:**
- Modify: `readmeita.md`
- Modify: `README.md`

- [ ] **Step 1: Aggiornare l'italiano**

In `readmeita.md`, sostituire il punto elenco sulla portabilità (sezione "Perché RoboKeep",
attualmente: "**Portatile.** App, configurazione e log nella stessa cartella...") con:

```markdown
- **Dati al sicuro, fuori dalla cartella dell'app.** Configurazione, esiti e log stanno in
  `%APPDATA%\RoboKeep` (per-utente): non si perdono aggiornando o ripulendo la cartella del
  programma. Per l'uso **portatile** (tutto accanto all'eseguibile) basta creare un file vuoto
  `portable.flag` nella cartella dell'app.
```

E nella sezione "Configurazione", sostituire la frase sul modello portatile con la descrizione
della data root `%APPDATA%\RoboKeep` + `portable.flag`.

- [ ] **Step 2: Aggiornare l'inglese**

In `README.md`, sostituire il punto "**Portable.** App, configuration, and logs all in the same
folder..." con:

```markdown
- **Your data stays safe, outside the app folder.** Configuration, results, and logs live in
  `%APPDATA%\RoboKeep` (per user): they survive updates and cleanups of the program folder. For
  **portable** use (everything next to the executable), just create an empty `portable.flag`
  file in the app folder.
```

E aggiornare la sezione "Configuration" di conseguenza.

- [ ] **Step 3: Commit**

```
git add README.md readmeita.md
git commit -m "docs(v1.1): cartella dati %APPDATA% + modalita portatile con portable.flag"
```

---

## Task 13: Verifica finale

**Files:** nessuna modifica (solo verifica).

- [ ] **Step 1: Test completi**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj`
Expected: tutti i test passano (inclusi i nuovi: AppSettings, AppDataLocator, StaleBackupEvaluator, PreflightChecker).

- [ ] **Step 2: Build soluzione**

Verificare `Get-Process RoboKeep` (chiusa). Run: `dotnet build src/RoboKeep.sln -c Release`
Expected: Build succeeded, 0 warning nuovi rilevanti.

- [ ] **Step 3: Smoke test manuale**

Avvio dell'app: parte, crea/legge la data root in `%APPDATA%\RoboKeep`, mostra eventuale banner
salute, tray presente. Eseguire un job di prova reale (cartelle temporanee): pre-check, toast a
fine, aggiornamento esiti, banner ricalcolato.

- [ ] **Step 4: Aggiornare il CHANGELOG/Directory.Build.props (versione)**

Opzionale ma consigliato: portare `<Version>` in `src/Directory.Build.props` a `1.1.0` quando la
tappa è completa e verificata dall'utente.

---

## Self-Review (eseguita su questo piano)

**Copertura spec:**
- Cartella dati stabile → Task 2, 3, 4 (+ README Task 12). ✓
- Pre-check pre-avvio → Task 6 (puro), 7 (IO), 9 (UI). ✓
- Allerta fermo/fallito → Task 5 (puro), 8 (UI). ✓
- Toast + tray → Task 10. ✓
- Nuovi campi AppSettings + UI Impostazioni → Task 1, 11. ✓

**Coerenza tipi:** `BackupHealth`/`JobHealth` (Task 5) usati in Task 8; `PreflightInputs`/
`PreflightWarning`/`PreflightChecker.Evaluate` (Task 6) usati da `PreflightCollector` (Task 7) e
in Task 9; chiavi messaggio (`Preflight_*`, `Health_*`, `Tray_*`, `Toast_*`, `Set_*`) definite
nei rispettivi task di localizzazione. ✓

**Note di adattamento (codebase reale):** i task UI richiedono di adattarsi ai nomi reali del
campo AppHost dentro i ViewModel e alla struttura XAML esistente (griglie/sezioni). I task Core
sono completamente specificati e testabili senza ambiguità.
