# v1.3 VSS (backup file aperti) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Un job può copiare anche i file aperti/bloccati creando uno snapshot VSS del volume sorgente e copiando da lì.

**Architecture:** RoboKeep (utente) rilancia se stesso elevato (`--vss-helper <dir>`, 1 UAC) che crea lo snapshot via WMI `Win32_ShadowCopy`, espone un symlink e resta vivo fino a fine job. Il client rimappa la sorgente sul symlink (`sourceOverride`, gemello del `destinationOverride` esistente). Ogni fallimento VSS degrada a copia normale con avviso. Protocollo tra i due processi: file JSON in una cartella di sessione.

**Tech Stack:** .NET 10 (net10.0-windows), WPF + WPF-UI, WMI via pacchetto `System.Management`, xUnit.

**Spec:** `docs/superpowers/specs/2026-07-02-v1.3-vss-design.md`

**Regole di progetto (vincolanti):**
- Messaggi di commit SENZA doppi apici `"` (rompono PowerShell); usare here-string `@'...'@`.
- Ogni commit termina con `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Test: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj` (compila solo Core+Tests: gira anche con l'app aperta). NON buildare il progetto WPF se RoboKeep è aperto; in tal caso chiedere all'utente di chiuderlo.
- Stringhe UI in `src/RoboKeep/Localization/Loc.cs` (5 lingue: IT/EN/ES/FR/DE, blocchi separati nello stesso file); stringhe di log/riepilogo Core in `src/RoboKeep.Core/CoreLoc.cs` (metodo `L(it, en, es, fr, de)`).

---

### Task 1: `BackupJob.UseVss` + pacchetto System.Management

**Files:**
- Modify: `src/RoboKeep.Core/RoboKeep.Core.csproj`
- Modify: `src/RoboKeep.Core/Models/BackupJob.cs` (dopo `SnapshotMaxAgeDays`, riga ~87)
- Modify: `src/RoboKeep/MainWindow.xaml.cs` (metodi `Clone` riga ~378 e `CopyInto` riga ~403)
- Test: `src/RoboKeep.Tests/ConfigStoreTests.cs` (aggiunta a test esistenti, se il file esiste; altrimenti nuovo test in `src/RoboKeep.Tests/BackupJobTests.cs`)

- [ ] **Step 1: Aggiungi il pacchetto**

Run: `dotnet add src/RoboKeep.Core/RoboKeep.Core.csproj package System.Management`
Expected: pacchetto aggiunto al csproj (ultima versione stabile).

- [ ] **Step 2: Scrivi il test che fallisce (round-trip config)**

In `src/RoboKeep.Tests/BackupJobTests.cs` (crearlo se non esiste):

```csharp
using System.Text.Json;
using RoboKeep.Core.Models;

namespace RoboKeep.Tests;

public class BackupJobUseVssTests
{
    [Fact]
    public void UseVss_DefaultsToFalse()
        => Assert.False(new BackupJob().UseVss);

    [Fact]
    public void UseVss_RoundTripsThroughJson()
    {
        var job = new BackupJob { Name = "x", UseVss = true };
        var json = JsonSerializer.Serialize(job);
        var back = JsonSerializer.Deserialize<BackupJob>(json)!;
        Assert.True(back.UseVss);
    }
}
```

- [ ] **Step 3: Verifica che fallisca**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter UseVss`
Expected: FAIL di compilazione (`UseVss` non esiste).

- [ ] **Step 4: Aggiungi la proprietà al modello**

In `BackupJob.cs`, dopo `SnapshotMaxAgeDays`:

```csharp
    /// <summary>true → prima della copia crea uno snapshot VSS del volume sorgente e copia da lì,
    /// così anche i file aperti/bloccati vengono letti integri. Richiede sorgente su volume NTFS
    /// locale e conferma amministratore (UAC) all'avvio del run.</summary>
    public bool UseVss { get; set; }
```

- [ ] **Step 5: Aggiorna Clone e CopyInto in `MainWindow.xaml.cs`**

In `Clone` aggiungi dopo `SnapshotMaxAgeDays = j.SnapshotMaxAgeDays,`:

```csharp
        UseVss = j.UseVss,
```

In `CopyInto` aggiungi dopo `to.SnapshotMaxAgeDays = from.SnapshotMaxAgeDays;`:

```csharp
        to.UseVss = from.UseVss;
```

- [ ] **Step 6: Verifica che passi**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter UseVss`
Expected: PASS (2 test).

- [ ] **Step 7: Commit**

```powershell
git add src/RoboKeep.Core/RoboKeep.Core.csproj src/RoboKeep.Core/Models/BackupJob.cs src/RoboKeep/MainWindow.xaml.cs src/RoboKeep.Tests/BackupJobTests.cs
git commit -m @'
feat(v1.3): proprieta UseVss sul job + pacchetto System.Management

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 2: `VssPathMapper` (puro)

**Files:**
- Create: `src/RoboKeep.Core/Services/VssPathMapper.cs`
- Test: `src/RoboKeep.Tests/VssPathMapperTests.cs`

- [ ] **Step 1: Scrivi i test che falliscono**

```csharp
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public class VssPathMapperTests
{
    [Theory]
    [InlineData(@"C:\Users\robi\Documenti", @"C:\")]
    [InlineData(@"e:\Backup", @"E:\")]
    public void GetVolumeRoot_DriveLetterPath_ReturnsRoot(string path, string expected)
        => Assert.Equal(expected, VssPathMapper.GetVolumeRoot(path));

    [Theory]
    [InlineData(@"\\server\share\cartella")]
    [InlineData(@"relativo\cartella")]
    [InlineData("")]
    public void GetVolumeRoot_NonLocalOrRelative_ReturnsNull(string path)
        => Assert.Null(VssPathMapper.GetVolumeRoot(path));

    [Fact]
    public void MapToSnapshot_ReplacesVolumeWithLinkRoot()
        => Assert.Equal(@"D:\sess\source\Users\robi\Documenti",
            VssPathMapper.MapToSnapshot(@"C:\Users\robi\Documenti", @"D:\sess\source"));

    [Fact]
    public void MapToSnapshot_SourceIsVolumeRoot_ReturnsLinkRoot()
        => Assert.Equal(@"D:\sess\source",
            VssPathMapper.MapToSnapshot(@"C:\", @"D:\sess\source"));

    [Fact]
    public void MapToSnapshot_TrailingSlashOnSource_Normalized()
        => Assert.Equal(@"D:\sess\source\Users",
            VssPathMapper.MapToSnapshot(@"C:\Users\", @"D:\sess\source"));
}
```

- [ ] **Step 2: Verifica che falliscano**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter VssPathMapper`
Expected: FAIL di compilazione.

- [ ] **Step 3: Implementa**

```csharp
namespace RoboKeep.Core.Services;

/// <summary>
/// Mappatura percorsi per VSS (funzioni pure): estrae la radice del volume dalla sorgente
/// e riscrive il percorso del job dentro lo snapshot esposto dal symlink di sessione.
/// </summary>
public static class VssPathMapper
{
    /// <summary>Radice del volume (es. <c>C:\</c>) se il percorso è assoluto con lettera di
    /// unità; null per UNC, percorsi relativi o vuoti (VSS non applicabile).</summary>
    public static string? GetVolumeRoot(string path)
    {
        if (string.IsNullOrWhiteSpace(path)) return null;
        if (path.Length < 3) return null;
        if (!char.IsAsciiLetter(path[0]) || path[1] != ':' || (path[2] != '\\' && path[2] != '/'))
            return null;
        return char.ToUpperInvariant(path[0]) + @":\";
    }

    /// <summary>Riscrive <paramref name="sourcePath"/> (es. <c>C:\Users\x</c>) sotto la radice
    /// dello snapshot (es. <c>D:\sess\source</c> → <c>D:\sess\source\Users\x</c>).</summary>
    public static string MapToSnapshot(string sourcePath, string snapshotRoot)
    {
        var volume = GetVolumeRoot(sourcePath)
            ?? throw new ArgumentException($"Percorso non idoneo a VSS: {sourcePath}");
        var relative = sourcePath[volume.Length..].TrimEnd('\\', '/');
        return relative.Length == 0 ? snapshotRoot : Path.Combine(snapshotRoot, relative);
    }
}
```

- [ ] **Step 4: Verifica che passino**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter VssPathMapper`
Expected: PASS (7 casi).

- [ ] **Step 5: Commit**

```powershell
git add src/RoboKeep.Core/Services/VssPathMapper.cs src/RoboKeep.Tests/VssPathMapperTests.cs
git commit -m @'
feat(v1.3): VssPathMapper - radice volume e rimappatura sorgente su snapshot

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 3: `VssSessionProtocol` (puro)

**Files:**
- Create: `src/RoboKeep.Core/Services/VssSessionProtocol.cs`
- Test: `src/RoboKeep.Tests/VssSessionProtocolTests.cs`

- [ ] **Step 1: Scrivi i test che falliscono**

```csharp
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public class VssSessionProtocolTests : IDisposable
{
    private readonly string _dir = Path.Combine(Path.GetTempPath(), Guid.NewGuid().ToString());

    public VssSessionProtocolTests() => Directory.CreateDirectory(_dir);
    public void Dispose() { if (Directory.Exists(_dir)) Directory.Delete(_dir, true); }

    [Fact]
    public void Paths_AreInsideSessionDir()
    {
        Assert.Equal(Path.Combine(_dir, "request.json"), VssSessionProtocol.RequestFile(_dir));
        Assert.Equal(Path.Combine(_dir, "ready.json"), VssSessionProtocol.ReadyFile(_dir));
        Assert.Equal(Path.Combine(_dir, "release.flag"), VssSessionProtocol.ReleaseFile(_dir));
        Assert.Equal(Path.Combine(_dir, "source"), VssSessionProtocol.LinkDir(_dir));
    }

    [Fact]
    public void Request_RoundTrips()
    {
        var req = new VssRequest(@"C:\", 1234, new List<string> { "{ID-1}" });
        VssSessionProtocol.WriteRequest(_dir, req);
        var back = VssSessionProtocol.ReadRequest(_dir);
        Assert.Equal(@"C:\", back.Volume);
        Assert.Equal(1234, back.ParentPid);
        Assert.Equal(new[] { "{ID-1}" }, back.StaleShadowIds);
    }

    [Fact]
    public void Ready_Success_RoundTrips()
    {
        VssSessionProtocol.WriteReady(_dir, VssReady.Ok("{SHADOW-ID}", @"D:\sess\source"));
        var back = VssSessionProtocol.ReadReady(_dir)!;
        Assert.True(back.Success);
        Assert.Equal("{SHADOW-ID}", back.ShadowId);
        Assert.Equal(@"D:\sess\source", back.LinkPath);
        Assert.Null(back.Error);
    }

    [Fact]
    public void Ready_Failure_RoundTrips()
    {
        VssSessionProtocol.WriteReady(_dir, VssReady.Fail("WMI 0x80042308"));
        var back = VssSessionProtocol.ReadReady(_dir)!;
        Assert.False(back.Success);
        Assert.Equal("WMI 0x80042308", back.Error);
    }

    [Fact]
    public void ReadReady_MissingFile_ReturnsNull()
        => Assert.Null(VssSessionProtocol.ReadReady(_dir));
}
```

- [ ] **Step 2: Verifica che falliscano**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter VssSessionProtocol`
Expected: FAIL di compilazione.

- [ ] **Step 3: Implementa**

```csharp
using System.Text.Json;

namespace RoboKeep.Core.Services;

/// <summary>Richiesta del client al helper elevato: volume da fotografare, PID del padre da
/// sorvegliare e snapshot residui di run precedenti da eliminare.</summary>
public sealed record VssRequest(string Volume, int ParentPid, List<string> StaleShadowIds);

/// <summary>Esito della creazione snapshot scritto dal helper.</summary>
public sealed record VssReady(bool Success, string? ShadowId, string? LinkPath, string? Error)
{
    public static VssReady Ok(string shadowId, string linkPath) => new(true, shadowId, linkPath, null);
    public static VssReady Fail(string error) => new(false, null, null, error);
}

/// <summary>
/// Protocollo file della sessione VSS tra RoboKeep (utente) e il helper elevato:
/// request.json (client→helper), ready.json (helper→client), release.flag (client→helper),
/// e la cartella del symlink verso il device dello snapshot.
/// </summary>
public static class VssSessionProtocol
{
    private static readonly JsonSerializerOptions Options = new() { WriteIndented = true };

    public static string RequestFile(string sessionDir) => Path.Combine(sessionDir, "request.json");
    public static string ReadyFile(string sessionDir) => Path.Combine(sessionDir, "ready.json");
    public static string ReleaseFile(string sessionDir) => Path.Combine(sessionDir, "release.flag");
    public static string LinkDir(string sessionDir) => Path.Combine(sessionDir, "source");

    public static void WriteRequest(string sessionDir, VssRequest request) =>
        File.WriteAllText(RequestFile(sessionDir), JsonSerializer.Serialize(request, Options));

    public static VssRequest ReadRequest(string sessionDir) =>
        JsonSerializer.Deserialize<VssRequest>(File.ReadAllText(RequestFile(sessionDir)), Options)
            ?? throw new InvalidDataException("request.json vuoto o non valido.");

    public static void WriteReady(string sessionDir, VssReady ready) =>
        File.WriteAllText(ReadyFile(sessionDir), JsonSerializer.Serialize(ready, Options));

    /// <summary>null se il file non esiste ancora (helper non pronto).</summary>
    public static VssReady? ReadReady(string sessionDir)
    {
        var path = ReadyFile(sessionDir);
        if (!File.Exists(path)) return null;
        return JsonSerializer.Deserialize<VssReady>(File.ReadAllText(path), Options);
    }

    public static void SignalRelease(string sessionDir) =>
        File.WriteAllText(ReleaseFile(sessionDir), "");
}
```

- [ ] **Step 4: Verifica che passino**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter VssSessionProtocol`
Expected: PASS (5 test).

- [ ] **Step 5: Commit**

```powershell
git add src/RoboKeep.Core/Services/VssSessionProtocol.cs src/RoboKeep.Tests/VssSessionProtocolTests.cs
git commit -m @'
feat(v1.3): protocollo file sessione VSS (request/ready/release)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 4: `VssLedger` (registro snapshot per orfani)

**Files:**
- Create: `src/RoboKeep.Core/Services/VssLedger.cs`
- Test: `src/RoboKeep.Tests/VssLedgerTests.cs`

- [ ] **Step 1: Scrivi i test che falliscono**

```csharp
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public class VssLedgerTests : IDisposable
{
    private readonly string _dir = Path.Combine(Path.GetTempPath(), Guid.NewGuid().ToString());
    private string LedgerPath => Path.Combine(_dir, "vss-ledger.json");

    public void Dispose() { if (Directory.Exists(_dir)) Directory.Delete(_dir, true); }

    [Fact]
    public void List_MissingFile_ReturnsEmpty()
        => Assert.Empty(new VssLedger(LedgerPath).List());

    [Fact]
    public void Add_ThenList_ContainsId()
    {
        var ledger = new VssLedger(LedgerPath);
        ledger.Add("{ID-1}");
        Assert.Equal(new[] { "{ID-1}" }, ledger.List());
    }

    [Fact]
    public void Remove_DeletesId_OthersSurvive()
    {
        var ledger = new VssLedger(LedgerPath);
        ledger.Add("{ID-1}");
        ledger.Add("{ID-2}");
        ledger.Remove("{ID-1}");
        Assert.Equal(new[] { "{ID-2}" }, ledger.List());
    }

    [Fact]
    public void Add_Duplicate_NotDuplicated()
    {
        var ledger = new VssLedger(LedgerPath);
        ledger.Add("{ID-1}");
        ledger.Add("{ID-1}");
        Assert.Single(ledger.List());
    }
}
```

- [ ] **Step 2: Verifica che falliscano**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter VssLedger`
Expected: FAIL di compilazione.

- [ ] **Step 3: Implementa**

```csharp
using System.Text.Json;

namespace RoboKeep.Core.Services;

/// <summary>
/// Registro su file degli shadow ID creati da RoboKeep. Se un run VSS muore senza pulizia,
/// l'ID resta nel registro: il run VSS successivo lo passa al helper (elevato) che elimina
/// lo snapshot residuo. Stesso pattern della pulizia .inprogress del versioning.
/// </summary>
public sealed class VssLedger
{
    private readonly string _path;

    public VssLedger(string path) => _path = path;

    public IReadOnlyList<string> List()
    {
        if (!File.Exists(_path)) return Array.Empty<string>();
        try
        {
            return JsonSerializer.Deserialize<List<string>>(File.ReadAllText(_path)) ?? new();
        }
        catch { return Array.Empty<string>(); }
    }

    public void Add(string shadowId)
    {
        var ids = List().ToList();
        if (!ids.Contains(shadowId)) ids.Add(shadowId);
        Save(ids);
    }

    public void Remove(string shadowId)
    {
        var ids = List().Where(i => i != shadowId).ToList();
        Save(ids);
    }

    private void Save(List<string> ids)
    {
        var dir = Path.GetDirectoryName(_path);
        if (!string.IsNullOrEmpty(dir)) Directory.CreateDirectory(dir);
        File.WriteAllText(_path, JsonSerializer.Serialize(ids));
    }
}
```

- [ ] **Step 4: Verifica che passino**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter VssLedger`
Expected: PASS (4 test).

- [ ] **Step 5: Commit**

```powershell
git add src/RoboKeep.Core/Services/VssLedger.cs src/RoboKeep.Tests/VssLedgerTests.cs
git commit -m @'
feat(v1.3): VssLedger - registro shadow ID per pulizia snapshot orfani

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 5: `sourceOverride` in args builder, runner e snapshot service

**Files:**
- Modify: `src/RoboKeep.Core/Services/RobocopyArgsBuilder.cs` (metodi `Build` riga ~21 e `BuildForceCopyPass` riga ~102)
- Modify: `src/RoboKeep.Core/Services/RobocopyRunner.cs` (metodo `RunAsync` riga ~49; il metodo ha anche una passata force-copy interna: cerca `BuildForceCopyPass` nel file e propaga lì l'override)
- Modify: `src/RoboKeep.Core/Services/SnapshotService.cs` (metodo `RunVersionedAsync` riga ~17 e chiamata `_runner.RunAsync` riga ~72)
- Test: `src/RoboKeep.Tests/RobocopyArgsBuilderTests.cs` (aggiunta)

- [ ] **Step 1: Scrivi i test che falliscono**

Aggiungi in fondo alla classe di test esistente in `RobocopyArgsBuilderTests.cs`:

```csharp
    [Fact]
    public void Build_SourceOverride_ReplacesSource()
    {
        var job = new BackupJob { Name = "j", Source = @"C:\dati", Destination = @"E:\bk" };
        var args = RobocopyArgsBuilder.Build(job, sourceOverride: @"D:\sess\source\dati");
        Assert.Equal(@"D:\sess\source\dati", args[0]);
        Assert.Equal(@"E:\bk", args[1]);
    }

    [Fact]
    public void Build_NoSourceOverride_UsesJobSource()
    {
        var job = new BackupJob { Name = "j", Source = @"C:\dati", Destination = @"E:\bk" };
        var args = RobocopyArgsBuilder.Build(job);
        Assert.Equal(@"C:\dati", args[0]);
    }

    [Fact]
    public void BuildForceCopyPass_SourceOverride_ReplacesSource()
    {
        var job = new BackupJob { Name = "j", Source = @"C:\dati", Destination = @"E:\bk" };
        var args = RobocopyArgsBuilder.BuildForceCopyPass(job, new[] { "*.pst" },
            sourceOverride: @"D:\sess\source\dati");
        Assert.Equal(@"D:\sess\source\dati", args[0]);
    }
```

- [ ] **Step 2: Verifica che falliscano**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter SourceOverride`
Expected: FAIL di compilazione (parametro inesistente).

- [ ] **Step 3: Aggiungi il parametro a `Build`**

Firma:

```csharp
    public static IReadOnlyList<string> Build(
        BackupJob job, bool dryRun = false, string? logFile = null, string? destinationOverride = null,
        string? sourceOverride = null)
```

E la riga della sorgente diventa:

```csharp
        var source = (sourceOverride ?? job.Source ?? "").Trim();
```

Stessa modifica in `BuildForceCopyPass` (firma con `string? sourceOverride = null` in coda, `var source = (sourceOverride ?? job.Source ?? "").Trim();`).

- [ ] **Step 4: Propaga in `RobocopyRunner.RunAsync`**

Firma (riga ~49):

```csharp
    public async Task<RobocopyRunResult> RunAsync(
        BackupJob job, bool dryRun = false, IProgress<string>? progress = null, CancellationToken ct = default,
        string? destinationOverride = null, string? sourceOverride = null)
```

Alla chiamata `RobocopyArgsBuilder.Build(job, dryRun, destinationOverride: destinationOverride)` (riga ~57) aggiungi `, sourceOverride: sourceOverride`. Cerca nel file eventuali chiamate a `BuildForceCopyPass` (passata force-copy) e aggiungi anche lì `sourceOverride: sourceOverride`.

- [ ] **Step 5: Propaga in `SnapshotService.RunVersionedAsync`**

Firma (riga ~17):

```csharp
    public async Task<RobocopyRunResult> RunVersionedAsync(
        BackupJob job, IProgress<string>? progress = null, CancellationToken ct = default,
        string? sourceOverride = null)
```

Alla chiamata `_runner.RunAsync(job, dryRun: false, progress, ct, destinationOverride: curr)` (riga ~72) aggiungi `, sourceOverride: sourceOverride`.

- [ ] **Step 6: Verifica che tutto passi**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj`
Expected: PASS, nessuna regressione.

- [ ] **Step 7: Commit**

```powershell
git add src/RoboKeep.Core/Services/RobocopyArgsBuilder.cs src/RoboKeep.Core/Services/RobocopyRunner.cs src/RoboKeep.Core/Services/SnapshotService.cs src/RoboKeep.Tests/RobocopyArgsBuilderTests.cs
git commit -m @'
feat(v1.3): sourceOverride in args builder, runner e versioning

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 6: `VssUnavailableException` + `VssHelper` (lato elevato, WMI)

**Files:**
- Create: `src/RoboKeep.Core/Services/VssUnavailableException.cs`
- Create: `src/RoboKeep.Core/Services/VssHelper.cs`

Nessun unit test per il helper (WMI + admin: si copre nello smoke manuale). L'eccezione è banale.

- [ ] **Step 1: Crea l'eccezione**

```csharp
namespace RoboKeep.Core.Services;

/// <summary>Lo snapshot VSS non si può creare (UAC negato, WMI in errore, timeout, volume
/// non idoneo). Il chiamante degrada a copia normale con avviso: mai bloccare il backup.</summary>
public sealed class VssUnavailableException : Exception
{
    public VssUnavailableException(string message) : base(message) { }
    public VssUnavailableException(string message, Exception inner) : base(message, inner) { }
}
```

- [ ] **Step 2: Crea il helper**

```csharp
using System.Diagnostics;
using System.Management;

namespace RoboKeep.Core.Services;

/// <summary>
/// Lato elevato della sessione VSS (gira in un processo con privilegi amministrativi,
/// lanciato da RoboKeep con --vss-helper). Crea lo snapshot del volume via WMI, espone
/// un symlink al device e resta vivo finché il client non segnala il rilascio, il padre
/// muore o scade il timeout di sicurezza. Pulisce sempre, anche in caso di errore.
/// </summary>
public static class VssHelper
{
    private static readonly TimeSpan SafetyTimeout = TimeSpan.FromHours(12);
    private static readonly TimeSpan PollInterval = TimeSpan.FromSeconds(1);

    /// <summary>Punto di ingresso del processo elevato. Ritorna l'exit code.</summary>
    public static int Run(string sessionDir)
    {
        string? shadowId = null;
        string? linkDir = null;
        try
        {
            var request = VssSessionProtocol.ReadRequest(sessionDir);

            // Pulizia residui di run precedenti (best-effort: siamo già elevati, è il
            // momento giusto). Un ID inesistente non è un errore.
            foreach (var stale in request.StaleShadowIds)
                TryDeleteShadow(stale);

            shadowId = CreateShadow(request.Volume);
            var device = GetDeviceObject(shadowId);

            linkDir = VssSessionProtocol.LinkDir(sessionDir);
            // Il device path richiede il backslash finale per essere attraversabile.
            Directory.CreateSymbolicLink(linkDir, device + @"\");

            VssSessionProtocol.WriteReady(sessionDir, VssReady.Ok(shadowId, linkDir));

            WaitForRelease(sessionDir, request.ParentPid);
            return 0;
        }
        catch (Exception ex)
        {
            try { VssSessionProtocol.WriteReady(sessionDir, VssReady.Fail(ex.Message)); }
            catch { }
            return 1;
        }
        finally
        {
            if (linkDir is not null)
                try { Directory.Delete(linkDir); } catch { }
            if (shadowId is not null)
                TryDeleteShadow(shadowId);
        }
    }

    private static string CreateShadow(string volume)
    {
        using var shadowClass = new ManagementClass("Win32_ShadowCopy");
        using var inParams = shadowClass.GetMethodParameters("Create");
        inParams["Volume"] = volume;
        inParams["Context"] = "ClientAccessible";
        using var outParams = shadowClass.InvokeMethod("Create", inParams, null);

        var returnValue = Convert.ToInt32(outParams["ReturnValue"]);
        if (returnValue != 0)
            throw new VssUnavailableException($"Win32_ShadowCopy.Create fallita (codice {returnValue}).");
        return (string)outParams["ShadowID"];
    }

    private static string GetDeviceObject(string shadowId)
    {
        using var searcher = new ManagementObjectSearcher(
            $"SELECT DeviceObject FROM Win32_ShadowCopy WHERE ID = '{shadowId}'");
        foreach (ManagementObject shadow in searcher.Get())
            return (string)shadow["DeviceObject"];
        throw new VssUnavailableException($"Snapshot {shadowId} creato ma non trovato in WMI.");
    }

    private static void TryDeleteShadow(string shadowId)
    {
        try
        {
            using var searcher = new ManagementObjectSearcher(
                $"SELECT * FROM Win32_ShadowCopy WHERE ID = '{shadowId}'");
            foreach (ManagementObject shadow in searcher.Get())
                shadow.Delete();
        }
        catch { /* best-effort: l'ID resta nel ledger e verrà ritentato al prossimo run */ }
    }

    private static void WaitForRelease(string sessionDir, int parentPid)
    {
        var releaseFile = VssSessionProtocol.ReleaseFile(sessionDir);
        var deadline = DateTime.UtcNow + SafetyTimeout;
        while (DateTime.UtcNow < deadline)
        {
            if (File.Exists(releaseFile)) return;
            if (!IsProcessAlive(parentPid)) return;
            Thread.Sleep(PollInterval);
        }
    }

    private static bool IsProcessAlive(int pid)
    {
        try { return !Process.GetProcessById(pid).HasExited; }
        catch { return false; }
    }
}
```

- [ ] **Step 3: Verifica la compilazione**

Run: `dotnet build src/RoboKeep.Core/RoboKeep.Core.csproj -v quiet`
Expected: 0 errori (il progetto WPF non viene toccato).

- [ ] **Step 4: Commit**

```powershell
git add src/RoboKeep.Core/Services/VssUnavailableException.cs src/RoboKeep.Core/Services/VssHelper.cs
git commit -m @'
feat(v1.3): VssHelper - creazione snapshot WMI, symlink, attesa release e pulizia

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 7: `VssSession` (lato client)

**Files:**
- Create: `src/RoboKeep.Core/Services/VssSession.cs`

Anche qui niente unit test diretto (lancia un processo elevato): le parti pure sono già coperte (mapper, protocollo, ledger). La logica di attesa/timeout si verifica nello smoke.

- [ ] **Step 1: Implementa**

```csharp
using System.ComponentModel;
using System.Diagnostics;

namespace RoboKeep.Core.Services;

/// <summary>
/// Lato client della sessione VSS: lancia il helper elevato (prompt UAC), attende lo snapshot
/// e espone la sorgente rimappata. Il Dispose segnala il rilascio e attende la pulizia.
/// Qualsiasi fallimento diventa <see cref="VssUnavailableException"/>: il chiamante degrada
/// a copia normale, mai bloccare il backup.
/// </summary>
public sealed class VssSession : IAsyncDisposable
{
    private const int ErrorCancelled = 1223; // UAC negato dall'utente
    private static readonly TimeSpan ReadyTimeout = TimeSpan.FromSeconds(60);
    private static readonly TimeSpan ExitTimeout = TimeSpan.FromSeconds(30);
    private static readonly TimeSpan PollInterval = TimeSpan.FromMilliseconds(250);

    private readonly string _sessionDir;
    private readonly VssLedger _ledger;
    private readonly Process _helper;

    /// <summary>Percorso del job rimappato dentro lo snapshot (da passare come sourceOverride).</summary>
    public string SnapshotSourcePath { get; }
    public string ShadowId { get; }

    private VssSession(string sessionDir, VssLedger ledger, Process helper, string shadowId, string mapped)
    {
        _sessionDir = sessionDir;
        _ledger = ledger;
        _helper = helper;
        ShadowId = shadowId;
        SnapshotSourcePath = mapped;
    }

    /// <summary>
    /// Apre una sessione: helper elevato (UAC), snapshot del volume di <paramref name="sourcePath"/>.
    /// Lancia <see cref="VssUnavailableException"/> se lo snapshot non è disponibile.
    /// </summary>
    public static async Task<VssSession> OpenAsync(
        string sourcePath, string sessionRoot, VssLedger ledger, CancellationToken ct = default)
    {
        var volume = VssPathMapper.GetVolumeRoot(sourcePath)
            ?? throw new VssUnavailableException(CoreLoc.S("Vss_NotEligible"));

        var exePath = Environment.ProcessPath
            ?? throw new VssUnavailableException("Percorso eseguibile non determinabile.");

        var sessionDir = Path.Combine(sessionRoot, Guid.NewGuid().ToString("N"));
        Directory.CreateDirectory(sessionDir);

        VssSessionProtocol.WriteRequest(sessionDir, new VssRequest(
            volume, Environment.ProcessId, ledger.List().ToList()));

        Process helper;
        try
        {
            helper = Process.Start(new ProcessStartInfo
            {
                FileName = exePath,
                Arguments = $"--vss-helper \"{sessionDir}\"",
                UseShellExecute = true, // necessario per il verbo runas
                Verb = "runas",
            }) ?? throw new VssUnavailableException("Avvio del processo elevato fallito.");
        }
        catch (Win32Exception ex) when (ex.NativeErrorCode == ErrorCancelled)
        {
            TryDeleteDir(sessionDir);
            throw new VssUnavailableException(CoreLoc.S("Vss_UacDenied"), ex);
        }

        // Il timeout parte da qui: Process.Start con runas ritorna solo dopo la risposta
        // al prompt UAC, quindi il tempo di decisione dell'utente non è conteggiato.
        var deadline = DateTime.UtcNow + ReadyTimeout;
        VssReady? ready;
        while ((ready = VssSessionProtocol.ReadReady(sessionDir)) is null)
        {
            if (helper.HasExited && VssSessionProtocol.ReadReady(sessionDir) is null)
                throw new VssUnavailableException("Il processo elevato è terminato senza esito.");
            if (DateTime.UtcNow > deadline)
                throw new VssUnavailableException("Timeout in attesa dello snapshot (60s).");
            ct.ThrowIfCancellationRequested();
            await Task.Delay(PollInterval, ct).ConfigureAwait(false);
        }

        if (!ready.Success || ready.ShadowId is null || ready.LinkPath is null)
            throw new VssUnavailableException(ready.Error ?? "Errore snapshot sconosciuto.");

        ledger.Add(ready.ShadowId);
        var mapped = VssPathMapper.MapToSnapshot(sourcePath, ready.LinkPath);
        return new VssSession(sessionDir, ledger, helper, ready.ShadowId, mapped);
    }

    /// <summary>Segnala il rilascio, attende la pulizia del helper e rimuove l'ID dal registro.</summary>
    public async ValueTask DisposeAsync()
    {
        try
        {
            VssSessionProtocol.SignalRelease(_sessionDir);
            var deadline = DateTime.UtcNow + ExitTimeout;
            while (!_helper.HasExited && DateTime.UtcNow < deadline)
                await Task.Delay(PollInterval).ConfigureAwait(false);

            // Se il helper è uscito pulito ha cancellato lo snapshot: togli l'ID dal registro.
            // Se non è uscito, lascia l'ID: verrà ripulito al prossimo run VSS.
            if (_helper.HasExited)
                _ledger.Remove(ShadowId);
        }
        finally
        {
            _helper.Dispose();
            TryDeleteDir(_sessionDir);
        }
    }

    private static void TryDeleteDir(string dir)
    {
        try { Directory.Delete(dir, true); } catch { }
    }
}
```

- [ ] **Step 2: Verifica la compilazione**

Run: `dotnet build src/RoboKeep.Core/RoboKeep.Core.csproj -v quiet`
Expected: 0 errori. NB: `CoreLoc.S("Vss_NotEligible")` e `"Vss_UacDenied"` esistono dal Task 9 — se il build fallisce solo per quelle chiavi, usa stringhe letterali provvisorie e sostituiscile nel Task 9 (oppure esegui prima gli step CoreLoc del Task 9).

- [ ] **Step 3: Commit**

```powershell
git add src/RoboKeep.Core/Services/VssSession.cs
git commit -m @'
feat(v1.3): VssSession - lancio helper elevato, attesa ready, dispose con release

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 8: CLI `--vss-helper`

**Files:**
- Modify: `src/RoboKeep/CliOptions.cs`
- Modify: `src/RoboKeep/App.xaml.cs`
- Test: `src/RoboKeep.Tests/CliOptionsTests.cs` — ATTENZIONE: `CliOptions` sta nel progetto WPF, che i test non referenziano. Verifica con `Grep "CliOptions" src/RoboKeep.Tests`: se non ci sono test esistenti per CliOptions, salta il test unitario (la logica è un parse banale coperto dallo smoke).

- [ ] **Step 1: Aggiungi l'opzione al parser**

In `CliOptions.cs` aggiungi la proprietà dopo `ConfigPath`:

```csharp
    /// <summary>Cartella di sessione VSS: se presente, il processo esegue il helper elevato
    /// (creazione snapshot) e termina. Uso interno, non documentato all'utente.</summary>
    public string? VssHelperDir { get; private set; }
```

E il case nel `switch` di `Parse`:

```csharp
                case "--vss-helper":
                    if (i + 1 < args.Length)
                        o.VssHelperDir = args[++i];
                    break;
```

- [ ] **Step 2: Dispatch in `App.OnStartup`**

In `App.xaml.cs`, subito dopo `var options = CliOptions.Parse(e.Args);` (prima del blocco `HasCommand`):

```csharp
        if (options.VssHelperDir is not null)
        {
            // Modalità helper elevato: crea lo snapshot, attende il rilascio, pulisce, esce.
            // Niente GUI, niente localizzazione: il processo comunica solo via file di sessione.
            var helperExit = RoboKeep.Core.Services.VssHelper.Run(options.VssHelperDir);
            Shutdown(helperExit);
            return;
        }
```

- [ ] **Step 3: Verifica la compilazione dell'app WPF**

PRIMA chiedi all'utente di chiudere RoboKeep se aperto. Poi:
Run: `dotnet build src/RoboKeep/RoboKeep.csproj -c Debug -v quiet`
Expected: 0 errori.

- [ ] **Step 4: Commit**

```powershell
git add src/RoboKeep/CliOptions.cs src/RoboKeep/App.xaml.cs
git commit -m @'
feat(v1.3): flag CLI --vss-helper per il processo elevato

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 9: Integrazione `BackupRunner` + stringhe CoreLoc

**Files:**
- Modify: `src/RoboKeep.Core/CoreLoc.cs` (dizionario `Map`)
- Modify: `src/RoboKeep.Core/Services/BackupRunner.cs`
- Modify: `src/RoboKeep/AppHost.cs` (`BuildRunner`, riga ~43)

- [ ] **Step 1: Aggiungi le chiavi CoreLoc**

Nel dizionario `Map` di `CoreLoc.cs` (stile delle voci esistenti, metodo `L(it, en, es, fr, de)`):

```csharp
        ["Vss_Created"] = L(
            "[vss] snapshot creato ({0}): copio dai file congelati, inclusi quelli aperti.",
            "[vss] snapshot created ({0}): copying from frozen files, including open ones.",
            "[vss] instantánea creada ({0}): copio desde los archivos congelados, incluidos los abiertos.",
            "[vss] instantané créé ({0}) : copie depuis les fichiers figés, y compris ceux ouverts.",
            "[vss] Snapshot erstellt ({0}): Kopie aus eingefrorenen Dateien, auch geöffnete."),
        ["Vss_Released"] = L(
            "[vss] snapshot rilasciato.",
            "[vss] snapshot released.",
            "[vss] instantánea liberada.",
            "[vss] instantané libéré.",
            "[vss] Snapshot freigegeben."),
        ["Vss_Unavailable"] = L(
            "[vss] ATTENZIONE: snapshot non disponibile ({0}): i file aperti verranno saltati.",
            "[vss] WARNING: snapshot unavailable ({0}): open files will be skipped.",
            "[vss] ATENCIÓN: instantánea no disponible ({0}): los archivos abiertos se omitirán.",
            "[vss] ATTENTION : instantané indisponible ({0}) : les fichiers ouverts seront ignorés.",
            "[vss] ACHTUNG: Snapshot nicht verfügbar ({0}): geöffnete Dateien werden übersprungen."),
        ["Vss_UacDenied"] = L(
            "conferma amministratore negata",
            "administrator approval denied",
            "aprobación de administrador denegada",
            "approbation administrateur refusée",
            "Administratorbestätigung verweigert"),
        ["Vss_NotEligible"] = L(
            "la sorgente non è su un volume locale NTFS",
            "the source is not on a local NTFS volume",
            "el origen no está en un volumen NTFS local",
            "la source n'est pas sur un volume NTFS local",
            "die Quelle liegt nicht auf einem lokalen NTFS-Volume"),
```

- [ ] **Step 2: Costruttore `BackupRunner` con radice sessioni VSS**

In `BackupRunner.cs`, aggiungi campo e parametro (dopo `lockFolder`, stile identico):

```csharp
    private readonly string? _vssSessionRoot;
```

Firma costruttore: aggiungi `string? vssSessionRoot = null` in coda e `_vssSessionRoot = vssSessionRoot;` nel corpo.

- [ ] **Step 3: Apertura/chiusura sessione in `RunJobAsync`**

Dentro `RunJobAsync`, dopo la riga `using var lockHandle = ...` e prima di `var cred = ...`:

```csharp
        // Snapshot VSS: se richiesto e disponibile, la copia legge dallo snapshot invece
        // che dalla sorgente viva. Se non disponibile → copia normale con avviso (mai bloccare).
        VssSession? vss = null;
        string? sourceOverride = null;
        if (job.UseVss && !dryRun && _vssSessionRoot is not null)
        {
            try
            {
                var ledger = new VssLedger(Path.Combine(_vssSessionRoot, "vss-ledger.json"));
                vss = await VssSession.OpenAsync(job.Source, _vssSessionRoot, ledger, ct).ConfigureAwait(false);
                sourceOverride = vss.SnapshotSourcePath;
                progress?.Report(string.Format(CoreLoc.S("Vss_Created"),
                    VssPathMapper.GetVolumeRoot(job.Source)));
            }
            catch (VssUnavailableException ex)
            {
                progress?.Report(string.Format(CoreLoc.S("Vss_Unavailable"), ex.Message));
            }
        }
```

Poi avvolgi il `try/finally` esistente: nel `finally` (dove c'è `_credentials.Disconnect`) aggiungi PRIMA della disconnessione:

```csharp
            if (vss is not null)
            {
                await vss.DisposeAsync().ConfigureAwait(false);
                progress?.Report(CoreLoc.S("Vss_Released"));
            }
```

E propaga `sourceOverride` alle tre chiamate di esecuzione dentro `RunJobAsync`:
- `_snapshots.RunVersionedAsync(job, progress, ct)` → `_snapshots.RunVersionedAsync(job, progress, ct, sourceOverride)`
- entrambe le `_runner.RunAsync(job, dryRun, progress, ct)` → `_runner.RunAsync(job, dryRun, progress, ct, sourceOverride: sourceOverride)`

- [ ] **Step 4: Cablaggio in `AppHost.BuildRunner`**

```csharp
        return new BackupRunner(Config, runner, log, email, Credentials, Results, snapshots, LockFolder,
            Path.Combine(Store.DirectoryPath, "vss"));
```

- [ ] **Step 5: Verifica**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj`
Expected: PASS, nessuna regressione.

- [ ] **Step 6: Commit**

```powershell
git add src/RoboKeep.Core/CoreLoc.cs src/RoboKeep.Core/Services/BackupRunner.cs src/RoboKeep/AppHost.cs
git commit -m @'
feat(v1.3): BackupRunner apre la sessione VSS e degrada con avviso se non disponibile

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 10: Idoneità VSS nel pre-check

**Files:**
- Create: `src/RoboKeep.Core/Services/VssEligibility.cs`
- Modify: `src/RoboKeep.Core/Models/PreflightModels.cs`
- Modify: `src/RoboKeep.Core/Services/PreflightChecker.cs`
- Modify: `src/RoboKeep.Core/Services/PreflightCollector.cs`
- Test: `src/RoboKeep.Tests/PreflightCheckerTests.cs` (aggiunta)

- [ ] **Step 1: Scrivi i test che falliscono**

Aggiungi in `PreflightCheckerTests.cs` (stile dei 3 test hard-link esistenti):

```csharp
    [Fact]
    public void Evaluate_VssSourceNotEligible_Warns()
    {
        var w = PreflightChecker.Evaluate(new PreflightInputs(true, long.MaxValue, 0, null,
            VssSourceEligible: false));
        Assert.Contains(w, x => x.MessageKey == "Preflight_VssNotEligible");
    }

    [Fact]
    public void Evaluate_VssSourceEligible_NoWarning()
    {
        var w = PreflightChecker.Evaluate(new PreflightInputs(true, long.MaxValue, 0, null,
            VssSourceEligible: true));
        Assert.DoesNotContain(w, x => x.MessageKey == "Preflight_VssNotEligible");
    }

    [Fact]
    public void Evaluate_VssNotRelevant_NoWarning()
    {
        var w = PreflightChecker.Evaluate(new PreflightInputs(true, long.MaxValue, 0, null));
        Assert.DoesNotContain(w, x => x.MessageKey == "Preflight_VssNotEligible");
    }
```

- [ ] **Step 2: Verifica che falliscano**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter Vss`
Expected: FAIL di compilazione (`VssSourceEligible` non esiste).

- [ ] **Step 3: Estendi il record input**

In `PreflightModels.cs`:

```csharp
public sealed record PreflightInputs(
    bool DestinationReachable,
    long FreeBytes,
    long MinFreeBytes,
    long? SourceSizeBytes,
    bool? VersionedDestSupportsHardLinks = null,
    bool? VssSourceEligible = null);
```

- [ ] **Step 4: Estendi il checker**

In `PreflightChecker.Evaluate`, dopo il controllo hard-link:

```csharp
        if (i.VssSourceEligible == false)
            w.Add(new PreflightWarning(PreflightSeverity.Warning, "Preflight_VssNotEligible", ""));
```

- [ ] **Step 5: Crea `VssEligibility` e usala nel collector**

`src/RoboKeep.Core/Services/VssEligibility.cs`:

```csharp
namespace RoboKeep.Core.Services;

/// <summary>Controllo IO di idoneità VSS: volume locale con lettera e filesystem NTFS
/// (VSS non fotografa exFAT/FAT32 né percorsi di rete).</summary>
public static class VssEligibility
{
    public static bool IsEligible(string sourcePath)
    {
        var root = VssPathMapper.GetVolumeRoot(sourcePath);
        if (root is null) return false;
        try
        {
            var drive = new DriveInfo(root);
            return drive.IsReady
                && string.Equals(drive.DriveFormat, "NTFS", StringComparison.OrdinalIgnoreCase);
        }
        catch { return false; }
    }
}
```

In `PreflightCollector.Collect`, dopo la riga `bool? hardLinks = ...`:

```csharp
        // Solo per i job VSS: la sorgente deve stare su un volume NTFS locale.
        bool? vssEligible = job.UseVss ? VssEligibility.IsEligible(job.Source) : null;
        return new PreflightInputs(reachable, free, minFreeBytes, size, hardLinks, vssEligible);
```

(sostituendo il `return` esistente).

- [ ] **Step 6: Verifica che passino**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj`
Expected: PASS.

- [ ] **Step 7: Commit**

```powershell
git add src/RoboKeep.Core/Services/VssEligibility.cs src/RoboKeep.Core/Models/PreflightModels.cs src/RoboKeep.Core/Services/PreflightChecker.cs src/RoboKeep.Core/Services/PreflightCollector.cs src/RoboKeep.Tests/PreflightCheckerTests.cs
git commit -m @'
feat(v1.3): pre-check idoneita VSS della sorgente (NTFS locale)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 11: Editor job — casella VSS + stringhe UI

**Files:**
- Modify: `src/RoboKeep/ViewModels/JobEditorViewModel.cs` (dopo la proprietà `SnapshotMaxAgeDays`, riga ~204)
- Modify: `src/RoboKeep/JobEditorWindow.xaml` (nella `ui:Card` delle opzioni, dopo il blocco `Editor_LogAll`, riga ~84)
- Modify: `src/RoboKeep/Localization/Loc.cs` (5 blocchi lingua)

- [ ] **Step 1: Proprietà nel ViewModel**

```csharp
    /// <summary>Copia anche i file aperti creando uno snapshot VSS della sorgente.</summary>
    public bool UseVss
    {
        get => _job.UseVss;
        set { _job.UseVss = value; OnPropertyChanged(); }
    }
```

- [ ] **Step 2: Casella nello XAML**

Dopo lo `StackPanel` di `Editor_LogAll` (riga ~84), dentro la stessa `ui:Card`:

```xml
                        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                            <CheckBox Content="{l:Tr Editor_UseVss}" IsChecked="{Binding UseVss}"/>
                            <ctl:InfoHint Margin="6,0,0,0" Text="{l:Tr Editor_UseVssTip}"/>
                        </StackPanel>
```

- [ ] **Step 3: Stringhe in `Loc.cs` (tutte e 5 le lingue, vicino alle chiavi `Editor_LogAll*`)**

IT:
```csharp
        ["Editor_UseVss"] = "Copia anche i file aperti (VSS)",
        ["Editor_UseVssTip"] = "Prima della copia crea uno snapshot del volume sorgente: anche i file in uso (Outlook, database) vengono copiati integri. Richiede conferma amministratore (UAC) all'avvio del backup e sorgente su disco locale NTFS.",
```
EN:
```csharp
        ["Editor_UseVss"] = "Also copy open files (VSS)",
        ["Editor_UseVssTip"] = "Creates a snapshot of the source volume before copying: files in use (Outlook, databases) are copied intact. Requires administrator approval (UAC) when the backup starts and a local NTFS source drive.",
```
ES:
```csharp
        ["Editor_UseVss"] = "Copiar también los archivos abiertos (VSS)",
        ["Editor_UseVssTip"] = "Crea una instantánea del volumen de origen antes de copiar: los archivos en uso (Outlook, bases de datos) se copian íntegros. Requiere aprobación de administrador (UAC) al iniciar la copia y origen en disco local NTFS.",
```
FR:
```csharp
        ["Editor_UseVss"] = "Copier aussi les fichiers ouverts (VSS)",
        ["Editor_UseVssTip"] = "Crée un instantané du volume source avant la copie : les fichiers en cours d'utilisation (Outlook, bases de données) sont copiés intacts. Nécessite l'approbation administrateur (UAC) au démarrage de la sauvegarde et une source sur disque local NTFS.",
```
DE:
```csharp
        ["Editor_UseVss"] = "Auch geöffnete Dateien kopieren (VSS)",
        ["Editor_UseVssTip"] = "Erstellt vor dem Kopieren einen Snapshot des Quellvolumes: auch in Benutzung befindliche Dateien (Outlook, Datenbanken) werden vollständig kopiert. Erfordert Administratorbestätigung (UAC) beim Start des Backups und eine Quelle auf einem lokalen NTFS-Laufwerk.",
```

E la chiave pre-check (vicino a `Preflight_NoHardLink`, 5 lingue):

IT: `["Preflight_VssNotEligible"] = "non è possibile copiare i file aperti: la sorgente non è su un volume locale NTFS (il backup salterà i file in uso)",`
EN: `["Preflight_VssNotEligible"] = "open files cannot be copied: the source is not on a local NTFS volume (files in use will be skipped)",`
ES: `["Preflight_VssNotEligible"] = "no se pueden copiar los archivos abiertos: el origen no está en un volumen NTFS local (los archivos en uso se omitirán)",`
FR: `["Preflight_VssNotEligible"] = "impossible de copier les fichiers ouverts : la source n'est pas sur un volume NTFS local (les fichiers en cours d'utilisation seront ignorés)",`
DE: `["Preflight_VssNotEligible"] = "geöffnete Dateien können nicht kopiert werden: die Quelle liegt nicht auf einem lokalen NTFS-Volume (Dateien in Benutzung werden übersprungen)",`

- [ ] **Step 4: Avviso al salvataggio se la sorgente non è idonea**

In `src/RoboKeep/JobEditorWindow.xaml.cs`, metodo `OnSave`, dopo il controllo esistente
Versioned/HardLink (cerca `HardLinkSupport`), aggiungi:

```csharp
        // VSS richiede sorgente su volume NTFS locale: avvisa subito, non solo al run.
        // Solo informativo: il salvataggio prosegue (al run scatterà il fallback con avviso).
        if (_vm.Job.UseVss && !VssEligibility.IsEligible(_vm.Job.Source))
        {
            System.Windows.MessageBox.Show(
                Loc.Instance["Preflight_VssNotEligible"],
                _vm.Job.Name,
                System.Windows.MessageBoxButton.OK,
                System.Windows.MessageBoxImage.Warning);
        }
```

(`VssEligibility` sta in `RoboKeep.Core.Services`, già negli using se c'è `HardLinkSupport`;
la chiave `Preflight_VssNotEligible` è definita allo Step 3.)

NB: questo controllo fa IO su `DriveInfo` ma è istantaneo (non enumera file), quindi può
stare sul thread UI come le validazioni esistenti.

- [ ] **Step 5: Build dell'app (chiedi prima di chiudere RoboKeep se aperto)**

Run: `dotnet build src/RoboKeep/RoboKeep.csproj -c Debug -v quiet`
Expected: 0 errori.

- [ ] **Step 6: Commit**

```powershell
git add src/RoboKeep/ViewModels/JobEditorViewModel.cs src/RoboKeep/JobEditorWindow.xaml src/RoboKeep/JobEditorWindow.xaml.cs src/RoboKeep/Localization/Loc.cs
git commit -m @'
feat(v1.3): editor job - casella Copia anche i file aperti (VSS)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 12: Wizard — domanda file aperti

**Files:**
- Modify: `src/RoboKeep.Core/Models/JobWizardAnswers.cs`
- Modify: `src/RoboKeep.Core/Services/JobWizardPlanner.cs`
- Modify: `src/RoboKeep/ViewModels/JobWizardViewModel.cs` (passo 4, dopo `ExcludeCommonTemp`)
- Modify: `src/RoboKeep/JobWizardWindow.xaml` (TabItem passo 4, righe ~121-135)
- Modify: `src/RoboKeep/Localization/Loc.cs` (5 lingue, vicino alle chiavi `Wiz_*`)
- Test: `src/RoboKeep.Tests/JobWizardPlannerTests.cs` (aggiunta)

- [ ] **Step 1: Scrivi i test che falliscono**

```csharp
    [Fact]
    public void BuildJob_HasOpenFiles_SetsUseVss()
    {
        var job = JobWizardPlanner.BuildJob(new JobWizardAnswers
        {
            Name = "n", Source = @"C:\s", Destination = @"E:\d", HasOpenFiles = true,
        });
        Assert.True(job.UseVss);
    }

    [Fact]
    public void BuildJob_NoOpenFiles_UseVssFalse()
    {
        var job = JobWizardPlanner.BuildJob(new JobWizardAnswers
        {
            Name = "n", Source = @"C:\s", Destination = @"E:\d",
        });
        Assert.False(job.UseVss);
    }
```

- [ ] **Step 2: Verifica che falliscano**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter OpenFiles`
Expected: FAIL di compilazione.

- [ ] **Step 3: Estendi answers e planner**

In `JobWizardAnswers.cs`, dopo `ExcludeCommonTemp`:

```csharp
    public bool HasOpenFiles { get; set; }
```

In `JobWizardPlanner.BuildJob`, nell'inizializzatore del job dopo `Enabled = true,`:

```csharp
            UseVss = a.HasOpenFiles,
```

- [ ] **Step 4: Verifica che passino**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj --filter OpenFiles`
Expected: PASS.

- [ ] **Step 5: ViewModel del wizard**

In `JobWizardViewModel.cs`, dopo la proprietà `ExcludeCommonTemp` (riga ~92):

```csharp
    public bool HasOpenFiles
    {
        get => _a.HasOpenFiles;
        set { _a.HasOpenFiles = value; OnPropertyChanged(); RaisePreview(); }
    }
```

E in `BuildResult()`, nell'inizializzatore di `answers` dopo `ExcludeCommonTemp = _a.ExcludeCommonTemp,`:

```csharp
            HasOpenFiles = _a.HasOpenFiles,
```

- [ ] **Step 6: XAML passo 4**

In `JobWizardWindow.xaml`, dopo la `CheckBox` di `Wiz_Excl` (riga ~133):

```xml
                        <CheckBox Content="{l:Tr Wiz_OpenFiles}" IsChecked="{Binding HasOpenFiles}" Margin="0,14,0,0"/>
                        <TextBlock Style="{StaticResource Hint}" Text="{l:Tr Wiz_OpenFilesHint}"/>
```

- [ ] **Step 7: Stringhe in `Loc.cs` (5 lingue, accanto alle `Wiz_Excl*`)**

IT:
```csharp
        ["Wiz_OpenFiles"] = "Alcuni file restano aperti durante il backup (es. Outlook, database)?",
        ["Wiz_OpenFilesHint"] = "Consigliato se fai il backup mentre lavori: uno snapshot VSS congela il volume e copia anche i file in uso. Richiede conferma amministratore (UAC) all'avvio del backup.",
```
EN:
```csharp
        ["Wiz_OpenFiles"] = "Are some files open while the backup runs (e.g. Outlook, databases)?",
        ["Wiz_OpenFilesHint"] = "Recommended if you back up while working: a VSS snapshot freezes the volume and copies files in use too. Requires administrator approval (UAC) when the backup starts.",
```
ES:
```csharp
        ["Wiz_OpenFiles"] = "¿Algunos archivos permanecen abiertos durante la copia (p. ej. Outlook, bases de datos)?",
        ["Wiz_OpenFilesHint"] = "Recomendado si haces la copia mientras trabajas: una instantánea VSS congela el volumen y copia también los archivos en uso. Requiere aprobación de administrador (UAC) al iniciar la copia.",
```
FR:
```csharp
        ["Wiz_OpenFiles"] = "Des fichiers restent-ils ouverts pendant la sauvegarde (p. ex. Outlook, bases de données) ?",
        ["Wiz_OpenFilesHint"] = "Recommandé si vous sauvegardez en travaillant : un instantané VSS fige le volume et copie aussi les fichiers en cours d'utilisation. Nécessite l'approbation administrateur (UAC) au démarrage de la sauvegarde.",
```
DE:
```csharp
        ["Wiz_OpenFiles"] = "Bleiben Dateien während des Backups geöffnet (z. B. Outlook, Datenbanken)?",
        ["Wiz_OpenFilesHint"] = "Empfohlen, wenn Sie während der Arbeit sichern: ein VSS-Snapshot friert das Volume ein und kopiert auch Dateien in Benutzung. Erfordert Administratorbestätigung (UAC) beim Start des Backups.",
```

- [ ] **Step 8: Test completi + build app**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj`
Expected: PASS.
Run (con RoboKeep chiuso): `dotnet build src/RoboKeep/RoboKeep.csproj -c Debug -v quiet`
Expected: 0 errori.

- [ ] **Step 9: Commit**

```powershell
git add src/RoboKeep.Core/Models/JobWizardAnswers.cs src/RoboKeep.Core/Services/JobWizardPlanner.cs src/RoboKeep/ViewModels/JobWizardViewModel.cs src/RoboKeep/JobWizardWindow.xaml src/RoboKeep/Localization/Loc.cs src/RoboKeep.Tests/JobWizardPlannerTests.cs
git commit -m @'
feat(v1.3): wizard - domanda file aperti che attiva VSS

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 13: Verifica finale + smoke manuale (con l'utente)

- [ ] **Step 1: Suite completa**

Run: `dotnet test src/RoboKeep.Tests/RoboKeep.Tests.csproj`
Expected: PASS, tutti verdi.

- [ ] **Step 2: Build Debug (con RoboKeep chiuso — chiedi all'utente)**

Run: `dotnet build src/RoboKeep/RoboKeep.csproj -c Debug -v quiet`
Expected: 0 errori.

- [ ] **Step 3: Smoke manuale (guida l'utente, non eseguire tu)**

Dallo spec, sezione "Verifica end-to-end":
1. Job con VSS su cartella con un file tenuto aperto/bloccato → UAC → il file viene copiato.
2. UAC negato → il job completa in copia normale con l'avviso `[vss]` nel log.
3. Kill di RoboKeep durante un run VSS → al run successivo lo snapshot residuo viene pulito (controllare con `vssadmin list shadows` da prompt admin).
4. VSS + versioning insieme → snapshot datato corretto, file aperto presente.
5. Wizard: la nuova domanda appare al passo 4 e imposta la casella nell'editor.
6. Windows 11 Home: se `Win32_ShadowCopy.Create` non fosse supportato, verificare che l'avviso di fallback sia chiaro.

- [ ] **Step 4: Al termine dello smoke confermato dall'utente**: aggiornare `CHANGELOG.md` (sezione `[1.3.0]`), bump versione in `src/Directory.Build.props` a `1.3.0`, commit, tag e release come da prassi delle versioni precedenti (SOLO dopo conferma esplicita dell'utente).
