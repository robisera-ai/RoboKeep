<ui:FluentWindow x:Class="RoboKeep.ScheduledTasksWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:l="clr-namespace:RoboKeep.Localization"
        Title="{l:Tr Tasks_Title}" Width="820" Height="520" MinWidth="600" MinHeight="380"
        ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica"
        WindowCornerPreference="Round" WindowStartupLocation="CenterOwner">

    <ui:FluentWindow.Resources>
        <!-- Il legame col job e' la riga che decide se l'attivita' va tenuta: in giallo quando
             e' orfana, cosi' si vede a colpo d'occhio quale riga guardare. I colori vengono dal
             tema di WPF-UI e restano leggibili in chiaro e in scuro. -->
        <Style TargetType="TextBlock" x:Key="LinkText">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsOrphan}" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCautionBrush}"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="{l:Tr Tasks_Title}">
            <ui:TitleBar.Icon><ui:SymbolIcon Symbol="CalendarClock24"/></ui:TitleBar.Icon>
        </ui:TitleBar>

        <TextBlock Grid.Row="1" Text="{l:Tr Tasks_Intro}" TextWrapping="Wrap" Opacity="0.8"
                   Margin="16,4,16,8"/>

        <Grid Grid.Row="2" Margin="16,0,16,8">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,4,0">
                <ItemsControl ItemsSource="{Binding Rows}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ui:Card Margin="0,0,0,8" Padding="12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14"
                                                   TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding LinkText}" Style="{StaticResource LinkText}"
                                                   Margin="0,2,0,0"/>
                                        <TextBlock Text="{Binding NextRunText}" FontSize="12" Opacity="0.8"
                                                   TextWrapping="Wrap" Margin="0,4,0,0"/>
                                        <TextBlock Text="{Binding LastRunText}" FontSize="12" Opacity="0.8"
                                                   TextWrapping="Wrap"/>
                                        <!-- Il comando in monospazio: e' l'unico modo per vedere
                                             che un'attivita' lancia una copia diversa dell'app. -->
                                        <TextBlock Text="{Binding CommandText}" FontSize="11" Opacity="0.7"
                                                   FontFamily="Cascadia Mono, Consolas"
                                                   TextWrapping="Wrap" Margin="0,6,0,0"/>
                                    </StackPanel>
                                    <ui:Button Grid.Column="1" Icon="{ui:SymbolIcon Delete24}"
                                               Content="{l:Tr Tasks_Delete}" Click="OnDelete"
                                               VerticalAlignment="Top" Margin="12,0,0,0"
                                               IsEnabled="{Binding DataContext.CanAct,
                                                   RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}"/>
                                </Grid>
                            </ui:Card>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Stato vuoto: senza righe la finestra direbbe niente, e «niente» si confonde
                 con «non ha funzionato». -->
            <TextBlock Text="{l:Tr Tasks_Empty}" HorizontalAlignment="Center" VerticalAlignment="Center"
                       Opacity="0.7" TextWrapping="Wrap"
                       Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVis}}"/>
        </Grid>

        <Grid Grid.Row="3" Margin="16,0,16,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding Status}" TextWrapping="Wrap"
                       Foreground="{DynamicResource SystemFillColorCautionBrush}"
                       VerticalAlignment="Center" Margin="0,0,12,0"/>
            <!-- Chiudi resta sempre attivo: bloccare l'uscita durante una lettura sarebbe
                 una trappola. Aggiorna si spegne finche' l'elenco non e' pronto. Nessun
                 collegamento all'Utilita' di pianificazione: cancellare o ritoccare li'
                 disallinea job e attivita', e qui c'e' gia' tutto cio' che serve per decidere. -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ui:Button Icon="{ui:SymbolIcon ArrowSync24}" Content="{l:Tr Tasks_Refresh}"
                           Click="OnRefresh" Margin="0,0,8,0" IsEnabled="{Binding CanAct}"/>
                <ui:Button Content="{l:Tr Common_Close}" Appearance="Primary" Click="OnClose"/>
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
