<ui:FluentWindow x:Class="RobocopySW.JobEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:ctl="clr-namespace:RobocopySW.Controls"
        mc:Ignorable="d"
        Title="Modifica job" Height="740" Width="740"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        WindowStartupLocation="CenterOwner">
    <ui:FluentWindow.Resources>
        <Style TargetType="TextBlock" x:Key="Label">
            <Setter Property="Margin" Value="0,12,8,4"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="Hint">
            <Setter Property="Opacity" Value="0.7"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Modifica job">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="Edit24"/>
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="20,4,8,0" Padding="0,0,16,0">
            <StackPanel>
                <TextBlock Text="Nome" Style="{StaticResource Label}"/>
                <ui:TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="es. Documenti"/>

                <TextBlock Text="Sorgente" Style="{StaticResource Label}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ui:TextBox Grid.Column="0" Text="{Binding Source, UpdateSourceTrigger=PropertyChanged}"/>
                    <ui:Button Grid.Column="1" Content="Sfoglia…" Icon="{ui:SymbolIcon FolderOpen24}" Margin="8,0,0,0" Click="OnBrowseSource"/>
                </Grid>

                <TextBlock Text="Destinazione" Style="{StaticResource Label}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ui:TextBox Grid.Column="0" Text="{Binding Destination, UpdateSourceTrigger=PropertyChanged}"/>
                    <ui:Button Grid.Column="1" Content="Sfoglia…" Icon="{ui:SymbolIcon FolderOpen24}" Margin="8,0,0,0" Click="OnBrowseDest"/>
                </Grid>

                <ui:Card Margin="0,16,0,0" Padding="14">
                    <StackPanel>
                        <CheckBox Content="Mirror — rende la destinazione identica alla sorgente (CANCELLA i file rimossi)"
                                  IsChecked="{Binding Mirror}"/>
                        <TextBlock Style="{StaticResource Hint}"
                                   Text="Disattivato = copia e aggiorna soltanto, senza mai cancellare in destinazione."/>
                        <CheckBox Content="Non sovrascrivere i file più recenti in destinazione (/XO)" IsChecked="{Binding ExcludeOlder}" Margin="0,6,0,0"/>
                        <CheckBox Content="Copia anche ACL/owner (/COPYALL — utile su share di rete)" IsChecked="{Binding CopyAll}" Margin="0,6,0,0"/>
                        <CheckBox Content="Job attivo (incluso in «Avvia tutti»)" IsChecked="{Binding Enabled}" Margin="0,6,0,0"/>
                    </StackPanel>
                </ui:Card>

                <Grid Margin="0,4,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Thread (/MT)" Style="{StaticResource Label}"/>
                            <ctl:InfoHint Margin="0,12,0,0" Text="Numero di file copiati in parallelo (multi-thread). Più alto = più veloce, soprattutto con tanti file piccoli. 8 va bene quasi sempre; 16+ su SSD o share di rete veloci; su hard disk meccanici meglio non esagerare. 0 disattiva il multi-thread."/>
                        </StackPanel>
                        <ui:TextBox Text="{Binding MultiThread, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Tentativi (/R)" Style="{StaticResource Label}"/>
                            <ctl:InfoHint Margin="0,12,0,0" Text="Quante volte riprovare a copiare un file se la copia fallisce (es. file temporaneamente in uso o rete instabile). Con file spesso aperti tieni un valore basso (1) per non aspettare a lungo."/>
                        </StackPanel>
                        <ui:TextBox Text="{Binding Retries, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Attesa sec (/W)" Style="{StaticResource Label}"/>
                            <ctl:InfoHint Margin="0,12,0,0" Text="Secondi di attesa tra un tentativo e l'altro. Si combina con i Tentativi (es. 1 tentativo × 5 sec). Valori alti allungano molto i tempi se ci sono file problematici."/>
                        </StackPanel>
                        <ui:TextBox Text="{Binding Wait, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </Grid>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Credenziale di rete (per share UNC)" Style="{StaticResource Label}"/>
                    <ctl:InfoHint Margin="0,12,0,0" Text="Serve solo se sorgente o destinazione è una cartella di rete condivisa (\\server\condivisione) che richiede utente e password. Le credenziali si creano in Impostazioni → Credenziali. Per dischi locali o USB lascia «nessuna»."/>
                </StackPanel>
                <ComboBox ItemsSource="{Binding Credentials}" SelectedItem="{Binding SelectedCredential}"
                          DisplayMemberPath="Display"/>

                <Grid Margin="0,4,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Escludi file (uno per riga)" Style="{StaticResource Label}"/>
                            <ctl:InfoHint Margin="0,12,0,0" Text="Schemi di file da NON copiare, uno per riga. Esempi: *.tmp (tutti i .tmp), ~$* (temporanei di Office), *.log. Lascia vuoto per copiare tutto."/>
                        </StackPanel>
                        <ui:TextBox Text="{Binding ExcludeFilesText, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True" Height="76" TextWrapping="NoWrap"
                                 VerticalScrollBarVisibility="Auto" PlaceholderText="*.tmp"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Escludi cartelle (una per riga)" Style="{StaticResource Label}"/>
                            <ctl:InfoHint Margin="0,12,0,0" Text="Nomi di cartelle da saltare, una per riga. Esempi: cache, node_modules, Temp. Vengono escluse ovunque compaiano nell'albero."/>
                        </StackPanel>
                        <ui:TextBox Text="{Binding ExcludeDirsText, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True" Height="76" TextWrapping="NoWrap"
                                 VerticalScrollBarVisibility="Auto" PlaceholderText="cache"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Anteprima comando robocopy" Style="{StaticResource Label}"/>
                <Border CornerRadius="6" Background="#1E1E1E" Padding="10" Margin="0,0,0,12">
                    <TextBox Text="{Binding CommandPreview, Mode=OneWay}" IsReadOnly="True" Style="{x:Null}"
                             FontFamily="Cascadia Mono, Consolas" FontSize="12" TextWrapping="Wrap"
                             Background="#1E1E1E" Foreground="#9CDCFE" CaretBrush="#9CDCFE"
                             BorderThickness="0" MinHeight="48"/>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="2" Padding="20,12" Background="#11888888">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="Annulla" Width="110" Margin="0,0,8,0" IsCancel="True" Click="OnCancel"/>
                <ui:Button Content="Salva" Width="110" Appearance="Primary" Icon="{ui:SymbolIcon Save24}" IsDefault="True" Click="OnSave"/>
            </StackPanel>
        </Border>
    </Grid>
</ui:FluentWindow>
