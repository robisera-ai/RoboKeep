# v1.8 — Correzioni post-1.7 e disco a riposo persistente

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** chiudere ciò che la 1.7 ha lasciato incoerente (email, icone, esempio di config, guida, documento di analisi, test intermittente) e rendere persistente tra le sessioni il "disco a riposo" dopo un errore hardware, con un modo esplicito per riabilitarlo.

**Architecture:** tutto resta nel Core come funzioni pure + un piccolo store JSON (`FaultedDiskStore`), sul modello di `LastResultStore`. La UI legge lo stato dallo store e lo mostra con un nuovo valore di `BackupHealth`; il runner lo consulta prima di toccare un disco. Nessuna nuova dipendenza.

**Tech Stack:** .NET 10, WPF + WPF-UI 4.3, xUnit. Test: `dotnet test src/RoboKeep.Tests --nologo` (aggiungere `-c Release` prima di un tag).

**Fuori da questo piano** (meritano piani propri, dopo): controllo aggiornamenti opzionale via GitHub Releases; lettura SMART reale a richiesta tramite helper elevato; anteprima `/MT` nell'editor allineata al tetto runtime.

**Regola di sicurezza del repo:** commit solo dopo che l'utente ha confermato che funziona; niente virgolette doppie nei messaggi di commit passati via PowerShell; chiedere di chiudere RoboKeep se la build fallisce per DLL bloccata, mai terminarlo.

---

## Mappa dei file

| File | Ruolo |
|---|---|
| `src/RoboKeep.Tests/CultureCollection.cs` (nuovo) | collection xUnit non parallela per i test che cambiano la lingua |
| `src/RoboKeep.Tests/RobocopyRunnerIntegrationTests.cs`, `ExitCodeInterpreterTests.cs` | entrano nella collection |
| `docs/guide/it/08-verifica-integrita.md`, `docs/guide/en/08-integrity-verification.md` | residuo "log nell'app" |
| `config/config.example.json` | esempio completo con tutti i campi |
| `src/RoboKeep.Core/Models/JobResult.cs` | `HealthWarnings` |
| `src/RoboKeep.Core/Services/EmailService.cs` | oggetto/corpo distinti per errore hardware; funzioni pure testabili |
| `src/RoboKeep.Tests/EmailServiceTests.cs` (nuovo) | test di oggetto e corpo |
| `src/RoboKeep.Core/Models/JobLastResult.cs` | `HardwareError`, `HardwareErrorDetail` |
| `src/RoboKeep.Core/Models/JobHealth.cs` | `BackupHealth.HardwareError` |
| `src/RoboKeep.Core/Services/StaleBackupEvaluator.cs` | classifica l'errore hardware |
| `src/RoboKeep.Core/Services/FaultedDiskStore.cs` (nuovo) | dischi a riposo, persistiti con scadenza |
| `src/RoboKeep.Tests/FaultedDiskStoreTests.cs` (nuovo) | |
| `src/RoboKeep.Core/Services/BackupRunner.cs` | consulta e aggiorna lo store; persiste l'esito hardware |
| `src/RoboKeep/AppHost.cs` | crea lo store e lo passa al runner |
| `src/RoboKeep/ViewModels/MainViewModel.cs`, `MainWindow.xaml`, `MainWindow.xaml.cs` | icona, tooltip, banner, pulsante "Riabilita dischi" |
| `src/RoboKeep/Localization/Loc.cs`, `src/RoboKeep.Core/CoreLoc.cs` | testi in 5 lingue |
| `ANALISI.md` | tappe v1.5, v1.6, v1.7 |
| `CHANGELOG.md` | sezione Unreleased |

---

### Task 1: Isolare i test che cambiano la lingua del processo

Perché: `BackupRunner_AppendsItalianRecapToLog` imposta `DefaultThreadCurrentUICulture` per tutto il processo mentre gli altri test girano in parallelo. Ha fatto fallire il workflow di release v1.7.0. Una collection con `DisableParallelization = true` fa girare quei test da soli.

**Files:**
- Create: `src/RoboKeep.Tests/CultureCollection.cs`
- Modify: `src/RoboKeep.Tests/RobocopyRunnerIntegrationTests.cs:11`
- Modify: `src/RoboKeep.Tests/ExitCodeInterpreterTests.cs` (riga della dichiarazione di classe)

- [ ] **Step 1: Creare la collection**

```csharp
namespace RoboKeep.Tests;

/// <summary>
/// I test di questa collection cambiano la lingua del PROCESSO (DefaultThreadCurrentUICulture)
/// o del thread: girano da soli, mai in parallelo con gli altri. Senza, un test che asserisce
/// su un testo localizzato puo' vedere la lingua sbagliata a caso (release v1.7.0 fallita in CI).
/// </summary>
[CollectionDefinition(Name, DisableParallelization = true)]
public sealed class CultureCollection
{
    public const string Name = "Cultura";
}
```

- [ ] **Step 2: Assegnare le due classi alla collection**

In `RobocopyRunnerIntegrationTests.cs`, sopra `public sealed class RobocopyRunnerIntegrationTests`:

```csharp
[Collection(CultureCollection.Name)]
```

Stessa riga sopra la classe in `ExitCodeInterpreterTests.cs`.

- [ ] **Step 3: Eseguire la suite tre volte**

Run: `cd src && for i in 1 2 3; do dotnet test RoboKeep.Tests --nologo 2>&1 | grep -E "Superat|Non superat"; done`
Expected: tre righe `Superato! ... Non superati: 0` con lo stesso totale di prima (365).

- [ ] **Step 4: Commit**

```
test: i test che cambiano la lingua girano in una collection non parallela
```

---

### Task 2: Residui nella guida e config di esempio

**Files:**
- Modify: `docs/guide/it/08-verifica-integrita.md:42-43`
- Modify: `docs/guide/en/08-integrity-verification.md:42-43`
- Modify: `config/config.example.json` (riscrivere)

- [ ] **Step 1: Guida italiana** — sostituire il blocco citazione in fondo con:

```markdown
> Ogni verifica finisce in *cronologia*, con esito e durata e con il suo log: un doppio clic lo
> apre nel Blocco note.
```

- [ ] **Step 2: Guida inglese**

```markdown
> Every verification lands in the *history*, with outcome, duration and its own log: a
> double-click opens it in Notepad.
```

- [ ] **Step 3: config.example.json** — sostituire il file con:

```json
{
  "settings": {
    "logRoot": "",
    "tempRoot": "",
    "compressLogs": true,
    "logRetentionDays": 30,
    "credentialScope": "Machine",
    "staleAfterDays": 7,
    "minFreeSpaceMb": 1024,
    "preflightEnabled": true,
    "notificationsEnabled": true,
    "email": {
      "enabled": false,
      "smtpHost": "smtp.example.com",
      "smtpPort": 587,
      "useSsl": true,
      "from": "backup@example.com",
      "to": "admin@example.com",
      "username": "backup@example.com",
      "passwordProtected": null,
      "onlyOnError": true
    }
  },
  "credentials": [
    {
      "id": "nas-ufficio",
      "host": "\\\\nas01\\backup$",
      "user": "DOMINIO\\backup",
      "passwordProtected": "<impostata-dalla-GUI-cifrata-DPAPI>"
    }
  ],
  "jobs": [
    {
      "name": "Documenti",
      "source": "D:\\documenti",
      "destination": "E:\\Backup\\Documenti",
      "mirror": true,
      "excludeOlder": false,
      "copyAll": false,
      "multiThread": 8,
      "unbufferedIO": false,
      "restartable": false,
      "logAllFiles": false,
      "excludeFiles": [ "*.tmp", "~$*" ],
      "excludeDirs": [ "cache" ],
      "forceCopyFiles": [],
      "forceCopySmart": false,
      "retries": 1,
      "wait": 5,
      "enabled": true,
      "credentialId": null,
      "versioned": true,
      "snapshotKeepCount": 30,
      "snapshotMaxAgeDays": 0,
      "useVss": false,
      "schedule": "Daily",
      "scheduleTime": "21:00",
      "scheduleWeekDay": "Monday",
      "scheduleMonthDay": 1,
      "verifyAfterRun": true,
      "verifyEveryDays": 7,
      "interPacketGapMs": 0,
      "destinationVolumeId": null,
      "destinationVolumeLabel": null
    },
    {
      "name": "Cartella-di-rete",
      "source": "\\\\nas01\\backup$\\Reparti\\Acquisti",
      "destination": "\\\\srv-backup\\backup$\\Reparti\\Acquisti",
      "mirror": true,
      "copyAll": true,
      "multiThread": 16,
      "retries": 3,
      "wait": 15,
      "enabled": true,
      "credentialId": "nas-ufficio",
      "interPacketGapMs": 25
    }
  ]
}
```

Nota per chi esegue: `logRoot`/`tempRoot` vuoti = cartella dati; `snapshotKeepCount: 0` = illimitato; `verifyEveryDays: 0` = a ogni backup; `destinationVolumeId` si compila dall'editor con "Proteggi con questo disco". Verificare i nomi dei campi di `AppSettings` con `grep -n "public" src/RoboKeep.Core/Models/AppSettings.cs` e togliere quelli che non esistono.

- [ ] **Step 4: Verificare che l'esempio si carichi**

Run: `cd src && dotnet run --project RoboKeep -- --config ..\config\config.example.json --job Documenti --dry-run`
Expected: parte l'anteprima (fallisce su `D:\documenti` inesistente, ma senza eccezioni di parsing JSON).

- [ ] **Step 5: Commit**

```
docs: guida cap. 08 e config di esempio allineati alla 1.7
```

---

### Task 3: Email che distingue l'errore hardware

**Files:**
- Modify: `src/RoboKeep.Core/Models/JobResult.cs`
- Modify: `src/RoboKeep.Core/Services/EmailService.cs`
- Modify: `src/RoboKeep.Core/Services/BackupRunner.cs` (dove costruisce `healthNotes`)
- Create: `src/RoboKeep.Tests/EmailServiceTests.cs`

- [ ] **Step 1: Test che fallisce**

```csharp
using RoboKeep.Core.Models;
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public class EmailServiceTests
{
    private static JobResult Hw() => new()
    {
        JobName = "Progetti", Success = false, ExitCode = 16, HardwareError = true,
        HardwareErrorDetail = @"Win32 23: Copia del file in corso D:\x\f.dat",
        Status = "INTERROTTO", StartedAt = new DateTime(2026, 9, 22, 21, 0, 0),
    };

    [Fact]
    public void Subject_SaysHardwareError_NotGenericError()
    {
        Assert.Contains("HARDWARE", EmailService.BuildSubject(Hw()).ToUpperInvariant());
        Assert.DoesNotContain("HARDWARE", EmailService.BuildSubject(new JobResult { JobName = "J", Success = false }).ToUpperInvariant());
    }

    [Fact]
    public void Body_LeadsWithDetailAndAdvice_ForHardwareError()
    {
        var body = EmailService.BuildBody(Hw());
        var detailAt = body.IndexOf(@"D:\x\f.dat", StringComparison.Ordinal);
        var countsAt = body.IndexOf(CoreLoc.S("Lbl_FilesCopied"), StringComparison.Ordinal);
        Assert.True(detailAt >= 0 && detailAt < countsAt); // il dettaglio viene PRIMA dei conteggi
        Assert.Contains("SMART", body);                     // il consiglio azionabile c'e'
    }

    [Fact]
    public void Body_IncludesHealthWarnings_WhenPresent()
    {
        var r = new JobResult { JobName = "J", Success = true };
        r.HealthWarnings.Add("[disco] ATTENZIONE: 7 blocchi danneggiati");
        Assert.Contains("7 blocchi danneggiati", EmailService.BuildBody(r));
    }
}
```

- [ ] **Step 2: Eseguire** — Run: `dotnet test src/RoboKeep.Tests --nologo --filter EmailServiceTests`
Expected: errori di compilazione (`BuildSubject`, `BuildBody`, `HealthWarnings` non esistono).

- [ ] **Step 3: `JobResult`** — aggiungere dopo `HardwareErrorDetail`:

```csharp
    /// <summary>Avvisi di salute del disco (registro eventi) emessi per questo run: finiscono
    /// anche nell'email, non solo nel log.</summary>
    public List<string> HealthWarnings { get; set; } = new();
```

- [ ] **Step 4: `EmailService`** — sostituire `SendResultAsync` dalla riga `var esito = ...` fino a `.ToString();` con:

```csharp
        var subject = BuildSubject(result);
        var body = BuildBody(result);
```

e aggiungere alla classe:

```csharp
    /// <summary>Oggetto: un errore HARDWARE va riconosciuto dall'anteprima sul telefono.</summary>
    public static string BuildSubject(JobResult result)
    {
        var esito = result.HardwareError ? CoreLoc.S("Email_HardwareError")
            : result.Success ? "OK" : CoreLoc.S("Lbl_Error");
        return $"[RoboKeep] {esito} - {result.JobName}";
    }

    public static string BuildBody(JobResult result)
    {
        var esito = result.HardwareError ? CoreLoc.S("Email_HardwareError")
            : result.Success ? "OK" : CoreLoc.S("Lbl_Error");
        var yesNo = result.DryRun ? CoreLoc.S("Email_Yes") : CoreLoc.S("Email_No");
        var sb = new StringBuilder();
        if (result.HardwareError)
        {
            // Prima di tutto: cosa e' successo e cosa fare. I conteggi qui non interessano a nessuno.
            sb.AppendLine(string.Format(CoreLoc.S("Hw_Stop"), result.HardwareErrorDetail))
              .AppendLine(CoreLoc.S("Hw_Advice"))
              .AppendLine();
        }
        sb.AppendLine($"Job: {result.JobName}")
          .AppendLine($"{CoreLoc.S("Lbl_Result")}: {esito} (exit {result.ExitCode}) - {result.Status}")
          .AppendLine($"{CoreLoc.S("Email_Preview")}: {yesNo}")
          .AppendLine($"{CoreLoc.S("Email_Start")}: {result.StartedAt:yyyy-MM-dd HH:mm:ss}  {CoreLoc.S("Lbl_Duration")}: {result.Duration:hh\\:mm\\:ss}")
          .AppendLine()
          .AppendLine($"{CoreLoc.S("Lbl_FoldersCopied"),-16}: {result.DirsCopied}")
          .AppendLine($"{CoreLoc.S("Lbl_FilesCopied"),-16}: {result.FilesCopied}")
          .AppendLine($"{CoreLoc.S("Lbl_FilesUnchanged"),-16}: {result.FilesSkipped}")
          .AppendLine($"{CoreLoc.S("Lbl_FilesExtra"),-16}: {result.FilesExtra}")
          .AppendLine($"{CoreLoc.S("Lbl_FilesFailed"),-16}: {result.FilesFailed}")
          .AppendLine($"{CoreLoc.S("Lbl_DirsFailed"),-16}: {result.DirsFailed}");
        if (result.HealthWarnings.Count > 0)
        {
            sb.AppendLine();
            foreach (var w in result.HealthWarnings) sb.AppendLine(w);
        }
        return sb.ToString();
    }
```

- [ ] **Step 5: `CoreLoc`** — aggiungere accanto a `Hw_Status`:

```csharp
        ["Email_HardwareError"] = L("ERRORE HARDWARE", "HARDWARE ERROR", "ERROR DE HARDWARE",
            "ERREUR MATÉRIELLE", "HARDWAREFEHLER"),
```

- [ ] **Step 6: `BackupRunner`** — subito dopo il ciclo che riempie `healthNotes` (prima di `using var lockHandle`), niente; invece nel punto in cui esiste `run.Result` (dopo il blocco `catch` che costruisce il fallback hardware, prima di `var recap = ...`) aggiungere:

```csharp
            run.Result.HealthWarnings.AddRange(healthNotes);
```

Nel ramo "disco già a riposo" (quello che ritorna presto con `Hw_SkippedAfterFault`) non serve: non si invia email per quel salto — **decisione:** inviarla invece sì, perché un backup pianificato non eseguito è una notizia. Aggiungere prima del `return` di quel ramo:

```csharp
            var skippedResult = new JobResult { /* come gia' costruito */ };
            try { await _email.SendResultAsync(_config.Settings.Email, skippedResult, null, ct).ConfigureAwait(false); }
            catch (Exception ex) { progress?.Report(string.Format(CoreLoc.S("Email_SendFailed"), ex.Message)); }
            return skippedResult;
```

(riscrivere il `return new JobResult {...}` esistente assegnandolo prima a `skippedResult`).

- [ ] **Step 7: Test verdi** — Run: `dotnet test src/RoboKeep.Tests --nologo`
Expected: `Superato!`, totale 368.

- [ ] **Step 8: Commit**

```
feat: email con oggetto e consiglio dedicati all errore hardware, avvisi disco nel corpo
```

---

### Task 4: `FaultedDiskStore` — dischi a riposo persistiti

**Files:**
- Create: `src/RoboKeep.Core/Services/FaultedDiskStore.cs`
- Create: `src/RoboKeep.Tests/FaultedDiskStoreTests.cs`

- [ ] **Step 1: Test che fallisce**

```csharp
using RoboKeep.Core.Services;

namespace RoboKeep.Tests;

public sealed class FaultedDiskStoreTests : IDisposable
{
    private readonly string _dir = Path.Combine(Path.GetTempPath(), "RbcFault_" + Guid.NewGuid().ToString("N"));
    private string PathOf => Path.Combine(_dir, "faulted-disks.json");
    public void Dispose() { if (Directory.Exists(_dir)) Directory.Delete(_dir, true); }

    private static FaultedDisk Disk(string id, DateTime since) =>
        new(id, "BACKUP1", @"E:\", since, "Win32 23: CRC");

    [Fact]
    public void Mark_ThenFind_AcrossInstances()
    {
        var now = new DateTime(2026, 9, 22, 21, 0, 0);
        new FaultedDiskStore(PathOf).Mark(Disk(@"\\?\Volume{a}\", now));

        var found = new FaultedDiskStore(PathOf).Find(@"\\?\Volume{a}\", now.AddDays(3));
        Assert.NotNull(found);
        Assert.Equal("BACKUP1", found!.Label);
        Assert.Null(new FaultedDiskStore(PathOf).Find(@"\\?\Volume{b}\", now));
    }

    [Fact]
    public void Expires_AfterExpiryDays()
    {
        var now = new DateTime(2026, 9, 22);
        var store = new FaultedDiskStore(PathOf);
        store.Mark(Disk(@"\\?\Volume{a}\", now));
        Assert.NotNull(store.Find(@"\\?\Volume{a}\", now.AddDays(FaultedDiskStore.ExpiryDays - 1)));
        Assert.Null(store.Find(@"\\?\Volume{a}\", now.AddDays(FaultedDiskStore.ExpiryDays + 1)));
    }

    [Fact]
    public void Clear_RemovesEverything()
    {
        var now = DateTime.Now;
        var store = new FaultedDiskStore(PathOf);
        store.Mark(Disk(@"\\?\Volume{a}\", now));
        store.Clear();
        Assert.Empty(store.Load(now));
    }

    [Fact]
    public void CorruptFile_IsTreatedAsEmpty()
    {
        Directory.CreateDirectory(_dir);
        File.WriteAllText(PathOf, "{ non json");
        Assert.Empty(new FaultedDiskStore(PathOf).Load(DateTime.Now));
    }
}
```

- [ ] **Step 2: Eseguire** — Expected: non compila (`FaultedDisk`, `FaultedDiskStore` mancanti).

- [ ] **Step 3: Implementazione**

```csharp
using System.Text.Json;

namespace RoboKeep.Core.Services;

/// <summary>Un disco messo a riposo dopo un errore hardware.</summary>
/// <param name="VolumeId">Identita' del volume (<c>\\?\Volume{GUID}\</c>): non la lettera, che cambia.</param>
/// <param name="Label">Etichetta, solo per i messaggi.</param>
/// <param name="Root">Radice al momento dell'errore (es. <c>E:\</c>), solo per i messaggi.</param>
/// <param name="Since">Quando.</param>
/// <param name="Detail">Codice e percorso dell'errore.</param>
public sealed record FaultedDisk(string VolumeId, string Label, string Root, DateTime Since, string Detail);

/// <summary>
/// Dischi a riposo, persistiti tra le sessioni: dopo un errore hardware il disco non va toccato
/// finche' qualcuno non l'ha controllato — nemmeno dall'attivita' pianificata della notte dopo,
/// che gira in un altro processo. Identificati per volume, cosi' la lettera che cambia non
/// libera un disco guasto ne' blocca uno sano. Scadono da soli dopo <see cref="ExpiryDays"/>
/// giorni: un blocco dimenticato non deve fermare i backup per sempre. Best-effort come
/// <see cref="LastResultStore"/>: un errore di lettura o scrittura non ferma mai un backup.
/// </summary>
public sealed class FaultedDiskStore
{
    public const int ExpiryDays = 7;
    private static readonly JsonSerializerOptions Options = new() { WriteIndented = true };
    private static readonly Mutex CrossProcess = new(false, @"Global\RoboKeep.FaultedDisks");
    private readonly string _path;

    public FaultedDiskStore(string path) => _path = path;

    /// <summary>Dischi ancora a riposo (quelli scaduti vengono scartati).</summary>
    public IReadOnlyList<FaultedDisk> Load(DateTime now) =>
        LoadRaw().Where(d => (now - d.Since).TotalDays < ExpiryDays).ToList();

    public FaultedDisk? Find(string? volumeId, DateTime now) =>
        string.IsNullOrEmpty(volumeId) ? null
            : Load(now).FirstOrDefault(d => string.Equals(d.VolumeId, volumeId, StringComparison.OrdinalIgnoreCase));

    /// <summary>Mette a riposo un disco (sostituisce una voce precedente dello stesso volume).</summary>
    public void Mark(FaultedDisk disk) => Locked(() =>
    {
        var all = LoadRaw().Where(d => !string.Equals(d.VolumeId, disk.VolumeId, StringComparison.OrdinalIgnoreCase)).ToList();
        all.Add(disk);
        Save(all);
    });

    /// <summary>Riabilita tutti i dischi: l'utente dichiara di averli controllati.</summary>
    public void Clear() => Locked(() => Save(new List<FaultedDisk>()));

    private void Locked(Action action)
    {
        try
        {
            var acquired = false;
            try
            {
                try { acquired = CrossProcess.WaitOne(TimeSpan.FromSeconds(3)); }
                catch (AbandonedMutexException) { acquired = true; }
                action();
            }
            finally { if (acquired) CrossProcess.ReleaseMutex(); }
        }
        catch { /* best-effort */ }
    }

    private void Save(List<FaultedDisk> all)
    {
        var dir = Path.GetDirectoryName(_path);
        if (!string.IsNullOrEmpty(dir)) Directory.CreateDirectory(dir);
        File.WriteAllText(_path, JsonSerializer.Serialize(all, Options));
    }

    private List<FaultedDisk> LoadRaw()
    {
        try
        {
            if (!File.Exists(_path)) return new();
            return JsonSerializer.Deserialize<List<FaultedDisk>>(File.ReadAllText(_path), Options) ?? new();
        }
        catch { return new(); }
    }
}
```

- [ ] **Step 4: Test verdi** — Run: `dotnet test src/RoboKeep.Tests --nologo --filter FaultedDiskStoreTests` → 4 superati.

- [ ] **Step 5: Commit**

```
feat: FaultedDiskStore - dischi a riposo persistiti per identita di volume, con scadenza
```

---

### Task 5: Il runner consulta e aggiorna lo store; l'esito hardware viene persistito

**Files:**
- Modify: `src/RoboKeep.Core/Models/JobLastResult.cs`
- Modify: `src/RoboKeep.Core/Services/BackupRunner.cs`
- Modify: `src/RoboKeep/AppHost.cs`
- Modify: `src/RoboKeep.Tests/HardwareSafetyTests.cs`

- [ ] **Step 1: Test che fallisce** — in `HardwareSafetyTests`, dopo `AfterHardwareError_NextJobOnSameDisk_IsNotRun`:

```csharp
    [Fact]
    public async Task HardwareError_RestsTheDisk_AcrossRunners_AndIsPersistedInLastResult()
    {
        var config = new AppConfig();
        config.Settings.LogRoot = Path.Combine(_root, "logs");
        config.Settings.TempRoot = Path.Combine(_root, "temp");
        var creds = new CredentialService(config.Settings.CredentialScope);
        var results = new LastResultStore(Path.Combine(_root, "lastresults.json"));
        var faulted = new FaultedDiskStore(Path.Combine(_root, "faulted-disks.json"));
        BackupRunner NewRunner() => new(config,
            new RobocopyRunner(FakeRobocopyWithCrcError(), detectMedia: _ => DiskMedia.Unknown),
            new LogService(config.Settings), new EmailService(creds), creds, results, faultedDisks: faulted);

        var first = await NewRunner().RunJobAsync(Job("Primo"));
        Assert.True(first.HardwareError);
        var saved = results.Load()["Primo"];
        Assert.True(saved.HardwareError);                       // la UI lo sapra' anche domani
        Assert.Contains("Win32 23", saved.HardwareErrorDetail);
        Assert.Single(faulted.Load(DateTime.Now));              // il volume della temp e' a riposo

        // Un runner NUOVO (= un altro processo, es. l'attivita' pianificata) non tocca il disco.
        var sw = System.Diagnostics.Stopwatch.StartNew();
        var second = await NewRunner().RunJobAsync(Job("Secondo"));
        Assert.True(second.HardwareError);
        Assert.True(sw.Elapsed < TimeSpan.FromSeconds(1));

        faulted.Clear();                                        // "ho controllato il disco"
        var third = await NewRunner().RunJobAsync(Job("Terzo")); // riparte (e rifallisce sul finto robocopy)
        Assert.True(sw.Elapsed > TimeSpan.FromSeconds(1));
    }
```

- [ ] **Step 2: `JobLastResult`** — aggiungere:

```csharp
    /// <summary>true se l'ultimo run e' stato interrotto da un errore hardware del disco: la
    /// UI lo mostra con un'icona dedicata e il consiglio di controllare il supporto.</summary>
    public bool HardwareError { get; set; }
    public string? HardwareErrorDetail { get; set; }
```

- [ ] **Step 3: `BackupRunner`** — costruttore: aggiungere il parametro `FaultedDiskStore? faultedDisks = null` e il campo `_faultedDisks`. Sostituire il calcolo di `faulted` all'inizio di `RunJobAsync` con:

```csharp
        // Disco a riposo: in questa sessione (set in memoria) o persistito (store, per identita'
        // di volume: anche l'attivita' pianificata di domani lo rispetta).
        var now0 = DateTime.Now;
        string? faulted = null;
        string? faultedSince = null;
        foreach (var root in new[] { job.Destination, job.Source }.Select(RootOf).OfType<string>())
        {
            if (_faultedRoots.Contains(root)) { faulted = root; break; }
            var persisted = _faultedDisks?.Find(VolumeIdentity.ForPath(root)?.VolumeId, now0);
            if (persisted is not null) { faulted = root; faultedSince = persisted.Since.ToString("d"); break; }
        }
        if (faulted is not null)
        {
            progress?.Report(faultedSince is null
                ? string.Format(CoreLoc.S("Hw_SkippedAfterFault"), faulted)
                : string.Format(CoreLoc.S("Hw_SkippedPersisted"), faulted, faultedSince, FaultedDiskStore.ExpiryDays));
            // ... resto del ramo invariato (history, email di Task 3, return), ma aggiungere
            // prima del return:
            if (!dryRun)
                _results?.Update(new JobLastResult { JobName = job.Name, Success = false,
                    ExitCode = RobocopyRunner.HardwareFailureExitCode, HardwareError = true,
                    HardwareErrorDetail = CoreLoc.S("Hw_Status"), FinishedAt = now0 });
```

Nel `_results?.Update(...)` principale aggiungere `HardwareError = run.Result.HardwareError, HardwareErrorDetail = run.Result.HardwareErrorDetail,`.

`MarkFaulted` diventa:

```csharp
    private void MarkFaulted(string? path, string detail)
    {
        if (RootOf(path) is not { } root) return;
        _faultedRoots.Add(root);
        var vol = VolumeIdentity.ForPath(root);
        if (vol is not null)
            _faultedDisks?.Mark(new FaultedDisk(vol.VolumeId, vol.Label, root, DateTime.Now, detail));
    }
```

e le due chiamate passano il dettaglio: `MarkFaulted(faultPath ?? job.Destination, run.Result.HardwareErrorDetail ?? "")` e, nella verifica, `MarkFaulted(..., ex.Message)`.

- [ ] **Step 4: `CoreLoc`** — accanto a `Hw_SkippedAfterFault`:

```csharp
        ["Hw_SkippedPersisted"] = L(
            "[disco] job NON eseguito: il disco {0} è a riposo dal {1} per un errore hardware. Controllalo, poi premi «Riabilita dischi» nella finestra principale (si riabilita da solo dopo {2} giorni).",
            "[disk] job NOT run: disk {0} has been resting since {1} after a hardware error. Check it, then click \"Re-enable disks\" in the main window (it re-enables itself after {2} days).",
            "[disco] trabajo NO ejecutado: el disco {0} está en reposo desde el {1} por un error de hardware. Compruébalo y pulsa «Rehabilitar discos» en la ventana principal (se rehabilita solo tras {2} días).",
            "[disque] tâche NON exécutée : le disque {0} est au repos depuis le {1} après une erreur matérielle. Vérifiez-le, puis cliquez sur « Réactiver les disques » dans la fenêtre principale (réactivation automatique après {2} jours).",
            "[Datenträger] Job NICHT ausgeführt: Datenträger {0} ruht seit {1} nach einem Hardwarefehler. Prüfen Sie ihn und klicken Sie im Hauptfenster auf „Datenträger freigeben“ (nach {2} Tagen automatisch)."),
```

- [ ] **Step 5: `AppHost`** — aggiungere la proprietà e il cablaggio:

```csharp
    public FaultedDiskStore FaultedDisks { get; }
    // nel costruttore:
    FaultedDisks = new FaultedDiskStore(Path.Combine(store.DirectoryPath, "faulted-disks.json"));
    // in BuildRunner, ultimo argomento:
    ..., History, faultedDisks: FaultedDisks);
```

- [ ] **Step 6: Test verdi** — Run: `dotnet test src/RoboKeep.Tests --nologo` → tutti superati (369).

- [ ] **Step 7: Commit**

```
feat: disco a riposo persistente tra le sessioni ed esito hardware salvato per la UI
```

---

### Task 6: Salute "errore hardware" nella UI + pulsante "Riabilita dischi"

**Files:**
- Modify: `src/RoboKeep.Core/Models/JobHealth.cs`
- Modify: `src/RoboKeep.Core/Services/StaleBackupEvaluator.cs:42`
- Modify: `src/RoboKeep.Tests/StaleBackupEvaluatorTests.cs`
- Modify: `src/RoboKeep/ViewModels/MainViewModel.cs:288-360`
- Modify: `src/RoboKeep/MainWindow.xaml:118-181` e barra pulsanti (riga ~71)
- Modify: `src/RoboKeep/MainWindow.xaml.cs`
- Modify: `src/RoboKeep/Localization/Loc.cs` (5 sezioni)

- [ ] **Step 1: Test che fallisce** — in `StaleBackupEvaluatorTests`:

```csharp
    [Fact]
    public void HardwareError_BeatsPlainFailure()
    {
        var results = new Dictionary<string, JobLastResult>
        {
            ["hw"] = new() { JobName = "hw", Success = false, HardwareError = true, FinishedAt = DateTime.Now },
            ["ko"] = new() { JobName = "ko", Success = false, FinishedAt = DateTime.Now },
        };
        var h = StaleBackupEvaluator.Evaluate(new[] { "hw", "ko" }, results, 7, DateTime.Now);
        Assert.Equal(BackupHealth.HardwareError, h[0].Health);
        Assert.Equal(BackupHealth.Failed, h[1].Health);
    }
```

- [ ] **Step 2: Enum ed evaluator**

`JobHealth.cs`, dopo `Interrupted`:

```csharp
    /// <summary>Ultimo run interrotto da un errore hardware del disco: non "riprova", controlla il supporto.</summary>
    HardwareError,
```

`StaleBackupEvaluator.cs`, riga 42:

```csharp
        if (!r.Success) return r.HardwareError ? BackupHealth.HardwareError : BackupHealth.Failed;
```

- [ ] **Step 3: Test verde** — Run: `dotnet test src/RoboKeep.Tests --nologo --filter StaleBackupEvaluatorTests`.

- [ ] **Step 4: Testi in 5 lingue** — in `Loc.cs`, accanto a `Health_Failed` in ogni sezione:

| lingua | `Health_Hardware` | `Health_CountHardware` | `Main_ReenableDisks` | `Main_ReenableDisksConfirm` |
|---|---|---|---|---|
| it | `Errore hardware del disco: controlla cavo, box e SMART prima di riprovare. {0}` | `{0} con errore hardware` | `Riabilita dischi` | `I dischi messi a riposo per un errore hardware torneranno utilizzabili. Farlo solo dopo aver controllato cavo, box, alimentazione e salute SMART. Continuare?` |
| en | `Disk hardware error: check cable, enclosure and SMART before retrying. {0}` | `{0} with hardware error` | `Re-enable disks` | `Disks rested after a hardware error will become usable again. Do this only after checking cable, enclosure, power and SMART health. Continue?` |
| es | `Error de hardware del disco: comprueba cable, caja y SMART antes de reintentar. {0}` | `{0} con error de hardware` | `Rehabilitar discos` | `Los discos en reposo por un error de hardware volverán a estar disponibles. Hazlo solo tras comprobar cable, caja, alimentación y salud SMART. ¿Continuar?` |
| fr | `Erreur matérielle du disque : vérifiez câble, boîtier et SMART avant de réessayer. {0}` | `{0} avec erreur matérielle` | `Réactiver les disques` | `Les disques mis au repos après une erreur matérielle redeviendront utilisables. Ne le faites qu'après avoir vérifié câble, boîtier, alimentation et état SMART. Continuer ?` |
| de | `Hardwarefehler des Datenträgers: Kabel, Gehäuse und SMART prüfen, bevor Sie es erneut versuchen. {0}` | `{0} mit Hardwarefehler` | `Datenträger freigeben` | `Nach einem Hardwarefehler ruhende Datenträger werden wieder nutzbar. Nur nach Prüfung von Kabel, Gehäuse, Stromversorgung und SMART-Zustand. Fortfahren?` |

- [ ] **Step 5: `MainViewModel.EvaluateHealth`** — aggiungere `int nHardware = 0;` e, prima del ramo `if (h == BackupHealth.Failed)`:

```csharp
            if (h == BackupHealth.HardwareError)
            {
                var detail = results.TryGetValue(jvm.Name, out var hr) ? hr.HardwareErrorDetail ?? "" : "";
                jvm.HealthTooltip = string.Format(Loc.Instance["Health_Hardware"], detail).Trim();
                nHardware++;
            }
            else if (h == BackupHealth.Failed)
```

e nel banner, prima di `nFailed`: `if (nHardware > 0) parts.Add(string.Format(Loc.Instance["Health_CountHardware"], nHardware));`

Aggiungere la proprietà osservabile:

```csharp
    private bool _hasFaultedDisks;
    public bool HasFaultedDisks { get => _hasFaultedDisks; private set { _hasFaultedDisks = value; OnPropertyChanged(); } }
```

e in fondo a `EvaluateHealth`: `HasFaultedDisks = _host.FaultedDisks.Load(DateTime.Now).Count > 0;`

- [ ] **Step 6: `MainWindow.xaml`** — trigger riga (dopo `Interrupted`, riga 126):

```xml
<DataTrigger Binding="{Binding Health}" Value="HardwareError">
    <Setter Property="Background" Value="#33B00020"/>
</DataTrigger>
```

trigger icona (dopo il blocco `Interrupted`, riga 176):

```xml
<DataTrigger Binding="{Binding Health}" Value="HardwareError">
    <Setter Property="Visibility" Value="Visible"/>
    <Setter Property="Foreground" Value="#B00020"/>
    <Setter Property="Text" Value="&#x26D4;"/>
</DataTrigger>
```

pulsante nella barra, dopo il pulsante Cronologia (riga 71):

```xml
<ui:Button Icon="{ui:SymbolIcon ArrowReset24}" Content="{l:Tr Main_ReenableDisks}" Click="OnReenableDisks"
           Appearance="Caution" Margin="0,0,8,0"
           Visibility="{Binding HasFaultedDisks, Converter={StaticResource BoolToVis}}"/>
```

Verificare che `ArrowReset24` esista: `grep -c ArrowReset24 ~/.nuget/packages/wpf-ui/4.3.0/lib/net9.0-windows7.0/Wpf.Ui.dll` deve dare 1 (un simbolo inesistente fa crashare la finestra a runtime, non in build).

- [ ] **Step 7: `MainWindow.xaml.cs`**

```csharp
    private void OnReenableDisks(object sender, RoutedEventArgs e)
    {
        var ok = System.Windows.MessageBox.Show(Loc.Instance["Main_ReenableDisksConfirm"],
            Loc.Instance["Main_ReenableDisks"], MessageBoxButton.YesNo, MessageBoxImage.Warning);
        if (ok != MessageBoxResult.Yes) return;
        _host.FaultedDisks.Clear();
        _vm.ReloadLastResults();
    }
```

- [ ] **Step 8: Build + prova manuale** — Run: `cd src && dotnet build RoboKeep.sln --nologo` → 0 avvisi, 0 errori. Prova: scrivere a mano in `%APPDATA%\RoboKeep\faulted-disks.json` una voce con il `VolumeId` di E: (`Get-Volume E | Select UniqueId`), aprire l'app: il pulsante "Riabilita dischi" compare; lanciare un job su E: → salta con il messaggio `Hw_SkippedPersisted`; premere il pulsante → il file si svuota, il pulsante sparisce, il job riparte.

- [ ] **Step 9: Commit** (dopo conferma dell'utente)

```
feat: icona e banner per errore hardware, pulsante Riabilita dischi
```

---

### Task 7: Guida, ANALISI.md e changelog

**Files:**
- Modify: `docs/guide/it/16-risoluzione-problemi.md`, `docs/guide/en/16-troubleshooting.md` (sezione errore hardware)
- Modify: `ANALISI.md` (dopo `### v1.4`)
- Modify: `CHANGELOG.md` (nuova sezione in cima)

- [ ] **Step 1: Guida** — nel paragrafo che inizia con "In questi casi RoboKeep **si ferma subito**", dopo "non vengono avviati", aggiungere:

IT: `Il disco resta **a riposo anche nei giorni seguenti** (anche per i backup pianificati) finché non lo riabiliti con il pulsante **Riabilita dischi** della finestra principale, che compare solo quando serve; per sicurezza si riabilita da solo dopo 7 giorni. L'email di esito ha oggetto «ERRORE HARDWARE» e riporta in cima cosa è successo e cosa controllare.`

EN: `The disk **stays rested on the following days too** (scheduled backups included) until you re-enable it with the **Re-enable disks** button in the main window, which appears only when needed; as a safety net it re-enables itself after 7 days. The result email is titled "HARDWARE ERROR" and leads with what happened and what to check.`

- [ ] **Step 2: ANALISI.md** — aggiungere dopo la sezione v1.4:

```markdown
### v1.5 — Rotazione dei dischi: l'identità, non la lettera

Due dischi esterni alternati prendono la stessa lettera. Un mirror sul disco sbagliato cancella
quello che trova. La difesa è identificare il disco per **volume GUID** (assegnato alla
formattazione) e saltare, senza toccare nulla, quando il disco nello slot non è quello del job.
"Saltato" è un terzo esito: né successo né errore, con la sua icona neutra e exit code 0 per
l'attività pianificata.

### v1.6 — Guida in-app

Diciassette capitoli in italiano e inglese, resi nativamente in WPF. Il versioning imparò a
saltare un file illeggibile nel vecchio snapshot: **scelta poi rovesciata** (v1.7).

### v1.7 — La salute del disco viene prima del backup

Nata da un incidente reale: un box USB che faceva sparire il disco a metà scrittura, 17 settori
scritti a metà, la `$Mft` rovinata — e RoboKeep che aggravava con 8 thread su un 5400 rpm, ore
di copia sopra centinaia di errori CRC e nessun avviso nel mese in cui il registro eventi di
Windows già lo diceva. Le decisioni:

- **Fermarsi al primo errore hardware** (Win32 23/27/1117) invece di aggirarlo: si uccide
  robocopy prima del retry; il clone hard-link non salta più il file. Continuare a scrivere su un
  disco che segnala errori fisici è la scelta sbagliata, sempre. Il disco resta a riposo per la
  sessione (persistente dalla v1.8).
- **Tenere sveglio il PC** con una power request (non `SetThreadExecutionState`, legata al
  thread: il codice async lo cambia).
- **Tetto `/MT:2` sui dischi meccanici**, riconosciuti via `IOCTL_STORAGE_QUERY_PROPERTY`
  (nessuna elevazione). Un bridge USB muto senza TRIM è trattato da HDD: limitare un SSD costa
  minuti, non limitare un HDD lo maltratta.
- **Niente snapshot se nulla è cambiato**: un'anteprima `/L` decide. Fatta **senza `/MT`**:
  in multi-thread robocopy conta come copiate tutte le cartelle e dà exit 0 anche con una
  cartella nuova.
- **Salute dal registro eventi**, non dallo SMART: lo SMART di un disco USB non si legge senza
  privilegi di amministratore, e Windows senza elevazione dichiara "Healthy" un disco con settori
  pendenti. Il registro Sistema conserva blocchi danneggiati, errori di I/O e scritture perse, e
  si legge da utente normale. Avvisa, non blocca: nomina i dischi per lettera e numero.
- **Forza copia con `/IM`**: i robocopy recenti classificano "modificato" un file riscritto con
  stessa data e dimensione e lo saltano nonostante `/IS /IT`. Nei job versionati i file da
  forzare vengono prima scollegati dallo snapshot: la passata sovrascrive sul posto.
- **Verifica periodica** (7 giorni) invece che a ogni backup, con un log a sé per ogni verifica.
- Lezione di processo: un test asseriva su testo localizzato mentre un altro cambiava la lingua
  del processo in parallelo; la release è fallita in CI al primo tentativo. Ora nessun codice
  riconosce righe dal loro testo, e i test che toccano la lingua girano in una collection isolata.
```

- [ ] **Step 3: CHANGELOG** — in cima, prima di `## [1.7.0]`:

```markdown
## [Unreleased]

### Changed
- **A disk rested after a hardware error stays rested across sessions** — scheduled backups
  included — until you click the new **Re-enable disks** button (shown only when needed) or 7
  days pass. Disks are tracked by volume identity, not letter.
- **Hardware errors have their own health state**: a dedicated icon and tooltip in the job list
  (with the error detail), their own count in the warning banner, and a result email titled
  "HARDWARE ERROR" that leads with what happened and what to check. Disk-health warnings from the
  event log are included in the email body too.

### Fixed
- Tests that change the process language now run in an isolated, non-parallel collection: the
  intermittent CI failure that broke the first v1.7.0 release attempt can't recur.
- Guide chapter 08 and `config/config.example.json` brought up to date (the example was missing
  every field added since 1.0).

### Docs
- `ANALISI.md` now covers v1.5–v1.7 and the engineering decisions behind the hardware-safety work.
```

- [ ] **Step 4: Commit**

```
docs: guida, ANALISI.md (v1.5-1.7) e changelog per la 1.8
```

---

### Task 8: Wizard semplificato — via il "tipo di dischi", dentro versioni e pianificazione

Perché: il passo 2 chiede all'utente se i dischi sono SSD/HDD/USB/rete solo per decidere i thread e i tentativi. Dalla 1.7 il tipo di disco lo rileva `StorageProbe` a runtime e il tetto scatta comunque; la rete si riconosce dal percorso (`VolumeIdentity.IsNetworkPath`). La domanda è diventata inutile e, se l'utente sbaglia, dannosa (16 thread dichiarando "SSD" su un HDD: il tetto rimedia, ma la risposta era una bugia). In compenso il wizard **non chiede** le due cose che contano di più per un backup: tenere le versioni e pianificarlo. Un job creato dal wizard oggi nasce senza versioni e senza orario.

**Files:**
- Modify: `src/RoboKeep.Core/Models/JobWizardAnswers.cs`
- Modify: `src/RoboKeep.Core/Services/JobWizardPlanner.cs`
- Modify: `src/RoboKeep.Tests/JobWizardPlannerTests.cs`
- Modify: `src/RoboKeep/ViewModels/JobWizardViewModel.cs`, `src/RoboKeep/JobWizardWindow.xaml`
- Modify: `src/RoboKeep/Localization/Loc.cs`, `docs/guide/it/03-primo-backup.md`, `docs/guide/en/03-first-backup.md`

- [ ] **Step 1: Test che fallisce** — in `JobWizardPlannerTests` (sostituire i test su `RecommendedThreads`/`StorageKind`, che spariscono):

```csharp
    [Fact]
    public void Threads_AreEightAndLeftToTheRuntimeCap()
    {
        // Il tipo di disco non si chiede piu': lo rileva StorageProbe a runtime e limita a 2 se
        // serve. Il wizard mette il valore buono per SSD/rete.
        var job = JobWizardPlanner.BuildJob(new JobWizardAnswers { Name = "j", Source = @"C:\s", Destination = @"E:\d" });
        Assert.Equal(8, job.MultiThread);
    }

    [Fact]
    public void NetworkPath_GetsMoreRetries_DetectedFromThePath()
    {
        var local = JobWizardPlanner.BuildJob(new JobWizardAnswers { Name = "j", Source = @"C:\s", Destination = @"E:\d" });
        var unc = JobWizardPlanner.BuildJob(new JobWizardAnswers { Name = "j", Source = @"C:\s", Destination = @"\\nas\share\d" });
        Assert.Equal((1, 5), (local.Retries, local.Wait));
        Assert.Equal((3, 10), (unc.Retries, unc.Wait));
    }

    [Fact]
    public void KeepVersions_TurnsOnVersioningWithDefaultRetention()
    {
        var job = JobWizardPlanner.BuildJob(new JobWizardAnswers { Name = "j", Source = @"C:\s", Destination = @"E:\d", KeepVersions = true });
        Assert.True(job.Versioned);
        Assert.Equal(BackupJob.DefaultSnapshotKeepCount, job.SnapshotKeepCount);
        Assert.False(JobWizardPlanner.BuildJob(new JobWizardAnswers { Name = "j", Source = @"C:\s", Destination = @"E:\d" }).Versioned);
    }

    [Fact]
    public void DailySchedule_IsAppliedWithTheGivenTime()
    {
        var job = JobWizardPlanner.BuildJob(new JobWizardAnswers { Name = "j", Source = @"C:\s", Destination = @"E:\d", ScheduleDaily = true, ScheduleTime = "22:30" });
        Assert.Equal(ScheduleKind.Daily, job.Schedule);
        Assert.Equal("22:30", job.ScheduleTime);
        Assert.Equal(ScheduleKind.None, JobWizardPlanner.BuildJob(new JobWizardAnswers { Name = "j", Source = @"C:\s", Destination = @"E:\d" }).Schedule);
    }
```

- [ ] **Step 2: `JobWizardAnswers`** — togliere `SourceStorage` e `DestStorage`; aggiungere:

```csharp
    /// <summary>Tenere le versioni datate (ritenzione di default).</summary>
    public bool KeepVersions { get; set; }
    /// <summary>Pianificazione giornaliera all'ora indicata.</summary>
    public bool ScheduleDaily { get; set; }
    public string ScheduleTime { get; set; } = "21:00";
```

- [ ] **Step 3: `JobWizardPlanner`** — togliere `RecommendedThreads` e `Rank`; in `BuildJob`:

```csharp
        // La rete si riconosce dal percorso, non da una domanda. Il tipo di disco non si chiede:
        // StorageProbe lo rileva a runtime e limita i thread sui dischi meccanici.
        var onNetwork = VolumeIdentity.IsNetworkPath(a.Source) || VolumeIdentity.IsNetworkPath(a.Destination);
        var job = new BackupJob
        {
            // ... come prima, ma:
            MultiThread = 8,
            Versioned = a.KeepVersions,
            Schedule = a.ScheduleDaily ? ScheduleKind.Daily : ScheduleKind.None,
            ScheduleTime = string.IsNullOrWhiteSpace(a.ScheduleTime) ? "21:00" : a.ScheduleTime.Trim(),
        };
```

Cancellare `src/RoboKeep.Core/Models/StorageKind.cs` se nessun altro lo usa (`grep -rn StorageKind src --include=*.cs`).

- [ ] **Step 4: Test verdi (Core)** — Run: `dotnet test src/RoboKeep.Tests --nologo --filter JobWizardPlannerTests`.

- [ ] **Step 5: ViewModel** — togliere `SourceStorageIndex`/`DestStorageIndex` e `ShowNetCredNote` basato su di essi; `ShowNetCredNote` diventa `VolumeIdentity.IsNetworkPath(_a.Source) || VolumeIdentity.IsNetworkPath(_a.Destination)`. Aggiungere le proprietà `KeepVersions`, `ScheduleDaily`, `ScheduleTime` (con `OnPropertyChanged` e `RaisePreview()`), e `ShowVersionsNote => KeepVersions && !HardLinkSupport.IsSupported(Destination)` (destinazione non NTFS locale: avvisa che le versioni non saranno possibili). Aggiornare `StepLabel`/`CanGoNext`/`ShowNext`/`ShowOpen` al nuovo numero di passi (4 invece di 5).

- [ ] **Step 6: XAML** — eliminare il `TabItem` "Passo 2: tipo dischi". Nel passo "comportamento" aggiungere sotto `Wiz_Large`:

```xml
<CheckBox Content="{l:Tr Wiz_KeepVersions}" IsChecked="{Binding KeepVersions}" Margin="0,18,0,0"/>
<TextBlock Style="{StaticResource Hint}" Text="{l:Tr Wiz_KeepVersionsHint}"/>
<ui:InfoBar Margin="0,6,0,0" Severity="Warning" IsClosable="False"
            IsOpen="{Binding ShowVersionsNote}" Message="{l:Tr Preflight_NoHardLink}"/>
<CheckBox Content="{l:Tr Wiz_ScheduleDaily}" IsChecked="{Binding ScheduleDaily}" Margin="0,18,0,0"/>
<StackPanel Orientation="Horizontal" Margin="24,6,0,0">
    <TextBlock Text="{l:Tr Sched_Time}" VerticalAlignment="Center" Margin="0,0,8,0"/>
    <ui:TextBox Text="{Binding ScheduleTime, UpdateSourceTrigger=PropertyChanged}" Width="90" IsEnabled="{Binding ScheduleDaily}"/>
</StackPanel>
```

- [ ] **Step 7: Testi (5 lingue)** — togliere `Wiz_Step2Title`, `Wiz_SourceKind`, `Wiz_DestKind`, `Wiz_Ssd`, `Wiz_Hdd`, `Wiz_Usb`, `Wiz_Network`, `Wiz_KindHint`; rinumerare `Wiz_Step3Title..5` → `2..4`; aggiungere:

| | `Wiz_KeepVersions` | `Wiz_KeepVersionsHint` | `Wiz_ScheduleDaily` |
|---|---|---|---|
| it | `Tieni le versioni precedenti (consigliato)` | `Ogni backup salva una versione datata; i file invariati sono condivisi, quindi costa solo ciò che cambia. Servono un disco NTFS locale e le ultime 30 versioni vengono conservate.` | `Esegui ogni giorno alle` |
| en | `Keep previous versions (recommended)` | `Every backup saves a dated version; unchanged files are shared, so it only costs what changed. Needs a local NTFS disk; the last 30 versions are kept.` | `Run every day at` |
| es | `Conservar versiones anteriores (recomendado)` | `Cada copia guarda una versión fechada; los archivos sin cambios se comparten, así que solo cuesta lo que cambia. Requiere un disco NTFS local; se conservan las últimas 30 versiones.` | `Ejecutar cada día a las` |
| fr | `Conserver les versions précédentes (recommandé)` | `Chaque sauvegarde enregistre une version datée ; les fichiers inchangés sont partagés, donc seul ce qui change coûte. Nécessite un disque NTFS local ; les 30 dernières versions sont conservées.` | `Exécuter chaque jour à` |
| de | `Frühere Versionen behalten (empfohlen)` | `Jedes Backup speichert eine datierte Version; unveränderte Dateien werden geteilt, es kostet also nur, was sich ändert. Benötigt eine lokale NTFS-Festplatte; die letzten 30 Versionen bleiben erhalten.` | `Täglich ausführen um` |

`LocParityTests` fallisce se una chiave manca in una lingua: eseguirlo.

- [ ] **Step 8: Guida cap. 03** — nel passo 1, sostituire "quale cartella copiare, su quale destinazione, e se i file restano aperti mentre lavori" con: IT `quale cartella copiare, su quale destinazione, se vuoi tenere le versioni precedenti, a che ora eseguirlo ogni giorno, e qualche caso particolare (file aperti, file molto grandi). Il tipo di disco non te lo chiede: lo riconosce da solo.` / EN `which folder to copy, where to, whether to keep previous versions, what time to run it every day, and a few special cases (open files, very large files). It doesn't ask what kind of disk you have: it detects that on its own.`

- [ ] **Step 9: Build, suite intera, prova manuale** — `dotnet build src/RoboKeep.sln --nologo` (0 avvisi), `dotnet test src/RoboKeep.Tests --nologo` (verde). Aprire **Nuovo**: 4 passi, niente domanda sui dischi; spuntare versioni e orario → l'editor che si apre mostra `Versioned` attivo con 30 e la pianificazione giornaliera; salvando, `TrySyncJobTask` crea l'attività di Windows (già gestito da `OnNewJob`).

- [ ] **Step 10: Commit** (dopo conferma dell'utente)

```
feat: wizard senza la domanda sul tipo di dischi, con versioni e pianificazione giornaliera
```

---

### Task 9: Rilascio 1.8.0 (solo dopo conferma dell'utente)

- [ ] `dotnet test src/RoboKeep.Tests -c Release --nologo` cinque volte di fila: sempre verde.
- [ ] Attendere la CI verde su `main` (`gh run list --limit 1`).
- [ ] `<Version>1.8.0</Version>` in `src/Directory.Build.props`; `## [Unreleased]` → `## [1.8.0] - <data> — Persistent disk rest` con sezione Downloads (copiare quella della 1.7.0 cambiando il numero).
- [ ] Commit `release(v1.8.0): disco a riposo persistente`, tag `v1.8.0`, push di entrambi; seguire il workflow Release con `gh run watch --exit-status` e controllare il codice d'uscita, poi `gh release view v1.8.0`.
- [ ] Ricordare all'utente di aggiornare le copie installate e l'attività pianificata.

---

## Auto-verifica del piano

- Copertura: correzioni 1-6 dell'analisi → Task 1, 2, 3, 6/7; miglioramento 1 (disco a riposo persistente + UI) → Task 4, 5, 6; miglioramento 4 (test) → Task 1; wizard → Task 8. Fuori scope dichiarato: aggiornamenti, SMART, anteprima `/MT`.
- Nomi coerenti tra task: `FaultedDisk`, `FaultedDiskStore.{Load,Find,Mark,Clear,ExpiryDays}`, `JobLastResult.{HardwareError,HardwareErrorDetail}`, `JobResult.HealthWarnings`, `BackupHealth.HardwareError`, `EmailService.{BuildSubject,BuildBody}`, chiavi `Hw_SkippedPersisted`, `Email_HardwareError`, `Health_Hardware`, `Health_CountHardware`, `Main_ReenableDisks`, `Main_ReenableDisksConfirm`.
- Ogni task ha test o prova manuale esplicita e un commit.
